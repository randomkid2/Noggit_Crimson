# Attribution, licensing, and release compliance

This is a **fork**, and it is intended for public release. This file records what it derives
from, what it bundles, and what must not end up in it.

Not legal advice. The licence obligations below are the ones the licence texts state on their
face; verify anything load-bearing before you publish.

## This fork

| | |
|---|---|
| Upstream | [Marlamin/noggit-red](https://github.com/Marlamin/noggit-red) |
| Forked at | `6f0776d4` (2025-01-17) |
| Licence | **GPL-3.0** — unchanged, and it cannot be changed |
| Branch | `feature/tc335-db-editing` |

Noggit Red is GPL-3.0 (see `COPYING`). This fork is a derivative work, so it **must** be
distributed under GPL-3.0 with corresponding source. There is no option to relicense, close, or
dual-licence it, and that applies to every new file added here too.

### Statement of modification (GPL-3.0 §5(a))

GPL-3.0 requires modified files to carry prominent notices stating that they were changed and
when. Changes made in this fork:

| Date | Change |
|---|---|
| 2026-07-30 | `CMakeLists.txt` — raised `CMAKE_MINIMUM_REQUIRED` from 3.3 to 3.5 for CMake 4.x compatibility |
| 2026-07-30 | Added `docs/`, `tools/dev-db/`, `ATTRIBUTION.md` — TrinityCore 3.3.5 database-editing groundwork. No existing source files modified. |
| 2026-07-30 | Added `tests/fixtures/` — captured and synthetic `information_schema` metadata for schema-drift tests. No game or server content. |
| 2026-07-30 | Added `docs/plans/M0-schema-layer.md` — design for the database schema introspection layer. |
| 2026-07-30 | Added `src/noggit/database/` (`ColumnInfo.hpp`, `SchemaModel.hpp/.cpp`) — schema capability model. First new application source in this fork; carries the project GPL header. |
| 2026-07-30 | Added `tests/` — standalone Catch2 target for the schema layer. The repository had no test framework before this. |
| 2026-07-30 | Added `src/noggit/database/` connection and introspection layer (`ConnectionConfig.hpp`, `WorldDatabaseConnection.hpp/.cpp`, `SchemaIntrospector.hpp/.cpp`) plus live integration tests. |
| 2026-07-30 | Added the Qt-free logic layer for M1–M4: `TileCoordinates`, `SpawnTypes`, `SpawnQuery`, `ChangesetBuilder`, with 98 test cases. |
| 2026-07-30 | Added `DatabaseSettings.hpp/.cpp` — QSettings adapter. Now compiled and linked into `noggit.exe`. |
| 2026-07-30 | `src/mysql/mysql.cpp` — fixed two upstream defects that prevented `USE_SQL` from ever building: `#include <driver.h>` corrected to `<cppconn/driver.h>` (CMake and the README both resolve the include path to the parent of `cppconn/`), and added the missing `<QtWidgets/QMessageBox>`, `<memory>`, `<sstream>`, `<string>`. Verified by a successful link. |
| 2026-07-30 | Added M4/M5 modules: `ChunkTransform`, `GmCommands`, `ChangesetArchive`, `DoctorReport` + `DoctorConnectionChecks`, and split the pure SQL builders and row decoders into `SpawnQueryDetail`. |
| 2026-07-30 | Added `SqlFormat::rotationComponent` — quaternion components need significant-digit precision, not the coordinate formatter's fixed decimals. |
| 2026-07-30 | `src/noggit/DBC.h`, `src/noggit/DBC.cpp` — defined `gGameObjectDisplayInfoDB`, which upstream declares but never defines, so the first code to touch it was an unresolved external. Added `CreatureDisplayInfoDB` and `CreatureModelDataDB` wrappers (field layouts verified against `dist/definitions` for 3.3.5.12340), opened via a per-file helper so one missing DBC cannot abort the other eighteen as the single upstream `try` block does. |
| 2026-07-30 | `src/noggit/ui/windows/settingsPanel/SettingsPanel.{h,cpp}` — added the dev-schema field and the read-only safety notice, built in code rather than in the shared `.ui` form to avoid a merge conflict in generated XML. Also fixed four upstream QSettings defaults that fell back to `"127.0.0.1"` for the user, password, database and port keys, which put a hostname in three text fields and made the port parse as 0 on a fresh install. |
| 2026-07-30 | Added `DisplayResolver`, `ModelPathFixup`, `SpawnSceneCache` — display-id → model resolution, an anchored Qt-free `.mdx`/`.mdl` → `.m2` rewrite, and the per-tile drawable spawn store. |
| 2026-07-30 | `src/noggit/rendering/WorldRender.{hpp,cpp}`, `src/noggit/MapView.{h,cpp}` — database spawn overlay. Spawn transforms are appended to the existing `models_to_draw` map, so the overlay reuses the instanced M2 path and adds no shader or draw call; `MapTile::object_instances` is deliberately untouched so database spawns cannot reach the ADT save path. Adds a View-menu toggle and an explicit load action. |
| 2026-07-30 | `src/noggit/database/SpawnTypes.hpp` and 20 use sites — renamed `ValidationIssue::Severity::ERROR` to `BLOCKING`. `<wingdi.h>` defines `ERROR` as `0`, so the enum could not compile in any translation unit reaching a Windows header; the defect was latent only because the header had never been included from UI code. |
| 2026-07-30 | `src/noggit/MapView.cpp` — bound an OpenGL context around every mutation and destruction of the spawn cache. Releasing the last reference to a `Model` destroys its GL vertex arrays, and `OpenGL::Scoped`'s destructor throws when no context is current, which terminates the process from a destructor. Reproduced as a hard crash on the second load; the same guard `~MapView` already applies to its tools. |
| 2026-07-30 | Split the spawn load into `Load spawns (this tile)` and `Load spawns (all loaded tiles)...`, the latter running a pre-flight `COUNT` and confirming above 2000 spawns. Added `creatureCountSql` / `gameObjectCountSql` / `countTile`. Against a populated world database the multi-tile load can queue thousands of asynchronous model loads and present as a hang. |
| 2026-07-30 | Database spawns are culled by view distance directly rather than through `ModelInstance::isInRenderDist`, whose size ladder culls anything with `size_cat == 0` beyond 300 yards. `size_cat` is only ever populated from the MDDF/WDT path, so every database spawn would vanish at barely half an ADT tile. |
| 2026-07-31 | `src/noggit/Model.{h,cpp}` — record `_replaceable_texture_types`, the original M2 texture type per slot. The loader flattens every replaceable slot to `-1` and points it at `black.blp`, destroying the only evidence of which slots a creature's display id is meant to fill. Nothing existing reads the new member, so behaviour is unchanged. |
| 2026-07-31 | `src/noggit/rendering/WorldRender.cpp` — database spawns draw in their own pass grouped by (model, display id), binding that display's skins for the call and restoring them immediately. Creature skins come from `CreatureDisplayInfo.TextureVariation`, not from the M2, and `Model::_textures` is shared, so a skin left applied would repaint every other user of that model. Reuses the existing M2 program and `ModelRender::draw`; no shader or pass logic is duplicated. |
| 2026-07-31 | Added `src/noggit/DevBridge.{hpp,cpp}`, `tools/dev-bridge/`, `docs/dev-bridge.md` — a loopback command socket for driving the editor from a script, so rendering can be verified without a human at the window. Gated twice: excluded from the source list unless `-DNOGGIT_DEV_BRIDGE=ON` (default OFF), and listens only when `NOGGIT_BRIDGE_PORT` is set, bound to `QHostAddress::LocalHost`. Uses the already-linked `Qt5::Network`; no new dependency. |
| 2026-07-31 | `src/noggit/MapView.{h,cpp}` — `loadDatabaseSpawns` now returns its outcome as text and takes an `interactive` flag, so the menu raises dialogs while a script gets the same line back. Added `handleBridgeCommand`, which implements the bridge commands here rather than in `DevBridge` because they need the camera, world and spawn cache — making `DevBridge` a friend would have traded a clean seam for a shortcut. |
| 2026-07-31 | `src/noggit/rendering/ModelRender.cpp` — fixed the replaceable-texture lookup. `_replaceTextures` is a `std::map` keyed by texture TYPE, but the guard compared that key against the map's `size()`, so a type-11 monster skin in a map holding one entry gave `11 >= 1`, logged "index out of range" and bound nothing. Now a `find()`. Unreachable today because `Model.cpp` forces every `_specialTextures` entry to -1; fixed so it is not a trap for whoever enables that path. |
| 2026-07-31 | `src/noggit/database/DoctorReport.cpp` — case folding now ASCII by hand instead of `std::tolower`. Qt calls `setlocale(LC_ALL, "")` at startup, so under a Turkish `LC_CTYPE` an 'I' folds to a dotless 'i' and the configured writable schema stops comparing equal to itself — firing the loud live-database alarm against the *correct* schema. |
| 2026-07-31 | `tests/GmCommandsTests.cpp` — the locale-independence case asserted `expected.find('.')` against a string literal it had written itself, so both assertions held regardless of what the emitter produced. On a machine with no comma-decimal locale installed the case then returned early and reported verifying locale independence while verifying nothing. Now asserted against the emitted string. |
| 2026-07-31 | `src/noggit/database/SpawnSceneCache.{hpp,cpp}`, `DatabaseSpawnPanel`, `WorldRender.cpp`, `MapView.{h,cpp}` — spawn rotation, selection highlight and camera focus. Rotating a gameobject rewrites `rotation0..3` as well as `orientation`, because the core reads the latter and the client renders the former; writing one without the other produces a spawn that faces differently in game than in the editor. |
| 2026-07-31 | `src/noggit/scripting/script_global.cpp`, `script_model.cpp`, `src/noggit/tools/ScriptingTool.cpp` — made scripted object placement and removal undoable. `add_m2`, `add_wmo` and `model::remove` passed `action=false`, which skips the `registerObjectAdded`/`Removed` calls, so the scatter brush shipped in `scripts/prop_placer.lua` could paint hundreds of models that Ctrl+Z could not touch. `ScriptingTool::onTick` now opens an action with the `eLMB` modality controller so one brush stroke is one undo step rather than one per frame, and is gated on the left mouse button as every other brush tool is — without that gate the brush fired for as long as a chunk stayed *selected*, which outlives the click. |
| 2026-07-31 | Added `src/noggit/database/GameTeleBuilder.{hpp,cpp}` and `tests/GameTeleBuilderTests.cpp` — exports project bookmarks as `game_tele` rows, reachable from Assist ▸ Export bookmarks as game_tele SQL. Pure, no Qt or database. The camera-yaw-to-server-orientation conversion is a 180° turn rather than a units change, because the two frames disagree about axis direction; derived in the header and pinned at four cardinals so a simplification that drops it fails a test instead of shipping teleports that face backwards. |
| 2026-07-31 | Added `src/noggit/ui/DatabaseSpawnPanel.{hpp,cpp}` — the spawn editing surface: load, list, select, move mode, pending changes, save and discard. `MapView::mousePressEvent` takes the click ahead of the active tool while move mode is on, so placing a creature does not also raise terrain under it. |
| 2026-07-31 | `SpawnSceneCache` now keeps the source `creature`/`gameobject` row per entry and tracks a dirty flag, so an edit can be written back. Moves convert to server coordinates through `SpawnPlacement::serverPositionFor`, the tested inverse of the load path, so an untouched spawn re-emits the coordinates it was read with. |
| 2026-07-31 | `WorldDatabaseConnection::executeScript` — runs a whole changeset. `execute()` sends one statement, so a multi-statement changeset was rejected wholesale: safe, but it made every emitted file unapplicable. Splits on statement boundaries and runs them in order on one connection, because `SET @CGUID` only survives on the connection that set it. Routes every statement through `execute()`, so the DEV_WRITE guard cannot be bypassed by the new entry point. |
| 2026-07-31 | Added `src/noggit/ui/GroundEffectSetEditor.{hpp,cpp}` — authoring for ground effect sets, reachable from Tools ▸ Ground Effect Sets. Noggit could already assign an existing effect id to a texture layer, derive the per-cell layer map from the alpha maps (`TextureSet::updateDoodadMapping`) and paint the exclusion stencil, but it could not *create* a set: `GroundEffectsTool` only ever calls `getByID`/`CheckIfIdExists`. This writes `GroundEffectTexture.dbc` and `GroundEffectDoodad.dbc` through `DBCFile::save()`, which emits them under the project's own `DBFilesClient/` — a patch, never the client install. Also bulk-assigns a set to every layer using the selected texture across a tile or all loaded tiles. |
| 2026-07-31 | `src/noggit/tools/TexturingTool.{hpp,cpp}`, `src/noggit/MapView.{h,cpp}` — added `selectedTexturePath()` and `cameraPosition()` accessors so the ground effect editor can target the working texture without being handed the texturing widget. |
| 2026-07-31 | `CMakeLists.txt` — excluded the four connector-dependent database sources from the build when MySQL Connector/C++ is absent. `collect_files` globs `src/noggit` recursively while the connector include path is added only when found, so a plain `-DUSE_SQL=OFF` build failed with `WorldDatabaseConnection.cpp(5,10): error C1083: Cannot open include file: 'cppconn/driver.h'`. Confirmed by building it, not by inspection. This restores the no-SQL configuration, which matters for a public fork whose users mostly will not have Connector/C++ installed. |
| 2026-07-31 | Added `src/noggit/TerrainRules.{hpp,cpp}` and `tests/TerrainRulesTests.cpp` — rule-driven automatic texturing by slope and height. Pure: no Qt, no OpenGL, no MapChunk; the world walk is templated so no Noggit header enters the header. Rules resolve by an explicit total order (priority, then specificity, then strength, then texture name, then list index) rather than first-match, because first-match makes the result depend on the order the user happened to add rules in and is unreproducible across sessions. |
| 2026-07-31 | Added `src/noggit/ErosionKernel.{hpp,cpp}` and `tests/ErosionKernelTests.cpp` — thermal erosion (angle-of-repose relaxation) on a plain height lattice. Thermal rather than hydraulic on purpose: hydraulic erosion is a simulation you stop rather than one that finishes, whereas a surface already at or below repose is a fixed point, so the kernel is idempotent and mass-conserving — both properties are asserted, not assumed. |
| 2026-07-31 | Added `src/noggit/AmbientOcclusion.{hpp,cpp}` and `tests/AmbientOcclusionTests.cpp` — horizon-sampled ambient occlusion baked into terrain vertex colours (MCCV). Deliberately does not include `MapHeaders.h` for `TILESIZE`/`UNITSIZE`: that header opens with `<glm/vec3.hpp>` and the standalone test target has no glm include path, so the two constants are restated with their source cited. |
| 2026-07-31 | Added `src/noggit/AlphaIntegrity.{hpp,cpp}` and `tests/AlphaIntegrityTests.cpp` — detects and repairs alpha-map states an ADT can legally store but no renderer can display: layer weights that do not sum to full coverage, layers referencing textures that carry no weight anywhere, and duplicate texture layers. Distinct from the two fixes already in the tree (`Alphamap::readNotCompressed`'s row/column-63 duplication and `TextureSet::convertToBigAlpha`), which it documents rather than repeats. |
| 2026-07-31 | `tests/CMakeLists.txt` — wired the four modules above into the standalone test target. That file is hand-maintained while the root `CMakeLists.txt` globs `src/noggit` recursively, so a new module builds into the application automatically but is silently untested until it is added here. |
| 2026-07-31 | Added `src/noggit/tools/ErosionTool.{hpp,cpp}` and `src/noggit/ui/ErosionToolSettings.{hpp,cpp}` — the erosion brush over `ErosionKernel`, plus `editing_mode::erosion` in `tool_enums.hpp`. The enumerator is appended rather than inserted because `MapView` indexes its tool vector by these values directly, so a tool's position in `MapView::createGUI` must equal its enumerator. `onTick` is gated on the left mouse button, which is safe here and was not in `ScriptingTool`: nothing in this path is an edge detector, so a skipped tick is equivalent to a tick with nothing to do. `deltaTime` is deliberately unused — thermal erosion has a fixed point, so frame rate changes how quickly a stroke settles and not what it settles to. |
| 2026-07-31 | Added `src/noggit/ui/AutoTextureDialog.{hpp,cpp}`, `src/noggit/ui/AmbientOcclusionDialog.{hpp,cpp}` and `src/noggit/ui/AlphaIntegrityReport.{hpp,cpp}` — the interactive surfaces over `TerrainRules`, `AmbientOcclusion` and `AlphaIntegrity`. All three are modeless dialogs on the Tools menu next to the ground effect editor rather than entries under Assist: each owns a scope selector and reports what it examined before it writes, while every Assist entry acts on the ADT under the camera the instant it is clicked. `AmbientOcclusionDialog` is a dialog rather than a `Tool` because `editing_mode::mccv` already belongs to `VertexPainterTool` and `ShaderTool`. Note `Noggit::AlphaIntegrityReport` (the per-chunk value struct) and `Noggit::Ui::AlphaIntegrityReport` (the window) differ only by namespace; both headers call the collision out. |
| 2026-07-31 | `src/noggit/MapView.cpp` — registered the four modules above. `ErosionTool` is **appended** to the `_tools` sequence in `createGUI` and nothing above it moves: `MapView` indexes that vector with `editing_mode` values directly (`selectedTexturePath`, and the `paint`/`object` resets in the destructor), so a tool's position must equal its enumerator and inserting anywhere earlier silently hands every later tool the wrong slot. The three dialogs share the separator the ground effect editor already opened rather than adding their own. |
| 2026-07-31 | `src/noggit/Action.{hpp,cpp}`, `src/noggit/MapChunk.{h,cpp}` — vertex-colour undo now restores the MCCV *flags*, not just the colours. `initMCCV` sets both `hasMCCV` and `header_flags.flags.has_mccv`; undo restored only the `mccv[]` array, so painting vertex colour on a chunk that had none and pressing Ctrl+Z looked correct — a chunk with no MCCV block already reads as neutral white, so the colour restore is a visual no-op — while `MapChunk::save` (which gates the whole block on the runtime flag, not the header bit) then wrote an MCCV block and a non-zero `ofsMCCV` into an ADT that never had either. Pre-existing and tree-wide: `MapChunk.cpp:891` and `:936` are the shipping vertex-paint entry points. The ambient-occlusion bake did not introduce it, it made it a bulk operation. Fixed by snapshotting both flags in each direction and adding `MapChunk::setHasMccv`, the counterpart to `initMCCV`, which can only ever *set* the flag while undo has to *clear* it. |
| 2026-07-31 | `src/noggit/ActionManager.cpp` — `endAction` clears `_cur_action` before the first thing that can throw. `Action::finish()` allocates the redo snapshot, so a bulk operation could leave it throwing `std::bad_alloc` with `_cur_action` still set; nothing ever cleared it again, and `beginAction` returns the running action rather than starting a new one, so from that point every stroke in the session joined a dead action that could never be closed and undo stopped working entirely. The action is deliberately left on the stack on failure: its undo data was recorded as the edits happened and is intact, which is the half that matters after an operation blows up. |
| 2026-07-31 | `src/noggit/ui/AutoTextureDialog.cpp`, `AlphaIntegrityReport.cpp`, `AmbientOcclusionDialog.cpp`, `tools/ErosionTool.cpp`, `ErosionKernel.{hpp,cpp}` — twelve defects found by adversarial review of the four new surfaces, each confirmed by two independent refutation attempts before being fixed. The ones worth naming: the AO bake held raw `MapTile*`/`MapChunk*` across `QProgressDialog::setValue`, which pumps the event loop and lets `MapView::tick()` unload those very tiles; the auto-texture dialog resolved "this ADT tile" from the live camera at apply time, so it could paint a tile the preview never examined; the erosion lattice used half the terrain's x resolution, making every second x-edge read twice the true slope; and the alpha report's row-selection camera focus aimed at the sky. |
| 2026-07-31 | `src/noggit/MapView.cpp` — three dev-bridge commands: `menus` lists every menu entry the running application actually offers, `tools` lists the registered tools and flags any whose list position does not equal its own `editing_mode` value, and `trigger <substring>` activates a menu entry (refusing an ambiguous substring rather than guessing). Compile and link evidence proves a `QAction` was constructed; it does not prove it was added to a menu a user can open, and it does not prove that activating it survives. Both gaps had to be closed by asking the running process. The tool-index invariant in particular lives in the agreement between `tool_enums.hpp` and `createGUI`, where nothing checks it. |
| 2026-07-31 | Added `src/noggit/ui/SpawnTilePicker.{hpp,cpp}` and extended `src/noggit/ui/DatabaseSpawnPanel.{hpp,cpp}` — multi-tile spawn loading. The picker **subclasses upstream's `minimap_widget`** rather than drawing a second grid; the base already implements click-select, shift 3×3 brush, ctrl-deselect, drag-paint and the map's own minimap. Its "loaded" marker is three-state (`NONE`/`CACHED_ONLY`/`DRAWN`), because the renderer only draws spawns for tiles it is already walking (`WorldRender.cpp:429` iterates `_loaded_tiles_buffer`) and Noggit streams roughly 5×5 around the camera — so picking a distant block loads spawns that are editable and in the changeset but not on screen. Marking that honestly, rather than painting every picked cell as loaded, is the difference between a useful load and one that reports success at an empty viewport. A poll timer refreshes the classification because which ADTs are streamed changes as the camera flies. |
| 2026-07-31 | `src/noggit/MapView.{h,cpp}`, `src/noggit/ui/tools/ViewportGizmo/ViewportGizmo.{hpp,cpp}`, `src/noggit/database/SpawnSceneCache.{hpp,cpp}` — axis-drag translate/rotate gizmo for database spawns. A spawn's `ModelInstance` is deliberately **not** passed to `handleTransformGizmo`: that compiles, since `ModelInstance` is a `SceneObject`, but it calls `updateTilesEntry(..., model_update::add)`, which reaches `adt->add_model()` and would register a server-side spawn into `MapTile::object_instances` — the ADT MDDF save path this fork keeps DB spawns structurally out of. It would also key undo on `uid`, which is 0 for every spawn, and write `pos`/`dir` without touching the source row, so the spawn would move on screen while the changeset stayed empty. A separate `handleDetachedGizmo` seam avoids all three; rotation routes through `SpawnSceneCache::rotateTo` so the gameobject `rotation0..3` handling is not bypassed. |
| 2026-07-31 | `src/noggit/MapView.{h,cpp}` — `loadDatabaseSpawnsForTiles` takes an explicit tile list, and `loadDatabaseSpawns` became a thin wrapper over it. The panel had reimplemented the connection setup, schema introspection, unsaved-changes warning, count threshold and the OpenGL `makeCurrent` + `scoped_setter` guard; a second copy of that is what made a multi-tile load fail a camera-tile check it had no reason to care about. Callers pass `::TileIndex` in ADT `(x, z)` order and the transposition to the server's `(x, y)` happens inside, so a caller cannot get the two frames the wrong way round. |
| 2026-07-31 | `src/noggit/MapView.cpp` — dev-bridge `mccvcheck`, which round-trips a vertex-colour edit on a chunk with no MCCV block and reports `hasMCCV` and `header_flags.flags.has_mccv` at each step. It exists because the defect it checks for is invisible on screen: a chunk with no MCCV block already reads as neutral white, so restoring the colour array is a no-op and an undo that leaves the flags set looks perfect. Measured against the running editor over tile 31,49: `before=0,0 after_edit=1,1 after_undo=0,0 after_redo=1,1`. The command restores the world before returning. |
| 2026-07-31 | `src/noggit/application/ApplicationEntry.cpp`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.{hpp,cpp}` — `--project`, `--map` and `--goto` open a project and enter a map with no window interaction, so the dev bridge can drive a real session instead of one assembled in a test. Every addition sits inside `#ifdef NOGGIT_DEV_BRIDGE_ENABLED` and is absent from a default build, for one reason: `openMapUnattended` calls `enterMapAt` with `uid_fix_mode::none` rather than `check_uid_then_enter_map`, which may raise the UID fix window — a modal, and a modal is a hang when there is no human present. That makes it fine for looking at a tile and wrong for editing one, since a session that skipped the UID check and then saved could reuse unique IDs; the header carries that warning and the session logs it on entry. `--goto` takes TrinityCore server coordinates and converts them through `SpawnPlacement::positionFor`, the same tested seam the bridge's own `goto` uses, rather than by hand. The project-open path mirrors `NoggitProjectSelectionWindow` minus the window and is kept in step with it by hand, which the comment says out loud. |
| 2026-07-31 | Added `src/noggit/AssetScan.{hpp,cpp}` and `tests/AssetScanTests.cpp`, plus the Assist ▸ `Report missing assets...` entry in `src/noggit/MapView.cpp` — every model, world model and texture the loaded tiles reference, and which of those the client data chain cannot produce. The module's split runs *through* the header rather than between two of them: everything with an out-of-line definition depends on nothing but the STL, while the world walk is a set of templates purely so `World.h`, `MapTile.h`, `Model.h`, `WMO.h` and `ClientData.hpp` stay out of it — that is what lets the aggregation half link into the standalone test target with no client install, no Qt and no build-system conditionals. Probes are memoised per distinct asset because `ClientData::exists` walks every open archive on a miss and a map resolves a few hundred distinct `.blp` names across tens of thousands of references; without it the scan presents as a hang. Paths are lower-cased by an explicit ASCII range test rather than `::tolower`, which has undefined behaviour for the bytes ≥ 0x80 that ADT texture lists and DBC string tables really carry — `ClientData::normalizeFilenameInternal` has exactly that bug and this does not reproduce it. Terrain textures are read through a plain 16×16 chunk loop rather than `World::for_all_chunks_on_tile`, which calls `mapIndex.setChanged` on entry and would put unsaved-change prompts in front of a user who only asked a question. `Missing` and `Unreadable` are kept apart because the fix differs — ship the file versus replace the corrupt one — even though both render as nothing in game. |
| 2026-07-31 | Added `src/noggit/UidCollisionLog.{hpp,cpp}` and `tests/UidCollisionLogTests.cpp`; `src/noggit/world_model_instances_storage.{hpp,cpp}` record each repair and `src/noggit/World.h` gains a narrow `uidCollisionLog()` accessor, reachable from Assist ▸ `UID collision report...`. Noggit already renumbers duplicate unique IDs in memory as instances load, and that repair is deliberately untouched — `newGUID()` is still called exactly once per collision and its result is still what the instance is stored under, because the renumbered uid is what the tile is then saved with and changing it would move real object placements. What was missing is the evidence: the whole collision set collapsed into one `_uid_duplicates_found` bool that `MapView.cpp:4670` turns into a single modal naming nothing, so a mapper hit by a `uid.ini` desync is told that "a" uid was in use and cannot learn which objects were affected, in which tile, or how many. The record is taken before the uid is overwritten and before the instance is moved, both of which destroy what it is about. The log holds its own mutex so a reader on the UI thread never waits on the storage lock, which instance loading holds for the whole of an add; it caps at 4096 records and keeps the *first*, since the earliest collisions identify where the desync came from and later ones repeat it; and it is deliberately not emptied by the storage's `clear()`, whose only route is `MapIndex::fixUIDs` — the operation a user runs *because of* these collisions, so wiping the evidence there would destroy it at the moment they went looking for it. The header is STL-only, which is what admits the recorder to the standalone test target and why the tile is kept as plain indices rather than a `TileIndex`; an out-of-grid position is recorded as "no tile" rather than clamped to one it does not belong to. `World.h` exposes the log rather than the storage because widening access to reach it would hand every caller the instance maps as well. |
| 2026-07-31 | `src/noggit/scripting/scripting_tool.{hpp,cpp}`, `src/noggit/tools/ScriptingTool.cpp` — **supersedes the `ScriptingTool::onTick` left-mouse gate recorded in the scripted-placement row above.** Gating `onTick` the way every terrain brush does broke the scripting brush outright: `sendBrushEvent` drives an edge detector comparing the previous button state against the current one, so returning early whenever the button was up meant the "up" state was never observed — `on_left_click` fired once per session, and `on_left_release` and every right-button callback never fired at all. `onTick` now calls it unconditionally and `sendBrushEvent` self-gates instead: idle with nothing pending it dispatches nothing, allocates no event object and opens no undo action, and it opens its own action with the `eLMB`/`eRMB` modality only when a callback is about to run, so a stroke is still one undo step rather than one per frame. The action moved into `sendBrushEvent` because the release edge lands on the tick *after* `MapView` closed the caller's LMB-modal action, so there is provably none open for it and a world edit with no action running dereferences a null `NOGGIT_CUR_ACTION`; `beginAction` returns the running action untouched when there is one (`ActionManager.cpp:64-65`), so a caller that opens its own keeps ownership and none of this fires. An action opened on a tick with no button held is closed immediately — it carries no modality controller, `endActionOnModalityMismatch` returns early on `eNONE`, and it could therefore never be closed at all, leaving `undo()` to assert with it still running. The per-tick contract is stated in the header, and the fallback for a caller that ignores it disarms itself the moment it observes one idle tick. |
| 2026-07-31 | `src/mysql/mysql.{h,cpp}` — removed the `CREATE DATABASE IF NOT EXISTS` that upstream ran on **every** `connect()`, and bound the whole seam to the nominated writable schema. As shipped, enabling UID storage and pointing Noggit at a production world database would have it create a schema and a table there before doing anything else: the project's central safety claim falsified by the application itself, in inherited code rather than in anything this fork wrote. Two changes, in this order. The `CREATE DATABASE` is gone outright, so a schema that does not exist is now a reported connection failure instead of something conjured into being. And nothing opens a connection at all unless `project/mysql/db` is exactly `project/mysql/dev_schema`, which puts the surviving `CREATE TABLE`, the `INSERT` and the `UPDATE` beyond reach of any other schema. The policy is deliberately not reimplemented here — the schema comes from `Database::DatabaseSettings` and the enforcement from `WorldDatabaseConnection`'s `DEV_WRITE` guard, the same path the fork's own database layer is already bound by, because a second parallel policy is a second thing to get wrong; the explicit check ahead of it is redundant on purpose, firing before a socket is opened and phrased in terms of UID storage rather than changesets. `testConnection` now opens `READ_ONLY`: a button whose entire job is to test reachability must not be the thing that writes to a server, and it correctly still reports success against a live database that UID storage itself will refuse. This also supersedes the `<driver.h>` include fix recorded above, by removing the connector headers from the file entirely — every statement goes through `WorldDatabaseConnection` now. Upstream's connect-per-call remains, unrestructured and documented: it is tolerable only because these five functions run a handful of times per session, and it is exactly why the fork's per-tile spawn queries never extended this seam. `mysql.h` also gained an include guard in place of `#pragma once`, per the repo's own rule. |
| 2026-07-31 | `docs/schema-335.md` — removed infrastructure detail from the provenance block ahead of publication. The precise server build and its locality, the measured database's table count, the core revision hash and the name of the particular TrinityCore fork it was cross-checked against, and the enumeration of one operator's four custom tables all described a private machine rather than the 3.3.5 schema; the note comparing it against "a second, heavily customised world database on the same server" disclosed a database inventory outright. The column-level facts are untouched — they are the schema ground truth this document exists for, and they remain measured from `information_schema` rather than quoted from anywhere. What replaces the removed material states plainly the limitation those details had been carrying implicitly: the measured `world` is not a pristine TDB install, so its table *list* and *count* say nothing about stock TDB 335.25101 and only the column facts do. The `playercreateinfo_spell_custom` / `spell_custom_attr` trap is kept, being a fact about stock TrinityCore rather than about anyone's server. |
| 2026-07-31 | Removed `resources/font_awesome.otf`, `resources/segoeui.ttf` and `resources/segoeuisb.ttf`, and rebuilt `src/noggit/ui/FontAwesome.{hpp,cpp}` and `resources/resources.qrc` around their absence. None of the three could lawfully be published from this repository. `font_awesome.otf` is Font Awesome 5 **Pro** Regular 5.14.0 — a paid product whose licence forbids redistributing the font file and specifically forbids committing it to a public repository — and the two Segoe UI faces are Microsoft's, whose name table permits use only as part of a licensed Microsoft product. All three were inherited from upstream, which does not make publishing them someone else's act. `noggit_font.ttf` stays, being Noggit's own icon font. Font Awesome is now resolved at runtime from `<install dir>/fonts` (and the working directory) under any of the usual Free or Pro file names, or from an installed system family preferring the solid face; Segoe UI is resolved by family name through the new `Noggit::Ui::UiFonts` instead of being loaded from a bundled file. With none of them present the application still runs: `FontAwesomeIconEngine` paints a standard Qt icon where one fits the meaning and a short text label otherwise, so every button stays identifiable. That degradation is real and visible, and is documented in the file rather than papered over — Font Awesome Free is SIL OFL 1.1 and may be installed freely, but part of the codepoint list in `FontAwesome::Icons` is Pro-only and stays blank even with Free installed. Nothing in this path downloads a font. `FontAwesome.hpp` also gained an include guard in place of `#pragma once`, per the repo's own rule. |
| 2026-07-31 | `.gitignore` — this repository ignores everything (`/**`) and whitelists back explicitly, and the inherited whitelist un-ignored directory *contents* (`!/src/**`) but never the directories themselves. `/**` matches the bare `src` entry too, so git prunes the directory and never looks inside it; the tracked files there survive only because gitignore does not apply to already-tracked files, which means a **new** file under any whitelisted directory is silently invisible to `git add`. That bit this fork the first time a new source file failed to stage, and it was still latent for `scripts/` (23 Lua/TypeScript files inherited from upstream, tracked but never un-ignored). Both are fixed by un-ignoring each directory alongside its contents. Added whitelist entries for `docs/`, `tools/` and `tests/`, then re-ignored — last matching pattern wins — the generated, machine-specific and secret-bearing files listed under "Not published" below. Ahead of publication two hard re-ignore blocks were added on top: build and editor artefacts, because the broad `!/bin/**` and `!/src/**` whitelists mean an in-source build lands somewhere git is already looking; and every WoW client data extension (`*.mpq`, `*.dbc`, `*.m2`, `*.wmo`, `*.adt`, `*.blp`, …) across the whole tree *including* `tests/` and `src/`, so a fixture cannot be added without noticing. That second block is a copyright control rather than tidiness: one committed `.blp` is redistribution of Blizzard assets, and it is permanent once pushed. |
| 2026-07-31 | `README.md` — rewritten as this fork's front page ahead of publication: what it adds over upstream, the never-writes-to-a-live-database constraint stated before anything else, requirements, quick start, an honest status-and-limitations section, and credit to upstream at the top rather than the bottom. Upstream's original text is **kept verbatim** rather than replaced, demoted under a `# Upstream documentation` heading that retains its LICENSE, BUILDING, SUBMODULES and CODING GUIDELINES sections unaltered. That is deliberate on both counts: the coding guidelines are still the rules this repository holds itself to, and the build instructions are still the ones the upstream project publishes, so dropping either would leave the fork's own documentation as the only surviving record of them and quietly attribute upstream's work to this fork. The new material says plainly that anyone who only wants a map editor should use upstream instead. |
| 2026-07-31 | `src/noggit/ui/tools/AssetBrowser/Ui/AssetBrowser.{ui,hpp,cpp}` — hot-loading of assets dropped into the project folder. The project folder already outranks every MPQ at *load* time (`ClientFile` tries disk before the archive walk), so a dropped `.m2` was always placeable; what was stale was the browser's tree, rebuilt only at construction, on a type-checkbox toggle and on a browse-mode change. Adds a **Rescan** button and an **Auto-refresh** `QFileSystemWatcher` over every directory the scan walks — the watcher does not recurse and assets sit at arbitrary depth, so there is no narrower root to watch — debounced by a single-shot 1 s timer because one file copy raises several change events and a folder copy raises one burst per file. Refuses to watch past 2048 directories and logs why rather than spawning a thread per 60 handles on Qt's Windows backend. Selection, scroll offset and expanded folders are captured and restored around the rebuild with the tree's signals blocked, so a watcher-driven rescan cannot re-enter the preview renderer and construct `scoped_model_reference`s off a file-system event. Also fixed a filter defect: the project-folder scan ignored the WMOs/Models checkboxes that the listfile scan honoured, so unticking a type still left that type on screen if it lived in the project folder; both paths now call one `isAssetTypeEnabled`. |
| 2026-07-31 | `src/noggit/ui/tools/AssetBrowser/Ui/AssetBrowser.{ui,hpp,cpp}` — assets dropped into the project folder now appear without restarting. The loose-file overlay was already live (`ClientFile` stats disk before walking the archives), so a dropped model was always *resolvable*; what was stale was the browser's tree, whose only in-session rebuild was toggling a filter checkbox twice. Adds a Rescan button and a debounced `QFileSystemWatcher` over the scanned directories, capped at 2048 watched folders because Qt's Windows backend spawns a thread per ~60 handles, degrading loudly to button-only rather than silently. The rescan is deferred while the browser is hidden and caught up in `showEvent`: the widget is built for every `MapView` and immediately hidden, and Noggit writes into the watched tree during ordinary editing — every ADT save, and the palette file on every palette change — so an unguarded handler would rescan on a timer through a whole texturing session. Selection, scroll and expansion survive a rescan, restored under a `QSignalBlocker` so the preview renderer is not re-entered from a filesystem event. Also made the project-folder scan honour the M2/WMO filters that the listfile path already honoured, and made extension matching case-insensitive in both the type filter and every browse-mode gate, so a hand-dropped `Foo.M2` is not silently dropped. |
| 2026-07-31 | `src/noggit/rendering/WorldRender.cpp`, `tests/ChangesetArchiveTests.cpp` — two low-severity defects carried from an earlier audit. **The stale model box count.** `ModelRender::drawBox` issues one instanced draw of `count` boxes against `ModelRender::_transform_buffer`, the single per-model buffer that `ModelRender::draw` overwrites with its own instances on every call, so the count in `model_boxes_to_draw` is only meaningful as the size of the *last* batch uploaded for that model this frame. `draw` records it with `emplace`, which keeps the *first* count offered. Upstream those are the same number — `models_to_draw` is keyed by `Model*`, so a model is drawn exactly once per frame — and this fork's database-spawn pass broke that invariant: `db_spawns_to_draw` is keyed by `(model, display id)` and runs *after* the `models_to_draw` loop over the same map, so one model can be drawn several times and the surviving count belongs to a batch the buffer no longer holds. A model appearing both as an ADT doodad and as a spawn, or as spawns under two display ids, therefore had the box pass fetch instance transforms past the end of the buffer whenever the stale count was the larger — an out-of-bounds instanced attribute read, reachable from the ordinary *draw models with box* toggle — and silently drop boxes when it was the smaller. The spawn pass now re-points an *existing* entry at the batch it just uploaded; it never creates one, because an entry's presence is `draw`'s decision about whether the model wants boxes at all. What that cannot recover is stated at the call site: the doodad batch's transforms are already gone from the buffer by then, so with one buffer per model and one box pass after everything, the choice is between the spawn boxes and out-of-bounds reads, not between the spawn boxes and the doodad ones. **The colliding test fixture.** Two archive cases built on a fixed name under the system temp directory and *opened by `remove_all`-ing it*, while `tests/CMakeLists.txt` registers this suite several times over, so two of those processes under `ctest -j` deleted each other's fixture mid-test — a failure that appears only under concurrency and teaches people to re-run until green. Both now use the file's own `TempDirectory`, which claims a randomised name with `create_directory` as an atomic claim and removes the tree from its destructor; that also fixes the leak the explicit `remove_all` at the foot of each case had whenever a `REQUIRE` above it threw first. |
| 2026-07-31 | `src/noggit/ui/TexturingGUI.{h,cpp}` — the texture palette had the asset browser's staleness with none of its accidental workarounds: `tileset_chooser` built its entire list inside its constructor and had no rebuild path at all, and the instance is kept for the whole session (one for the texturing tool's dock, one per texturing brush in the brush stack), so a `.blp` dropped into `<project>/tileset/` genuinely required restarting Noggit. The constructor body becomes `refresh()`, called by a new **Rescan** button, by a debounced `QFileSystemWatcher` behind an **auto** checkbox persisted to `texturePalette/auto_rescan`, and by `showEvent`. Follows the asset browser's decisions — 1 s single-shot debounce, a stated watch budget with loud degradation, and deferring the rescan while hidden — but deliberately narrows the watched set to directories whose *relative* path contains `tileset`, rather than every directory walked: this widget only ever lists a `.blp` under such a path, so a handle spent elsewhere cannot change what is displayed, and narrowing keeps both write-heavy paths (ADT saves under `world/maps`, and `noggit_palettes.json` in the project root, rewritten on every palette edit) out of the watch set entirely. The budget is 256 rather than 2048 because the palette, unlike the browser, exists in several instances at once. The stated cost of narrowing is that a *newly created* top-level `tileset` folder in a project that had none needs one Rescan click. Selection and scroll are restored around the rebuild under a `QSignalBlocker` on the selection model, because that model's `selectionChanged` reaches a handler that calls `makeCurrent()` and builds a `scoped_blp_texture_reference` — real GL work that must not run off a file-system event. The rebuild itself does no GL: `model_item` owns a plain raster `QPixmap` from `QPixmap::fromImage` with no reference into `TextureManager`, and the `blp_texture` that produced it was a stack local inside `BLPRenderer::render_blp_to_pixmap`, destroyed there under the renderer's own offscreen context, so discarding items cannot drop a last texture reference off the GL thread. Also hardened two paths that only became reachable once the scan could repeat: `model_item::data` no longer lets `render_blp_to_pixmap`'s throw escape a `paintEvent` (a truncated or mid-copy `.blp` is now a blank icon and a log line instead of a `std::terminate`), and the project walk uses `skip_permission_denied` under a `try`, since a directory vanishing between two iterator steps would otherwise throw out of a `QTimer` slot. No call-site change was needed at either construction site (`TexturingTool.cpp:197`, `BrushStackItem.cpp:130`): the machinery lives in the class, so both get it and cannot diverge. |
| 2026-07-31 | `src/noggit/WMO.{h,cpp}`, `src/external/blizzard-archive-library/{include/ClientFile.hpp,src/ClientFile.cpp}` — the WMO parser had no validation in the shipped build and its reads were unclamped, which together are an out-of-bounds read on any malformed or wrong-version `.wmo`. Every mandatory-chunk and version guard in both the root and group parsers was an `assert`, and `NDEBUG` is defined in the `RelWithDebInfo` configuration this project ships (`build/noggit.vcxproj`, `ClCompile` block for `RelWithDebInfo`), so none of them existed in the binary. The parse is a fixed chunk *sequence*, not a fourcc-driven dispatch, so a file from any later expansion desynchronises everything after the first differing chunk and the sizes then read from wrong offsets feed `resize()` and pointers aliased over the file buffer. Replaced with real runtime checks (`readChunkHeader` / `readOptionalChunkHeader` / `checkWmoVersion`) that log the file name and what was wrong and then `throw`, which is the idiom `Model::finishLoading` already uses for M2 version rejection (`Model.cpp:64-69`) and which `AsyncLoader::process` catches (`AsyncLoader.cpp:108-121`) to mark the object failed. Every mandatory chunk now also verifies its declared size fits in the remaining file, which is what makes the aliased reads (`MOVT` vertices, `MOCV` colours, `MLIQ` geometry) safe; the group file's flag-gated optional chunks keep upstream's deliberate "rewind 8 bytes and continue" tolerance but hard-fail on an impossible size. Name blocks (`MOTX`, `MOGN`, `MOSB`, `MODN`) are copied out with a guaranteed terminator instead of aliased, because a byte offset from another chunk is not guaranteed to land on a NUL-terminated string inside the file — which changes `WMOGroup`'s constructor to take the MOGN block by `std::vector<char> const&`. Also bounded five indexing bugs the validation exposed: `fogs[0]` on an empty fog list, `MOVI` indices against `MOVT`, `transparency_batches_count` against `MOBA`, the second `MOCV` against the first, and `doodads_per_group`'s doodad-set range against `modelis`. In `ClientFile`, `read()` no longer leaves `_pointer` past the end of the buffer (upstream assigned the unclamped `_pointer + bytes`, after which `getPointer()` handed every caller an out-of-bounds address — measured at 64 on a 10-byte file), always writes its whole destination with the unavailable tail zeroed so a short read is deterministic rather than stale, and `getPointer()` clamps defensively; added `bytesRemaining()` so a caller can check before reading rather than discovering a short read afterwards. |
| 2026-07-31 | `src/noggit/database/ChangesetBuilder.{hpp,cpp}`, `src/noggit/database/SpawnSceneCache.{hpp,cpp}`, `src/noggit/MapView.{h,cpp}`, `src/noggit/ui/DatabaseSpawnPanel.{hpp,cpp}`, `tests/ChangesetBuilderTests.cpp` — spawn **creation and deletion**, which turned the database editor from a spawn adjuster into a spawn editor: until now a user could load, select, move and rotate a spawn but could not place or remove one. The load-bearing part is guid allocation. `creature.guid` and `gameobject.guid` are two independent primary-key sequences, both counting up from 1 with no reserved range, so any number the editor invents in order to have something to select almost certainly names a real row in one or both tables — emitting it would not error, it would overwrite somebody's spawn with no diff to show for it. So `SpawnSceneCache` hands a placed spawn a *provisional* guid from `0xF0000000` upwards, flags it `is_new`, and `ChangesetBuilder::addNewCreature`/`addNewGameObject` **ignore that number entirely**, emitting `SET @CGUID_NEW := (SELECT IFNULL(MAX(guid), 0) FROM creature);` and keying the created rows at `@CGUID_NEW+1`, `+2`, … — two variables for two tables, because one allocation cannot serve both key spaces. The cost is idempotency and it is stated rather than hidden: a row the database has never seen has no key a `DELETE` could name, so the created sections are append-only and the emitted file carries its own `NOT IDEMPOTENT — apply once` block. Deleting by `(entry, map, position)` instead was rejected because it would remove a pre-existing duplicate somebody else authored. Three consequences fall out and are each pinned by a test: `dirtyEntries()` now excludes `is_new` entries, so a placed spawn that is subsequently dragged is emitted **once**, as a creation, and never as an edit keyed off `@CGUID` (which would name a real row); a spawn created and deleted again before saving produces no SQL at all, because `removedSpawns()` reports only guids the database issued; and a guid on both the written and the removed list is now refused, since the `DELETE` would run first and the `INSERT` put the row straight back. Deletion already took the `creature_addon` row with it — verified rather than assumed, and now covered. `clearPending()` drops placed spawns from the view after a save, because their guids are still the editor's invention and keeping them would let the next save either duplicate them or write a fictional guid into an `INSERT`; both it and `discardPending()` bind an OpenGL context, since releasing the last reference to a `Model` destroys vertex arrays from a destructor that throws when none is bound. `MapView::createDatabaseSpawn` resolves the entry id to a display id through a `READ_ONLY` connection with every table and column checked against `information_schema` first, and **refuses** an entry whose template resolves to no model rather than placing something invisible: a spawn with no `ModelInstance` cannot be picked, focused or dragged, so it would exist only as a row in a changeset the user had no way to review. The panel gains an entry-id/kind placement box with a click-to-place mode kept mutually exclusive with move mode, a delete button that confirms only when the deletion will actually emit SQL, and a three-way pending count; Discard now undoes placements and deletions as well as moves, and is documented as the only undo this subsystem has — database spawns do not participate in `ActionManager` and were deliberately not half-wired into it. |
| 2026-08-01 | `src/noggit/WMO.cpp`, `src/noggit/ChunkWater.cpp`, `src/external/blizzard-archive-library/include/ClientFile.hpp` — three memory-safety defects in the malformed-file paths. (1) Every array chunk in a group file was sized `size / sizeof(element)` and then read at its full declared `size`, so a payload that is not a whole number of elements wrote the remainder past the vector — a 13 byte MONR resized to one 12 byte normal and read 13. A shared `elementCount` helper now rejects the chunk at all nine sites (MOVI, MOVT, MONR, MOTV, MOBA, MODR, both trailing MOTV/MOCV chunks and the MOCV that `load_mocv` aliases) in the same style as the existing header checks. (2) A MOBA batch whose `index_count` is not a multiple of 3 is rejected at load: `intersect()` steps `i` by 3 from `index_start` and dereferences `_indices[i..i+2]`, so the last iteration read up to two elements past the batch and past `_indices` entirely when it was the final batch — on every click in the viewport. Rejected at load rather than by bounding that loop, because the format has no partial triangle, `WMOGroupRender` feeds the same count to a `GL_TRIANGLES` draw call, and the check then costs one pass over a short vector per file instead of a branch per triangle per click. (3) `ChunkWater::fromFile` computed the MH2O render mask as `ceil(width * height / 8)` from two `uint8` file fields and read it into a single `uint64_t`; a 255×255 record asked for 8129 bytes into 8. The same record then drove `liquid_layer`'s constructor past its fixed `std::array<liquid_vertex, 9 * 9>`, which is the larger overrun of the two, so the layer geometry is validated against the chunk's 8×8 subchunk grid once, before either is reached. Contrary to the framing this was handed, the mask overrun is **not** a regression from the always-write-`bytes` `read()` contract: upstream's `min(bytes, available)` clamp wrote 8129 bytes too whenever the ADT had that many left after `ofsInfoMask`, which for a real tile it always does — demonstrated under AddressSanitizer, which reports the `WRITE of size 8129` at the `memcpy` on `ClientFile.cpp:82`, not at the zero-fill. The contract is therefore kept and its caller obligation written into the header; every `read()` call site with a non-constant length was audited against it. |
| 2026-08-01 | `src/noggit/MapView.{h,cpp}`, `src/noggit/database/SpawnSceneCache.{hpp,cpp}` — two defects in the spawn create/delete path. **The schema was re-introspected on every click.** `createDatabaseSpawn` is reached synchronously from `mousePressEvent`, and it opened a connection and called `SchemaIntrospector::readModel` — a `SELECT` over `information_schema.columns` for the *whole* schema, roughly a hundred thousand rows against a populated TDB world database — before anything appeared, on the GUI thread, once per attempt to place a creature. The introspected model is now cached on `MapView` and shared by the load, create and save paths, which each had their own copy of the same four lines. What is cached is the measured *result*, never an assumed layout: HARD RULE 3 is about not hardcoding a column name or ordinal, and "does `creature` have `wander_distance`" is still answered from `information_schema` — once instead of per click. The cache is keyed on host, port, user and schema, so pointing Settings at a different server invalidates it rather than leaving it answering for the schema it used to be looking at, and the load action forces a re-read because it is the deliberate "go and read the database" gesture and is on no hot path — which is also how a schema changed under a running editor is picked up without a restart. **A spawn created and then deleted became unreachable.** `SpawnSceneCache::remove` moved every deleted entry into the tombstone list, but `dirtyEntries()` and `newEntries()` walk the tiles, which no longer held it, and `removedSpawns()` skipped it for being `is_new` — so `pendingCount()` returned 0, the panel said "No changes." and greyed out both Save and Discard, and the spawn's `Model` stayed resident with nothing left that could reach it. `remove` now destroys a created spawn outright instead of parking it: it has no row to `DELETE` and nothing to be restored to, since `discardPending()` drops created spawns regardless. The emitted SQL is unchanged and deliberately so — a spawn created and deleted before saving still produces nothing at all, not an `INSERT` followed by a `DELETE` — and `removedSpawns()` keeps its `is_new` filter as a second line of defence, because a `DELETE` naming a provisional guid would remove whatever the real sequence has since put there. That destruction releases the last reference to a `Model`, which destroys OpenGL vertex arrays from a destructor that throws when no context is bound — a terminate, not a catchable error — so `deleteDatabaseSpawn` now binds one, which it did not need to while every deletion was a move. |
| 2026-08-01 | `src/noggit/ui/TexturingGUI.{h,cpp}` — the texture palette's auto-refresh checkbox was inert in the one case it exists for. The watch set was narrowed to directories whose relative path contains `tileset`, and the project root was deliberately never watched, so in a project with no `<project>/tileset` yet the watch set was empty, the watcher stood down, and the user's first-ever texture folder was never noticed. The watcher now falls back to the project folder itself when no tileset directory exists, and re-points onto the real tree as soon as one appears. The project root is write-heavy — `ApplicationProjectWriter` rewrites `noggit_palettes.json` there on every palette edit — so the `directoryChanged` handler gates on one non-recursive listing of the root and refuses to even start the debounce timer until a tileset directory actually exists; a palette save therefore costs a directory listing and no rebuild. The gate uses the scan's own predicate (`normalizeFilenameInternal`, then a substring test for `tileset`) so the two cannot disagree, and the non-throwing `std::filesystem` overloads throughout, because it runs from a Qt slot where an escaping exception is a terminate. Ticking the box also now re-checks the root once, since ticking it does not replay the events that happened while it was unticked. Finally, the checkbox is no longer allowed to claim something it is not doing: if not one directory could be registered, it disables itself with a tooltip and a log line saying so, instead of sitting ticked and silent. |
| 2026-08-01 | Added `dist/themes/CrimsonSlate/` (`theme.qss`, `nodes_theme.json`, 34 generated PNGs) — a third selectable theme. Additive only: no existing theme or source file is touched, and the settings panel discovers it because it lists every `themes/*/` directory containing a `theme.qss`. It styles the widget classes both shipped themes leave to Fusion — `QToolTip`, `QSplitter::handle`, `QMainWindow::separator`, `QTabWidget::pane`, `QHeaderView`, `QAbstractItemView` selection and alternating rows, `QSizeGrip`, `QPlainTextEdit`, `QScrollBar` hover and pressed — and gives every control an explicit `:disabled` state, which is what the existing sheets most visibly lack. Three known-dead selectors in the shipped Dark theme are written correctly here rather than copied: `Noggit--Ui--PaletteList` (Qt spells `::` as `--`, so Dark's `Noggit-Ui--PaletteList` matches nothing), `texturing_brush_level_slider` (set with `setObjectName`, matched by Dark only as `accessibleName`), and `QRadioButton::indicator::unchecked` / `QSlider[...]::vertical`, which are pseudo-states written as sub-controls. The window buttons take real PNG icons through `qproperty-icon` so the frameless title bar stays identifiable with no Font Awesome installed, that font no longer being redistributable in this fork. The PNGs are generated, not drawn: `dist/themes/CrimsonSlate/generate_icons.py` is stdlib-only (no Pillow, no network) and rebuilds all 34 from the palette constants at its head, so a retheme is a value change and a re-run rather than an image editor. |
| 2026-08-01 | `src/noggit/application/ApplicationEntry.cpp`, `src/noggit/ui/windows/settingsPanel/SettingsPanel.cpp` — gave the application a dark `QPalette`, a single font, and a theme that is actually applied at startup. Upstream sets the Fusion style and never touches the palette, so every surface a stylesheet cannot select — `QMessageBox`, native dialogs, menu drop shadows, the combo box popup frame, drag-and-drop indicators, the text cursor, item view branch guides, and every primitive Fusion draws from `Light`/`Midlight`/`Mid`/`Dark`/`Shadow` — was drawn from the *default light* palette over a dark application, whichever theme was loaded. `QApplication::setPalette` now runs before the first widget exists, with all fifteen of the roles named in the design tokens plus the five-step shade ramp (a role left unset keeps its light-palette value, which is how a "dark" palette ends up with pale bevels) and the `Disabled` group, which is what decides whether a greyed-out control is readable or invisible. `HighlightedText` is the dark token rather than the light one because against the `#E8543F` accent that measures 4.85:1 instead of 3.19:1. The theme loader was a lambda inside the settings panel's `currentTextChanged` connection, which made the whole application's appearance a side effect of a signal: the directory scan behind it was **working-directory relative**, and `setCurrentText` is a silent no-op on a non-editable combo when the item is missing, so from a shortcut or a debugger the list came back empty, no signal fired, and Noggit ran completely unstyled with nothing in the log. It is now `Noggit::Application::applyTheme`, called from `main` before any window is constructed and from the combo for live restyling — one implementation, two callers. It resolves `themes/` against `QCoreApplication::applicationDirPath()`, logs the path it tried when the directory or the file is missing or unreadable, and falls back to a named default. `discard_changes` resolves the saved name against what was really found on disk and applies it explicitly in the case where no signal would fire, so the combo can no longer disagree with the stylesheet in force. Relative `url(themes/…)` in a sheet is anchored the same way, because Qt resolves those against the process working directory too and a theme's own images otherwise disappear along with it; the substitution quotes the result, since unquoted QSS `url()` does not survive a path with spaces. `@rpath` substitution is kept, with the trailing separator now stripped from the path rather than from the tail of the stylesheet (the original chopped two characters for a one-character suffix). The global font reuses `Ui::UiFonts::interfaceFont`, the family-resolution helper `FontAwesome.cpp` already had, at the 12px that Qt's 9pt Windows default resolves to at 96 dpi — a family change, not a size change — and the icon-font fallback is untouched because it sets its own font. |
| 2026-08-01 | `dist/noggit-themes/CrimsonSlate/` — rewrote `theme.qss`, regenerated the 34 PNG widget assets, repalletted `nodes_theme.json`, and added `check_qss.py`. The previous revision loaded correctly and changed nothing anyone could see: its palette was a near-clone of the inherited Dark theme (surface `#2E333B` against `#2D2F34`, accent `#4D8FF0` against `#5281B9`) and, despite the name, contained no crimson. What it *did* add was coverage the shipped themes never had — `QToolTip`, `QSplitter::handle`, `QMainWindow::separator`, `QTabWidget::pane`, scrollbar hover/pressed, and an explicit `:disabled` state for every control — and all of that is kept: `check_qss.py --coverage` diffs the two sheets and reports **0 selectors dropped**, 4 added. What changed is the appearance. The palette is now warm-red accent `#E8543F` on a four-step neutral ramp, matching the `QPalette` the application installs so a styled and an unstyled widget agree. The elevation model is **inverted**: surfaces get lighter towards the viewer, so inputs, list rows and the tool strip sit *up* on `#22262E` and menus and tool tips sit up again on `#2A2F38`, where the previous revision sank inputs into a near-black `#101216` well and made every panel read as a grid of holes. Group boxes lose their box entirely — no border, no background, title top-left in `text.dim` with one `stroke.soft` rule beneath it — which is the single largest change on screen, the tool docks being almost nothing but group boxes. One control height (26px) and one corner radius (4px) throughout, no gradient anywhere except the texture-level slider groove, whose gradient is a value ramp and therefore data. Selection is an accent wash at 26% alpha rather than a saturated fill, so a selected texture cell is still visible under its own selection. Three things here were measured rather than assumed, with a throwaway Qt 5.15.2 program rather than by reading the docs. **`rgba()` alpha** accepts a 0–1 float, a 0–255 integer *and* a percentage — the Qt reference is ambiguous and a wrong guess renders as either invisible or fully opaque, silently. **One padding cannot give one height**: `QPushButton` computes `min-height + 2 + 2·vpad`, `QLineEdit` and `QAbstractSpinBox` ignore `min-height` below their own floor and compute `26 + 2·vpad`, and `QComboBox` ignores it too and computes `23 + vpad_top + vpad_bottom` — so a single `padding: 4px` yields 26/34/34/31px, and the sheet instead carries three per-class paddings, including a deliberate 2px/1px asymmetric split that is the only way a combo lands on exactly 26. **Checked tool buttons** were fixed after looking at the running editor: `bg.overlay` on a `bg.raised` toolbar is a 1.13 contrast step and vanishes at icon-button size, which left the 2px accent bar carrying the signal alone — and a 2px vertical line between two icons reads as a *separator*, not as state. Checked is now the accent wash with the bar as its edge. `check_qss.py` ships in the theme directory beside the existing `generate_icons.py`: Qt discards properties, pseudo-states and sub-controls it does not understand without a word, so an invalid one is an invisible failure, and the checker validates all three against the Qt 5.15 reference plus colour literals, `rgba()` arity, brace balance and `url()` resolution. It is extended over the version used on the previous revision to parse the palette out of the sheet's own header — so the contrast table can never drift from the tokens — to require every hex literal in the body to be a declared token, and to diff selector coverage between two sheets. It reports **0 errors, 0 warnings**. Nothing outside `dist/noggit-themes/` was touched; no `.cpp`, `.hpp` or `.ui` file was modified. |
| 2026-08-01 | `src/noggit/MapView.{h,cpp}` — added a frame-timing instrument and the dev-bridge commands `perf` and `perfreset` that read it, as the measuring baseline for renderer work. The status bar's existing `_last_frame_durations` could not serve: it is cleared every second and collapsed to a mean, and a mean is exactly the statistic that hides a 90ms hitch inside an otherwise unremarkable second — which is what "it feels bad" actually describes. `paintGL` now writes one `PerfSample` per call into a fixed 4096-entry ring: entry timestamp, milliseconds spent in the body, and whether the frame drew. No allocation, no locking, no heap on the render path (both it and the command handler run on the GUI thread), and the sorting for percentiles happens once per `perf` invocation on a copy. The `rendered` flag is the point of the design, not decoration: `paintGL` early-returns whenever `_needs_redraw` is clear, and counting those as sub-microsecond frames would let an idle editor report the fastest numbers it ever produces — the sample is taken *before* the early-return test so a declined paint is still counted, as `skipped`. Only the drawing path and that early return are sampled; the lost-context and `preRender`-refused exits drew nothing and are transitions rather than frames, so sampling them would put durations in the window that measure something else. Two numbers are reported rather than one because they answer different questions: `work_*` is CPU-side submission inside `paintGL` (the buffer swap happens after it returns, and GL commands are asynchronous, so this is not GPU time), and `frame_*` is drawn-frame to drawn-frame interval. Percentiles are nearest-rank, so every value reported is one a frame actually took, rather than an interpolated time no frame had. The four scene counts come from the same four calls the status bar's `_status_culling` line uses, not from a second computation. `fps_limit` is reported alongside because `_update_every_event_loop` drives repaints from a `QTimer` at that rate, so `frame_p50` cannot go below `1000/fps_limit` however fast a frame is built — reading it as a hardware result would be wrong, and the same timer is halved to `fps_calcul * 2` for an unfocused window (`onApplicationStateChanged`, `ApplicationInactive`), which was measured making an unfocused baseline report 24 FPS against a focused 62.5 for an identical scene. Behaviour is otherwise unchanged: no existing flag, order or early return was altered, `_needs_redraw`/`_force_single_render` handling is byte-for-byte the previous logic with a brace pair added around one `return`. Verified building in both configurations, since `DevBridge.cpp` is excluded when `NOGGIT_DEV_BRIDGE=OFF` while `handleBridgeCommand` is not: `-DUSE_SQL=ON -DNOGGIT_DEV_BRIDGE=ON` and the default `OFF/OFF` build both link, and the Catch2 suite is unchanged at 396 cases / 276830 assertions. |
| 2026-08-01 | `src/noggit/ui/FontAwesome.{hpp,cpp}`, `src/noggit/ui/FontNoggit.cpp`, `src/noggit/ui/Toolbar.cpp`, `src/noggit/ui/TerrainTool.cpp`, `src/noggit/ui/tools/UiCommon/ExtendedSlider.{hpp,cpp}`, `ExtendedSliderUi.ui`, `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.cpp` — widget-level legibility and density work on the editing UI. **Both icon engines ignored `QIcon::Mode` entirely.** `FontAwesomeIconEngine::paint` and `FontNoggitIconEngine::paint` chose the pen from `QIcon::State` alone and accepted `mode` without ever reading it, so a *disabled and checked* button painted at full strength. `mode` is an override now and `state` still chooses between the other two colours. It has to: the probe widget carries an `accessibleName` that a QSS rule matches, QSS `color` writes `WindowText`, `Text` and `ButtonText` together, so a theme can author exactly one enabled colour and one `:disabled` colour on that selector and there is no third slot to spend — while `QIcon::Off` is what Qt passes both for an unchecked button and for one that is not checkable at all, which the engine cannot distinguish. An intermediate revision of this work keyed the pen off `mode` alone and left the checked state to the stylesheet; it was measured only against CrimsonSlate and is reverted. Per theme, over each theme's own button surface — checked / unchecked / unchecked had `Off` shared `Normal`: Dark `QToolButton` 4.03 / 10.25 / 3.65, Dark `QPushButton` 3.32 / 9.31 / 3.32, McNet 8.30 / 2.11 / 8.30, CrimsonSlate 9.39 / 2.39 / 9.39. Sharing with `Normal` is therefore not a contrast win everywhere — in Dark it takes unchecked icons from 10.25 down to 3.65, that theme's pair being a hue change (blue = checked) rather than a brightness ramp — and it buys the win in the other two sheets by deleting the only checked marker Dark and McNet have on a `QPushButton`, neither styling `QPushButton:checked` at all. The dim unchecked icons in McNet and CrimsonSlate are real and remain, and they are a *sheet* defect this function cannot reach: the `:disabled` colour on the two `Font*ButtonStyle` selectors is read as "unchecked", so a genuine disabled grey there dims every non-checkable icon button in the application. One colour in the sheet fixes it — CrimsonSlate's own `text.dim` `#8A93A0` measures 4.88:1 on the panel surface and 4.33:1 on a floating bar. **Both engines also built and stylesheet-polished a throwaway QWidget on every paint.** The widget exists only to carry the `accessibleName` that a QSS rule matches, so the palette can be resolved; constructing it per paint meant a widget construction plus a full match against the ~2000-line sheet per icon per repaint, and nothing absorbed that because `QIcon` caches pixmaps only for its own `QPixmapIconEngine`, not for custom engines — one redraw of the tool strip plus the view toolbar is ~35 icons and therefore ~35 of each. One probe widget per type is now built on first use and kept, which is *not* a cache that can go stale: `QApplication::setStyleSheet` repolishes `QApplication::allWidgets()`, and a parentless widget that was never shown is in that list, so live theme switching still repaints correctly. It is created lazily rather than at static-init time (which precedes `QApplication`) and deliberately never destroyed, a static `QWidget` torn down after `QApplication` being a crash at exit. `FontNoggit` additionally called `QFontDatabase::addApplicationFont` from inside `paint()` for every icon height it had not seen, and **threw `std::runtime_error` from that paint path** on failure; the family is resolved once now and an empty result degrades to the base engine's text fallback, because an exception thrown through Qt's painting code is provoked again by the very next repaint. **`pen` (0xf304) and `edit` (0xf044) had no fallback entry**, which is why the tablet button on all eighteen `ExtendedSlider` rows rendered the literal text "f304" in a 22×22 box whenever Font Awesome was absent — the two glyphs get distinct labels since they are the OFF and ON states of one button, and `eraser`/`networkwired` were added while in the table. **`ExtendedSlider`'s slider/spinbox conversion was written out three times** — slider → spin, spin → slider, and a third copy in the tablet-pressure handler — and is now one pair of named functions so the directions cannot drift apart, with zero-span guards added because the old expressions divided by `(max - min)` unguarded and `static_cast<int>` of the resulting infinity is undefined behaviour. The arithmetic also gained the offset of the spinbox minimum, which none of the three copies had. **That is not a live bug, and nothing a user can see changes.** All eighteen construction sites in the tree give the spinbox a minimum of exactly 0 — `TerrainTool.cpp:96,102,119`, `texturing_tool.cpp:87,96,104`, `FlattenTool.cpp:116,122`, `ShaderTool.cpp:39,47`, `ErosionToolSettings.cpp:142,149,166,175`, `GroundEffectsTool.cpp:265`, and `BrushStack.ui:45,55,65`, which set a maximum only — the slider's own minimum is 0 and `setSliderRange` has no caller, so the offset term is zero in both directions and the old and new forms compute the same number. An intermediate revision of this row claimed `texturing_tool.cpp:181` (Spray size, 100..4000) as an exception; that was wrong and is corrected here. `_spray_size_slider` is a plain `QSlider` driving a plain `QDoubleSpinBox` (declared `QSlider*` at `texturing_tool.hpp:173`), it has its own conversion in that file, it never reaches this class, and it is not touched. The offset is here for the next caller that sets a non-zero minimum, not for any that exists. Saved presets were never at risk either way: `toJSON` stores `rawValue()`, the spinbox reading, not the slider position. **Density.** `ExtendedSliderUi.ui` was four nested layouts at Fusion defaults (9px margins, 6px spacing) for three rows; margins are zeroed and the two single-item wrapper layouts removed. The `pressureBar` wrapper was the costly one: the bar is hidden unless a tablet drives the value, and Qt skips a hidden *widget* in a layout but a nested *layout* holding it is not empty, so it kept claiming margins and spacing on all eighteen instances. `TerrainTool`'s settings group set `contentsMargins(0,12,0,12)` inside a group box the theme already pads 12/14, a second inset inside the first, and was the only tool doing so — zeroed, which also makes its group boxes consistent with every other tool's. **Discoverability.** The tool strip is icon-only and hovering showed only the bare name, since `QAction::toolTip()` falls back to `text()`; nine of the sixteen tools have a number key, but that binding lives in `MapView::addHotkey`, a private table rather than `QAction::setShortcut`, so Qt could not advertise it and the only record of it was one line of prose in the Help window. The tooltips now name the key. This is deliberately *display only* and the table is commented as a copy of MapView's: registering real `QAction` shortcuts would put two independent handlers on one key, and the MapView binding carries a guard a `QAction` cannot express (it declines to fire while an edit action is open). **Hover cost.** `ViewToolbar`'s `QAction::hovered` handler ran `clear()` + `hide()` on the secondary-bar holder for all nineteen buttons although only Climb has a secondary bar, i.e. a visibility change inside the viewport overlay layout per hovered button. The guard added is on "is anything actually up" rather than on "does this action have a bar", because those differ — moving off Climb onto a neighbour must still take Climb's bar down, and an early return keyed on the action would have stranded it. No tool behaviour, no action registration, no draw flag and no save path was touched. Verified in both configurations (`USE_SQL=ON` and the default) and the Catch2 suite is unchanged at 396 cases / 391 passed / 5 skipped / 276830 assertions. |
| 2026-08-01 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `dist/noggit-themes/CrimsonSlate/check_qss.py` — style-sheet-only work on the view-toggle bar and the tool dock; no `.cpp`, `.hpp`, `.ui` or `CMakeLists.txt` was touched. **This row is a rewrite, not an append: the version of it that described the view-toggle work was wrong in three places and the change it described has been replaced.** The bar carries **20** checkable buttons, not nineteen — a count of the live `add_tool_icon` calls in `ViewToolbar`'s second constructor, three more being commented out and the twenty-first button on the bar being the history `QPushButton`, which is neither a `QToolButton` nor checkable. **8** of the twenty are ON at startup, not "most": `_draw_models`, `_draw_wmo`, `_draw_wmo_doodads`, `_draw_wmo_exterior`, `_draw_terrain`, `_draw_water`, `_draw_vertex_color` and `_draw_model_animations`, read off the `BoolToggleProperty` initialisers in `MapView.h`; the other twelve default OFF. And the ON buttons were never "filled red boxes": the base `QToolButton:checked` wash `rgba(232, 84, 63, 0.26)` composites over this bar to `#5B393A` and measures **1.339:1**, while the actual signal was the base rule's 2px accent **left** border at **3.694:1**. Replacing that with a neutral `stroke.hi` plate therefore did not remove a loud fill — it removed the only element on the button clearing the 3:1 floor for a graphical state indicator and capped the entire ON signal at 2.119:1, which matters more than it would have a week ago because the concurrent icon-engine fix in the row above makes a checked and an unchecked glyph the same colour, and the engines resolve their pen from their own probe widget rather than from the button, so `color` in a QSS rule cannot reach the glyph at all. What ships instead puts the two signals on **different axes**, so that hovering something already ON shows both and neither can be mistaken for the other. **ON is a 2px `border-bottom` in accent, 3.694:1 against the bar** — the same 2px accent rule the tab bar and the left tool strip already use — which restores the baseline's peak contrast and bounds loudness by *area* rather than by dulling the colour: the button renders 42×38 around a 22px icon, so the mark is 5.3% of the face against 100% for a wash, and eight of them at rest read as a broken rule along the foot of the bar rather than as eight red boxes. The base rule's objection that a 2px accent line reads as a *separator* rather than as state is an argument about a **left** border on a horizontal row, where the line lands in the gap between two neighbours; a bottom border lands under one button and touches none of the others, and the left strip keeps its wash unchanged. **HOVER is the plate**: `stroke.hi` fill at 1.556:1 carrying a `text.off` edge at 2.119:1, two cues rather than one. The first version of this block re-based hover to `#343A45`, which measures **1.176:1**, and this sheet rejects a 1.13 step as invisible at icon-button size for the identical case — so that fix reproduced the defect it named. A press, which this bar used to inherit from the base rule and which therefore rendered at **1.000:1**, an invisible state nobody had noticed, is now 2.348:1 with the ON underline held through it. Every number above was measured off a render rather than derived: a throwaway Qt 5.15.2 program declaring a class whose `metaObject()->className()` really is `Noggit::Ui::Tools::ViewToolbar::Ui::ViewToolbar`, applying this sheet, forcing each state through the members `QToolButton::initStyleOption` actually reads (`WA_UnderMouse`, `setDown`, `setChecked`) and sampling the pixels. It also establishes two things the arithmetic gets wrong: the button is 38px tall and not the 34 that `min-height` plus padding plus borders predicts, because a 22px icon makes the content box 28px and the floor never binds, and all six states come back at exactly 42×38, so no transition reflows the bar. **The obvious way to write the override does not work and that is now enforced.** `QToolBar:horizontal QToolButton:checked` is silently reduced by Qt to `QToolBar QToolButton:checked`: `QCss::Selector::pseudoClass()` reads `basicSelectors.last()` and nothing else, and `QCss::StyleSelector` (Qt 5.15.2 `qcssparser_p.h`) declares virtuals for a node's name, ids and attributes and none at all for its state, so there is no mechanism by which an ancestor's `:horizontal` could ever be consulted — the rule would then have restyled the left tool strip too. The bar is scoped by class name instead, which is safe because the two other `ViewToolbar` instances hold only `QWidgetAction`s and contain no `QToolButton`. `check_qss.py` gained a check that fails the sheet on a pseudo-state or sub-control anywhere but the subject of a selector, self-tested by injecting two such rules. Also: the dock title bar, which the tool dock retitles to the active tool on every mode change, was drawn identically to every section heading inside the panel it heads — now 13/600 `text.hi` with the same 2px accent bar the tool strip uses, with the left padding reduced by the border width so the text does not shift. And the tool dock's padding rule targeted `QScrollArea#dockWidgetScrollArea`, a name set only by the Advanced Docking System, which is linked but never instantiated; the tool panel's own scroll area is `scrollArea` with contents `scrollAreaWidgetContents` (`ToolPanelScroll.ui`), both unique in the tree, and the rule now names them while keeping the ADS selector so it stays ready. **Two dead selectors are recorded rather than fixed**, both the same defect: Qt resolves a type selector against `metaObject()->className()`, so a class without `Q_OBJECT` is invisible to QSS under its own name. `Noggit::Ui::toolbar` (the left tool strip) and `Noggit::Ui::PaletteList` (the small texture palette, `texture_palette_small.hpp`) both lack it -- there is no `moc_Toolbar.cpp` among the 580 generated -- so the strip never gets its floating-bar surface and the palette still selects with the cell-filling wash these rules exist to prevent. Neither is fixable from a style sheet: neither widget sets an object name. One `Q_OBJECT` line in each header makes six rules live. `check_qss.py --moc <build>/noggit_autogen` now resolves every application class selector against the class names moc really generated, which is how both were found and how they can be re-checked. Checker output: **0 errors, 0 warnings**; with `--moc`, 0 errors and exactly the 4 warnings for the dead selectors named above and nothing else — in particular `Noggit--Ui--Tools--ViewToolbar--Ui--ViewToolbar` resolves, so the rules discussed here are live. `--coverage` against the sheet at HEAD reports **0 selectors dropped**, 10 added. |
| 2026-08-01 | `src/noggit/rendering/Primitives.{hpp,cpp}`, `src/noggit/rendering/TileRender.cpp` — two render-path fixes kept, two reverted. No new files, no change to any serialised structure. **This row is a rewrite.** An earlier version of it described four fixes; two of those have been removed from the tree after review, and this row describes what is actually there. **Kept: `Primitives::Line` recompiled its shaders and leaked GL objects on every draw call.** `Line::draw` routed through `setup_shader`, which ran `_program.reset(new OpenGL::program{...})` — two `src_from_qrc` resource reads plus a compile and link — *and* `_vao.upload()` / `_buffers.upload()`, i.e. `glGenVertexArrays` and `glGenBuffers`, without deleting what the previous call had generated (`scoped.ipp` `deferred_upload_buffers<count>::upload()` calls `genBuffers` unconditionally over the same name array). So every line drawn leaked one VAO and two buffers, `unload()` could only ever free the final set, and the spline path did all of it twice because `initSpline()` was implemented as a throwaway two-point `draw()`. Zone-light outlines are drawn once per light per frame in light-editing mode, so this accrued at frame rate. Split into a `setup_shader()` that creates the program and wires the VAO exactly once and an `upload_geometry()` that re-fills the two buffers per draw — correct because `glVertexAttribPointer` records the buffer *name*, not its contents, so re-`glBufferData`ing the same names leaves the recorded VAO state intact; the buffers having no data store at the moment the attrib pointer is recorded is legal. `Line::unload()` is now guarded on the setup flag, since `deferred_upload_buffers` leaves `GLuint _buffers[count]` uninitialised and its `unload()` — unlike its destructor — does not check the generated flag, so unloading a `Line` that was never drawn hands `glDeleteBuffers` indeterminate names; `WorldRender::unload()` calls `_line_render.unload()` unconditionally, so that path runs in every session that never enters light mode. The same shape in the sibling primitives is left alone and noted rather than fixed. **Kept: `TileRender::setOverrideOcclusionCulling` assigned the wrong member**, `_tile_frustum_culled` instead of `_tile_occlusion_cull_override` — the one the getter reads, and one that has its own setter, `setFrustumCulled`. Its only caller sets the frustum flag false two lines later, so the write was erased and the override was never re-armed after `getTileOcclusionQueryResult` first cleared it. The fix is one-directional: the override only ever forces "not occluded", so a tile can be drawn a frame or two longer than needed and never culled sooner, it cannot hide geometry, and it cannot make an object unselectable because `_was_rendered_last_frame` — which gates rectangle selection in `World.cpp` — is set inside the loop it gates. **Reverted: the per-chunk alphamap upload budget, because it could corrupt a sampler reset.** That change replaced the `bool skip_upload_alphamap` parameter with an in/out `unsigned&` allowance spent per chunk. The upstream form gates the entire chunk-update block on that bool, so a tile either ran all 256 chunks through `fillSamplers` or ran none — all-or-nothing. Under the budget form a tile could enter with the allowance non-zero, take the `_requires_sampler_reset` branch that wipes `_draw_calls` to a single call with all eleven samplers `-1`, refill chunks 0..k, exhaust the allowance, and defer the remainder. Those deferred chunks keep the `ChunkTextureSamplers[]` values `fillSamplers` wrote for the *previous* draw-call layout while the layout has been reset out from under them, so they sample whatever the new call happens to have bound in that slot — for the frame the tile is drawn at the end of the same function. The mitigation in that code (`_requires_sampler_reset = _requires_sampler_reset && any_alphamap_deferred`) makes the state converge but does not prevent the artefact: the flag persists, so the next frame wipes `_draw_calls` again, and because the allowance is spent in `_loaded_tiles_buffer` order a later tile can be starved by earlier ones and keep drawing from a partially refilled draw call across many frames. Restoring the all-or-nothing guarantee was preferred over patching it (exempting a reset pass from the throttle, or making the deferred path safe) because it is the version that needs no invariant held across three flags, and because the throttle's benefit is not observable in the measured scene — it only engages while terrain streams. `TileRender.hpp` and the throttle block in `WorldRender.cpp` are byte-identical to HEAD again. **Reverted: the WMO visibility-test re-parenthesisation.** `!render && cullTest > 1 \|\| intersects(...)` does bind as `(!render && cullTest > 1) \|\| intersects(...)`, and the bracketed form has the same truth value in all three reachable cases, as claimed. But `WMOInstance::getExtents()` is not a pure getter: it calls `ensureExtents()`, which calls `recalcExtents()` when `_update_group_extents` is set. The old form evaluated it whenever `render` was already true and the new form never does, which the change's own justification did not mention. That is harmless in practice — `render == true` requires `!extentsDirty()` and therefore `!_need_recalc_extents`, so the skipped recompute is idempotent — but it is a behaviour change to a culling path bought for no measurable gain, so it goes. **Measurement.** Interleaved A/B of two binaries built from the same tree with only `src/noggit/rendering/**` differing, alternated BASE/MODIFIED within one session, `--project H:\ProjectsNogRed --map 0`, `goto -9457 62 80`, 60 s settle, `perfreset`, 15 s sample, gated on identical scene counts (`loaded_tiles=25`, `rendered_tiles=10`, `loaded_objects=15568`, `rendered_objects=3139`) in every run. `work_p50` 3.26 / 3.21 â†’ 3.20 / 3.20 ms and `work_p95` 3.91 / 3.97 â†’ 3.95 / 3.95 ms; a second batch under different ambient GPU load gave `work_p50` 2.29 / 2.23 â†’ 2.16 / 2.38 and `work_p95` 3.63 / 3.47 â†’ 3.11 / 3.70. **No measurable change in either direction**, which is the expected result: neither surviving fix touches a static fully-resident terrain view, `Primitives::Line` being reachable only in light-editing mode. The whole four-fix diff measured the same way before the reverts (`work_p50` 2.75, `work_p95` 3.89) is also indistinguishable, so nothing measurable was given up by removing two of them. Caveat on the method: the window could not be brought to `Qt::ApplicationActive` from an automated session — `SetForegroundWindow`, `SwitchToThisWindow`, a synthetic title-bar click and a minimise/restore cycle were all tried and `frame_p50` stayed at the 32 ms `ApplicationInactive` interval — so every number above is at the halved timer rate. Both arms are affected identically, so the comparison holds, but the absolute `work_ms` values are not the focused-window figures. `fps_limit` reads 60 on this machine, not 144. **Save path.** Traced explicitly, because a culling or caching change that could make a tile skip serialisation would outrank any frame-time gain: it cannot. `MapIndex::saveChanged` iterates `loaded_tiles()` and gates on the atomic `MapTile::changed`, never on `_loaded_tiles_buffer` (the frustum-culled draw set) and never on any `TileRender` member. The throttled call `TextureSet::uploadAlphamapData()` is read-only with respect to CPU-side data — it copies `alphamaps[]`/`tmp_edit_values` into a scratch array and issues `texSubImage3D`, mutating nothing that is written back out. `_chunk_update_flags` is a render word: its only readers outside the render path are `updateVerticesData`, `update_vertex_colors` and `update_shadows`, all three GPU uploads. And the one occlusion change that survives moves strictly toward drawing more, so it cannot remove a tile from any set. Both configurations build and link (`build`, `USE_SQL=ON`, and `build-nosql`) and the Catch2 suite is unchanged at 396 cases / 391 passed / 5 skipped / 276830 assertions. |
| 2026-08-01 | `src/noggit/ui/tools/UiCommon/DragField.{hpp,cpp}` (new), `src/noggit/ui/tools/UiCommon/CurveIcons.{hpp,cpp}` (new), `src/noggit/ui/tools/UiCommon/ExtendedSliderUi.ui`, `src/noggit/ui/tools/UiCommon/ExtendedSlider.cpp`, `src/noggit/ui/TerrainTool.cpp`, `src/noggit/ui/texturing_tool.cpp` — tool-settings dock layout redesign; appearance and affordance only, no tool behaviour changed. `ExtendedSlider` spent a label row plus a full-width `QSlider` plus a hidden pressure bar on a single number (~50px, fifteen instances, three in Raise/Lower alone). Its `doubleSpinBox` is now promoted in the `.ui` to `DragField`, a `QDoubleSpinBox` **subclass** — that is the whole safety argument, since the spin box remains the single source of truth for value, range, decimals, step, validation and locale, so every `setValue`/`setRange`/`setDecimals`/`value` call and the `valueChanged` connection are untouched, and keeping the object name `doubleSpinBox` means every `_ui.doubleSpinBox` reference compiles unchanged. The field draws its own label left, its value right, and its position in range as a 22%-alpha accent fill behind both, so the slider is folded into the row at zero extra height: 24px against ~50px. Drag to scrub (`singleStep()`, x10 with Shift, x0.1 with Ctrl, read off the event so the modifier is only consulted while the widget holds the mouse grab), click to type. The value text is left to the `QLineEdit` child rather than painted, and that child is marked `WA_TransparentForMouseEvents` only while unfocused — not hidden, because it is the widget's focus proxy and hiding it would drop the field out of the tab chain. The `QSlider` is kept and merely hidden: `setSliderRange()` is public API, `pressureBar` shares its range, and Qt skips a hidden widget in a layout entirely. `setPrefix`, `setTabletSupportEnabled`, the tablet-pressure path and `sliderToSpin`/`spinToSlider` are all untouched, and **no construction site's range, decimals, step or initial value changed** — all sixteen were re-read and are listed in the `ExtendedSlider.cpp` comment. TerrainTool's nine falloff `QRadioButton`s became a 3x3 grid of `QToolButton`s carrying drawn curve profiles (`CurveIcons`, SVG path data on a 24x24 grid rendered through `QPainterPath` — not `.svg` assets, since `Qt5Svg` and the `qsvg` plugin are not linked and `QIcon(":/x.svg")` would return null silently); `QButtonGroup` operates on `QAbstractButton`, the explicit `eTerrainType` ids are unchanged, and the `if (!stamp)` branch is preserved verbatim so `nextType()` reaches exactly the buttons it did before. Spacing follows one 4px scale. `texturing_tool`'s three redundant standalone `QLabel`s are gone, their text moved into the fields. No `CMakeLists.txt` change — the `src/noggit` globs are recursive — but the two new files need a CMake **re-configure** before building, because the glob runs at configure time. Both configurations build and link (`build`, `USE_SQL=ON`, and `build-nosql`) and the Catch2 suite is unchanged at 396 cases / 391 passed / 5 skipped / 276830 assertions. |
| 2026-08-01 | Added `dist/noggit-themes/CrimsonSlate/icons/` — the theme's own icon set: 103 drawings (110 manifest keys, 7 of them aliases) authored as SVG path data on a 24x24 grid and emitted as one `.svg` plus `.png` at 24 px and `@2x.png` at 48 px, with `manifest.json` as the name/codepoint/geometry contract the icon loader reads. Drawn rather than downloaded because this repository already had to remove a proprietary Font Awesome file and two Microsoft fonts it could not evidence a right to redistribute; self-drawn art carries no such risk and permanently removes the fallback that renders a bare codepoint (`f304`) on every tablet button. PNG rather than SVG at runtime because `CMakeLists.txt` links neither `Qt5Svg` nor the `qsvg` plugin, so `QIcon(":/x.svg")` returns a null icon silently; PNG needs no new module and no CMake change. `generate_icon_set.py` regenerates every file — Python 3 stdlib only, reusing the PNG encoder from the sibling `generate_icons.py` rather than adding a third one, with its own path flattener and an exact-SDF rasteriser. Crispness is measured, not asserted: `--verify` samples the alpha profile across 14 axis-aligned strokes at both sizes and requires 0/255 only, which is what pins every stroke centreline to an integer grid coordinate. |
| 2026-08-01 | `src/noggit/MapView.{h,cpp}` (status-bar members and their construction only), `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.{hpp,cpp}` — regrouped the status bar into three zones with exactly three emphasised readouts, wired the theme's never-fired `QLabel[state="value"]` hook, split the tile index and the frame time into labels of their own, moved the run-state readouts to permanent widgets so `showMessage()` no longer blanks them, and gave the twenty view toggles two-line tooltips, group comments and a `QToolButton` history button. Appearance and affordance only; no tool behaviour changed.
| 2026-08-01 | `src/noggit/ui/FontAwesome.{hpp,cpp}`, `src/noggit/ui/FontNoggit.{hpp,cpp}` — the two icon engines now resolve artwork before they resolve a glyph, so the whole application picks up the drawn icon set without one call site changing. **The seam.** `FontAwesomeIconEngine::paint` gained a first tier, `paintArtwork`, which looks the codepoint up in `<exe dir>/themes/<active theme>/icons/` and blits a tinted PNG; `FontNoggitIconEngine::paint` calls the same protected helper. Nothing else moved: `FontAwesomeIcon` and `FontNoggitIcon` construct exactly as before, the ~15 files that call them are untouched, and `pixmap()` already routed through `paint()` so it inherits the artwork for free. The directory is resolved against `QCoreApplication::applicationDirPath()` through `Noggit::Application::themesDirectory()`, the same function `applyTheme` uses — anything relative to the working directory breaks the moment Noggit is started from a shortcut or a debugger, a defect this tree has already had twice. **Lookup is by NAME, out of the icon set's own `manifest.json`, not out of the enumerator.** The two disagree and the manifest is right, because it collapses aliases: `FontAwesome::lightbulb` and `FontNoggit::TOOL_LIGHT` are one drawing, so the manifest points 0xf0eb at `tool_light` and there is no `lightbulb.png` to find; likewise `gamepad`/`view_mode_game`, `trashalt`/`trash`, `windowclose`/`times`. Deriving the file name from the enumerator would have silently missed exactly those four and nobody would have known which four. A built-in table is consulted second, for the names the manifest has no codepoint for. **A new `IconSet` parameter threads through every codepoint-keyed lookup**, because the two enums overlap in the Private Use Area — `FontNoggit::rmb` is 0xf868, `FontAwesome::biking` is 0xf84a — and a lookup that cannot tell them apart resolves a tool icon to another set's picture, which is worse than resolving none. **Caching.** The decoded `QImage` is cached per name and scale; the tinted `QPixmap` per set, codepoint, state, size, device pixel ratio and pen colour; and a miss is cached as a null image, so a codepoint the set does not cover costs one map lookup per paint and never a `stat()`. Deliberately not `QIcon::addPixmap`: the tint is the theme-resolved pen colour read from a polished probe widget at paint time, so a pre-baked `QIcon` would go stale across the theme switch the existing engines already support. The 48 px raster is used only where the target rect exceeds 24 device pixels — taking it always smears the 2-unit stroke the whole icon language is built on. **The `f304` defect is gone.** `fallbackLabel()` returned the raw codepoint for anything absent from its table, which is what printed the literal text "f304" inside the 22x22 tablet button of all fifteen `ExtendedSlider` rows; it now returns a short label from a table covering every value the application instantiates, and `"?"` — never hex — for anything else. **State is preserved and then extended.** `iconPenColor`'s `QPalette::ColorGroup` selection is untouched, so checked still resolves to the Normal group and unchecked to the Disabled one and the checked marker survives in every theme; on top of it `iconPenColorForMode` lightens 125% for `Mode::Active` (hover, previously indistinguishable from Normal) and `Mode::Disabled` now paints at 45% opacity, which is the only separation available given `QIcon::Off` already owns the theme's `:disabled` colour slot. `QIcon::On` additionally prefers `<name>_filled.png`, which is how the set spells an on/off pair that has to read without colour — the outlined star against the solid one. **A `.notdef` guard.** `FontAwesomeFont::hasGlyph` diverts a codepoint the resolved font carries no glyph for to the text label instead of an empty box, and it is gated on `hasFallback` so the ~60 keybinding hints in the Noggit font (`lmb`, `shift`, `a`-`z`, `f1`-`f12`), which have no table row on purpose, can never be diverted away from the font that renders them correctly today; verified by opening the Key Bindings window and seeing the mouse glyphs intact. **Added to `FontNoggit::Icons`:** `TOOL_SCRIPTING`, `TOOL_CHUNK`, `TOOL_EROSION` (0xf8e2-0xf8e4). All three are past the end of `noggit_font.ttf` and resolve through artwork; 0xf8e5-0xf8ed are left free because the nine falloff curves are reached through `UiCommon::falloffCurveIcon(int)` keyed by `eTerrainType` rather than through this enum. The three tool values are unused until `ChunkTool.cpp:34`, `ScriptingTool.cpp:38` and `ErosionTool.cpp:53` stop returning `FontNoggit::INFO` — three of sixteen tools are visually identical today — which is a one-line change in each and is not made here, because this file owns what an icon is and not which tool asks for one. **Evidence.** Both configurations rebuilt after a forced recompile of the two translation units, with no diagnostic from either (`build` with `USE_SQL=ON`, and `build-nosql`); the Catch2 suite is unchanged at 396 cases / 391 passed / 5 skipped / 276830 assertions; and the running editor was screenshotted — every icon in the sixteen-entry tool strip and the twenty-entry view toolbar is now drawn artwork, and the toolbar's `FontAwesomeIcon(FontAwesome::undo)` button paints `undo.png` on a machine with no Font Awesome font installed, where it previously painted the text "UND". |
| 2026-08-02 | Added `patches/` — `src/external/blizzard-archive-library` is a submodule pointing at a third-party repository this project cannot push to, and the parent repo records only its commit pointer, so a fix made inside it is invisible to everyone who clones. Fixes there are exported as patch files with a README explaining how to apply them. `0001` makes MPQ entry names use backslashes: MPQ resolves a file by hashing its name, so a stored name must be byte-for-byte what the client requests (`World\Maps\...`), while `Listfile::FileKey` normalises to forward slashes for Noggit's own lookups and that form went straight into `SFileAddFileEx`. Every archive written by Client ▸ Patch Client was therefore silently ignored by the game — valid header, correct payload, correct archive name, and MPQ viewers still listed the contents because they read the `(listfile)` rather than hashing. Diagnosed by comparing a known-working custom patch (`Creatureeemounteemount.m2`) against a Noggit-written one (`world/maps/...`); headers were otherwise identical. Confirmed working in game. |
| 2026-08-02 | Added `src/noggit/AssetDependencies.{hpp,cpp}`, `src/noggit/PatchAssetPacker.{hpp,cpp}` and `tests/AssetDependenciesTests.cpp`, plus two checkboxes and a progress dialog in `src/noggit/ui/windows/noggitWindow/NoggitWindow.{hpp,cpp}` — Client ▸ Patch Client now packs the assets the project's terrain *references*, not just the terrain. **No submodule change was needed**, which was the design constraint: `ClientData::_mutex` is a plain `std::mutex` held for the whole of `saveLocalFilesToArchive`, and `readFile`/`exists`/`getDiskPath` all take it, so collecting dependencies inside that function — the obvious place — self-deadlocks on its first `ClientFile`. It is a separate two-phase pass instead: resolve and extract to a staging directory with the archive still read-only, then open for writing once and add every staged file through `MPQArchive::addFile`, the writer that already carries the backslash fix. `AssetScanCollector` is **not** reused for the walk and cannot be: it reads in-memory loader state for `MapIndex::loaded_tiles()`, while Patch Client lives in the project browser where there is no `World`, no `MapView` and no GL context, and a project folder routinely holds tiles nobody visited. `AssetScan`'s aggregation half — `normalizeAssetKey`, `AssetKind`, `AssetScanResult`'s dedup, referrer capping, total ordering and reporting — is reused unchanged. The new byte-level walker adds the three links whose absence produces a model that loads in the editor and fails in game and that nothing in the tree records: WMO **group files** (`WMOGroup::load` composes `<root>_000.wmo` as a local and discards it), M2 **`.skin` files** for every `nViews`, and the MOSB **skybox**. Three spellings are deliberately not copied from the loaders: `.anim` names are `%04d-%02d`, not `Model.cpp`'s unpadded form, which matches 0 of the 38484 `.anim` entries in the shipped listfile; the group index is appended after stripping `.wmo` from the *end*, not at `find(".wmo")`; and `.mdx`→`.m2` is anchored at the end rather than done with `std::regex(".mdx")`, where `.` matches any character and turns `creature/amdxthing/foo.mdx` into `creature/.m2thing/foo.m2`. Base-client assets are excluded by default and gated behind a checkbox, decided by archive *name* because the user's own patch and Blizzard's load through the same `patch-{number}`/`patch-{character}` templates. Verified end to end against a real client: an ADT naming `Farm.wmo` produced an archive containing `world\wmo\azeroth\buildings\human_farm\farm.wmo` with `farm_000.wmo` and `farm_001.wmo`, zero forward-slash entries, and `SFileHasFile` — which hashes — finding all three. |
| 2026-08-08 | `docs/setup.md`, `docs/milestones.md`, `tests/AssetDependenciesTests.cpp`, `dist/noggit-themes/CrimsonSlate/icons/generate_icon_set.py` — pre-publication text pass over tracked files; no behaviour, no interface and no serialised structure is touched. Two absolute paths belonging to one contributor's machine are replaced by placeholders: the pasted `INSTALL` failure in `docs/setup.md` now reads `<repo>/bin/shaders`, matching the `<prefix>` placeholder already used two lines above it, and the three `isBaseClientArchiveName` cases that assert a *full disk path* still parses use a neutral client directory instead of a real one. The strings are arbitrary in both places — what the assertions test is that a path with directory separators reaches the extension check, which is unchanged, and the suite still reports 413 cases, 408 passed, 5 skipped. Two comments in `generate_icon_set.py` that described who has to add the missing `FontNoggit` enumerators are reworded to describe *what* is missing instead, since the requirement is a property of the icon set — entries with `enumerator`/`codepoint` of `None`, cross-referenced by `manifest.json`'s `needs_enumerator` — and not of whoever integrates it. `docs/milestones.md` says "creature-AI-dependent" where it said "AI-dependent": the sentence is about TrinityCore's waypoint tables being selected by the creature AI in use, and the longer spelling is the one that cannot be read the other way. |
| 2026-08-08 | Removed `src/noggit/DevBridge.{hpp,cpp}`, `tools/dev-bridge/` and `docs/dev-bridge.md`, the CMake option `NOGGIT_DEV_BRIDGE` and its `NOGGIT_DEV_BRIDGE_ENABLED` define, `MapView::handleBridgeCommand` and every command it implemented, and the unattended startup path that existed only to serve it (`--project`, `--map`, `--goto` in `src/noggit/application/ApplicationEntry.cpp`, and `NoggitWindow::openMapUnattended`). This project is published, and a loopback TCP command socket that lets an external process drive the editor has no place in a released build — that the option defaulted to `OFF` and its own documentation said not to ship a build with it `ON` is a reason not to carry the code at all, not a reason to trust a default. The unattended path went with it because `openMapUnattended` deliberately called `enterMapAt` with `uid_fix_mode::none`, skipping the UID fix and logging "do not save from this session": an entry point that produces an editor which must not be saved from should not be reachable from the command line of a public build. `--disable-update` and `--force-changelog` are untouched. Two members that existed only to serve bridge commands went with the handler — the 4096-entry `PerfSample` ring plus `recordPerfSample`, read only by `perf` and `perfreset`, and `_force_single_render`, set only by the bridge's `screenshot`, which had no menu route. The `fps_limit` / `Qt::PreciseTimer` clamp work in the same region of `paintGL` is unrelated to the bridge and was left alone, as was everything the bridge merely exercised: `loadDatabaseSpawns`, `loadDatabaseSpawnsForTiles`, `saveDatabaseChanges`, `createDatabaseSpawn`, `deleteDatabaseSpawn`, `discardDatabaseSpawnChanges`, `focusOnSpawn`, `terrainTexturesInScope`, `selectedTexturePath`, `cameraPosition` and `markSpawnOverlayDirty` all keep callers in `DatabaseSpawnPanel`, `AutoTextureDialog`, `GroundEffectSetEditor`, `AlphaIntegrityReport` and `AmbientOcclusionDialog`, and every one was checked for callers before anything was deleted. Verified in both configurations, with the option removed from both CMake caches rather than merely set `OFF`: the `-DUSE_SQL=ON` build (which had been carrying `NOGGIT_DEV_BRIDGE=ON`) and the default `OFF/OFF` build both compile and link, and the Catch2 suite is unchanged at 413 cases / 276924 assertions — 408 passed, 5 skipped with no dev database configured. |
| 2026-08-08 | `src/noggit/AssetDependencies.{hpp,cpp}`, `src/noggit/PatchAssetPacker.{hpp,cpp}`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.cpp`, `tests/AssetDependenciesTests.cpp` — three defects in the Client ▸ Patch Client dependency pass, the first of which made the tool report success while shipping a broken patch. (1) **Silent parse failure.** `collectAdtReferences`, `collectModelReferences` and `collectWorldModelReferences` returned `bool` and all three return values were discarded at the dispatch in `PatchAssetPacker::run`; the status had already been recorded as `Present` beforehand. An ADT with a truncated MTEX/MMDX block therefore yielded zero children, counted as a successfully walked reference in `summary()`, and appeared in no failure list — the patch shipped without every texture and model that file named and the report said it was fine. `forEachChunk` also stopped at the first unparseable chunk header without saying so, so a *partial* parse produced a non-empty and plausible-looking child list that was still a prefix of the truth. The three functions now return `AssetParseResult` (`ParseOutcome::Complete`/`Truncated`/`Unrecognised` plus a reason naming the chunk and the byte counts), the type carries `[[nodiscard]]` so the answer cannot be dropped again, and `PatchAssetPackerResult` grew a `parse_failures` list that `summary()` shouts about and `toReport()` prints *above* the missing and unreadable sections — it is the only line that can say the patch has a hole, because the names such a file omits were never looked up and so appear in no other tally. The file is still staged and still traversed as far as it got: packing a half-readable file beats dropping it. `Truncated` is deliberately distinguished from `Unrecognised` for MD20 and for a WMO cut short before its MOHD, since "this is not a model" sends the reader looking for the wrong problem when what they have is a file copied half way. (2) **Compaction was a no-op when nothing was staged.** `NoggitWindow` passes `compact_archive && !pack_dependencies` to `saveLocalFilesToArchive` precisely so the rebuild is deferred to this pass, but `run()` returned early on an empty staging set before it ever compacted — so ticking both Compact Archive and the dependency option on a project whose references were all already present silently skipped the compaction. Compaction now runs whenever it was asked for and the run was not cancelled, rather than only when this pass added something. (3) **Every reported failure claimed "(1 reference)".** The breadth-first walk inserted into the visited set before queueing and recorded the reference at *dequeue* time, so `addReference` ran exactly once per asset however many files named it; a missing texture used by forty models read as used by one, which makes triage impossible. Reference *edges* are now recorded for every discovery and replayed against the per-path verdict after the walk — replay is required rather than stylistic, because an edge is found before its target's verdict is known and `AssetScanResult` keeps the first verdict per asset and counts later ones as disagreements. Edges hold interned `std::string const*` into an `unordered_set` (pointer-stable across rehash) rather than two `std::string`s, which keeps a project with hundreds of thousands of reference sites from spending over a hundred megabytes on duplicated path text. Two behaviours improve as a side effect: cancelling now `break`s and replays instead of returning an empty scan, and a file that is optional to one parent and required by another is reported, which the old walk could not express because only the first edge to reach a path survived. Verified in both configurations (`-DUSE_SQL=ON` and the default) plus the test target; the Catch2 suite goes from 413 to 422 cases — 417 passed, 5 skipped with no dev database — with nine new cases pinning truncated-and-empty, truncated-but-partial, trailing-fragment, MD20-stub, M2-texture-table-overrun, WMO-truncated-before-MOHD, WMO-truncated-after-MOHD (group files still emitted), and the multi-parent reference count. The real `PatchAssetPackerResult::toReport()` was additionally driven from a scratch harness linked against the compiled `PatchAssetPacker.obj` over hand-built malformed ADTs, which prints `FAILED  …/test_00_01.adt / chunk MTEX at offset 12 declares 4096 bytes and only 33 remain` and `PARTIAL …/test_00_02.adt / chunk MMDX at offset 53 declares 512 bytes and only 0 remain / 2 reference(s) were recovered before the walk stopped`. No fixture files were added to the repository and nothing under `src/external` was touched. |
| 2026-08-08 | `src/noggit/PatchAssetPacker.cpp`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.cpp` — corrected the parse-failure wording. Both the summary line and the dialog paragraph said everything an unparsed file referenced was never followed. That is false for the common case: a partial parse stops at a bad chunk header and yields the references it reached, so the report overstated the damage for exactly the files most likely to occur. A report that overstates gets distrusted as fast as one that understates. Now "UNPARSED OR PARTIAL (some of their own references were never followed)", with the per-file detail carrying the recovered count and the stop point. |
| 2026-08-11 | `src/noggit/database/DoctorReport.cpp`, `tests/SpawnPlacementTests.cpp` — removed the UTF-8 byte order mark from two files added by this fork. Both are pure ASCII, so the BOM encoded nothing; its only effect was to sit in front of the licence header, which meant the first three bytes of the file were not the `//` the GPL-3.0 header check looks for. Three upstream files (`rendering/CursorRender.cpp`, `ui/GroundEffectsTool.cpp`, `ui/texturing_tool.cpp`) still carry a BOM and were deliberately left alone — they are not this fork's files to reformat. |
| 2026-08-11 | `README.md`, `docs/setup.md`, `docs/milestones.md`, `docs/CONTRIBUTING.md`, `docs/design-notes.md`, `docs/feature-recon.md`, `docs/ground-effects.md`, `patches/README.md`, `.gitignore` — pre-release documentation accuracy pass, plus one packaging fix. **`.gitignore` never un-ignored `/patches/`**, so `patches/0001-blizzard-archive-mpq-backslash-names.patch` and its README were invisible to `git add` and are untracked: the repository carried an `ATTRIBUTION.md` row announcing the directory, and would have shipped build instructions pointing at a required file it did not contain. `!/patches/` and `!/patches/**` added — both are needed, since `/**` on line 1 matches the bare directory entry and git then never looks inside. Verified with `git add --dry-run patches/`, which listed both files where it previously listed nothing. This is the third instance of the failure documented at the top of that file. No source, build or behaviour change. Every claim was checked against the tree rather than carried forward. Six were false. (1) **The required build step was undocumented.** Neither `README.md` nor `docs/setup.md` mentioned applying `patches/0001`, so a fresh clone followed to the letter produced an editor whose Client ▸ Patch Client writes archives the client silently ignores — the one failure mode in this project with no error message anywhere. Both now carry it as a numbered step, with the reason it cannot ship as a submodule commit, and `docs/CONTRIBUTING.md` warns contributors that the resulting permanently-dirty submodule must never be committed. (2) **`README.md` listed client-side MPQ patch building under "not planned, deliberately"** and `docs/milestones.md` under "explicitly out of scope"; it shipped, with a referenced-asset dependency walk. Both corrected, and the README gained a section describing it — it was the only major feature the feature list omitted entirely. (3) **Test figures were three revisions stale** — README and `setup.md` said 382 cases / 377 passed / 276,787 assertions and `milestones.md` said 220 cases / 3,098 assertions, against a measured `422 | 417 passed | 5 skipped` and 276,964 assertions with `ctest -LE needs-database` at 33/33. The no-connector row is now labelled as arithmetic (422 minus the six cases in `DatabaseIntegrationTests.cpp`) rather than presented as measured, because no machine here lacks the connector. (4) **`docs/CONTRIBUTING.md` claimed 337 files carry the GPL header**; measured, it is 813 of the 908 tracked sources under `src/` outside `src/external/`, and the 95 without are inherited upstream files. The command to recount is included so the number cannot rot silently again. (5) **`docs/design-notes.md` opened by describing a visual waypoint editor and a chunk mover as things the fork *is*.** Neither has a user interface; both are logic-only. Reworded to say so. (6) `AreatriggerDescriptions.csv` was cited at 1,220 rows against 1,219 plus a header. `docs/milestones.md` and `docs/feature-recon.md` are both now banner-marked as historical/point-in-time records with a current-status summary at the top, since both read as live status pages while describing work that has since shipped — `feature-recon.md`'s ten-feature table gained a "Now" column showing seven built. Honest-status rows were added for the map-making tools and the ground-effect editor recording that their menu entries were verified in the running editor but their *output* has never been signed off by anyone. Verified: 1,441 upstream commits from 2020-10-09 to 2025-01-17, the eleven schema corrections, the DDL grep (`src/mysql/mysql.cpp:70` the sole `CREATE TABLE`, no `CREATE DATABASE`), and every feature named in the README's list against its menu registration in `MapView.cpp`. |
| 2026-08-21 | `README.md`, `docs/CONTRIBUTING.md`, `docs/design-notes.md`, `src/noggit/application/NoggitApplication.cpp`, `src/noggit/MapView.cpp` (window title only), `src/noggit/ui/windows/noggitWindow/NoggitWindow.cpp` (window title only), `src/noggit/ui/windows/about/About.ui`, `src/noggit/ui/windows/changelog/Changelog.ui`, `src/noggit/ui/windows/projectSelection/NoggitProjectSelectionWindow.{hpp,ui}` — **the product this fork ships is now called Noggit Crimson.** Names only: no control changed its range, step, signal or semantics, and no code path was added or removed. Renamed are the four window titles (`NoggitWindow.cpp:75` `"Noggit - "`, `MapView.cpp:4239` `"Noggit Studio Red - "`, and the project-selection and changelog `windowTitle` strings), the About dialog's product line (`"Noggit Studio Red"`), the startup banner (`NoggitApplication.cpp:81`, previously `"Noggit Studio - "`), and the two "please reinstall Noggit Red" operator messages. **Upstream stays credited, deliberately and in every place it already was.** The GPL header line `// This file is part of Noggit3, …` is untouched in every file that carries it — it names the codebase lineage, not this product, and rewriting it would be a licensing change rather than a rename. The rows of this table are historical records and none was edited. Every "a fork of Noggit Red" sentence, every `github.com/Marlamin/noggit-red` link, this file's own upstream credit, and the contributor list in the About dialog survive verbatim; `README.md`'s table gained a `Product`/`Upstream` pair so the two names cannot be confused, and the About dialog gained one new line, "An unofficial GPL-3.0 fork of Noggit Red by Marlamin", because the About box is where a GPL-3.0 fork's attribution most needs to be legible. Three hits were examined and deliberately **not** renamed: `MapView.cpp:3803`/`:3817` label a repository and a Discord that belong to upstream and are still upstream's; `Updater.{h,ui}` downloads from `Intemporel/NoggitRedBinaries`, so "NoggitRed Updater" is the true label for what that (currently commented-out) dialog would fetch; and the bare `"&Noggit"` menu, the `"Noggit"` message-box captions and the prose "Noggit" in `docs/` describe inherited behaviour and a menu path rather than this product's name. The CMake project, the executable name, the `QSettings` organisation and application names (`ApplicationEntry.cpp:238-239`, both `"Noggit"`) and every on-disk path are unchanged on purpose — moving them would silently orphan every existing user's settings and project registry. **Evidence.** Both configurations rebuilt after force-touching all seven source files: `-DUSE_SQL=ON` and `build-nosql` each report `noggit.vcxproj -> …\bin\RelWithDebInfo\noggit.exe` with `Generating ui_About.h`, `ui_Changelog.h` and `ui_NoggitProjectSelectionWindow.h`, and no diagnostic attributable to this change — the only warnings on the touched units are the pre-existing `C4129 'T'` at `NoggitWindow.cpp:873` and the `C4005 STRPRODUCTVER` redefinition `cmake/revision.h.in` has always produced. The Catch2 suite is unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. The built editor was launched and closed: `MainWindowTitle` read `Noggit Crimson Project Selection`, it exited on `CloseMainWindow`, and its fresh `log.txt` opens `Noggit Crimson - 3.1525+ [56ae02a5]` with no error line. |
| 2026-08-25 | `src/noggit/MapTile.{h,cpp}`, `src/noggit/ModelInstance.{h,cpp}`, `src/noggit/WMOInstance.{h,cpp}`, `src/noggit/project/ApplicationProject.cpp`, `src/noggit/World.cpp`, `src/noggit/TileWater.cpp` — fixed four undefined-behaviour returns and removed dead code found by reading the compiler's own warning list. Three functions declared `T const&` but returned a prvalue, so every caller received a reference to a temporary destroyed at the end of the return statement (MSVC C4172): `MapTile::GetTextureHeightMappingData`, whose one caller is the per-texture-layer loop in `TileRender.cpp:579`, and `ModelInstance::getLocalExtents` / `WMOInstance::getLocalExtents`, both read by the box-selection path at `World.cpp:3954-3959` that decides which objects a drag-select picks up. Two more fell off the end of a non-void function past an `assert(false)` that `NDEBUG` compiles out in RelWithDebInfo (C4715): `ClientVersionFactory::mapToEnumVersion` returned an indeterminate enum and `MapToStringVersion` an indeterminate `std::string` whose destruction is heap corruption, not a wrong label; both now log and fall back to WOTLK, matching the fallback `ApplicationProjectReader.cpp:88` already applies to an unrecognised `ClientVersion`. Also removed three `LogError << "point A/B/C reached"` debug breadcrumbs left inside the per-object box-selection loop, each flushing to `log.txt` for every object considered, and an unread `colorTable().toStdVector()` copy in `TileWater::setWatermapImage` that allocated a vector Qt only ever populates for indexed formats. Returning by value changed no call site: all three callers already copied into a by-value local. |
| 2026-08-25 | `src/opengl/context.inl`, `src/opengl/shader.cpp`, `src/noggit/rendering/WMOGroupRender.{hpp,cpp}`, `src/noggit/rendering/WorldRender.{hpp,cpp}` — renderer smoothness pass, aimed at stalls rather than at the mean frame time. **The frame no longer blocks on the asset worker.** `WMOGroupRender::upload()` called `wait_until_loaded()` on every batch texture, and `upload()` is reached from `draw()`, i.e. from inside `paintGL`; the first frame a WMO became visible therefore stopped the GUI thread on a condition variable until `AsyncLoader` had pulled each BLP out of the archive. It now polls `finishedLoading()` and bails. Nothing new was invented to make that safe: `draw()` already re-enters `upload()` for as long as `_uploaded` is false, the `if (texture_not_uploaded) return;` early exit already sat ahead of `_draw_calls.clear()` and every GPU allocation, and `texture_not_uploaded` was a variable nothing ever set to true — the retry path was written and then never wired up. It cannot spin on a missing file because `AsyncObject::error_on_loading()` sets `finished` as well as `_loading_failed`. **Bail-and-retry needs a next frame, and one caller has none.** `WorldRender::saveMinimap` → `drawMinimap` → `draw` is a single-shot offline render whose output is written to a file, so a WMO whose batch textures were still queued would have been dropped from the generated minimap silently and permanently — a data-quality regression in the feature used to build a whole map atlas. The pre-existing waits do not cover it: `AsyncLoader::is_loading()` reports only what a worker has already picked up (`AsyncLoader.cpp:23-27` tests `_currently_loading`, not the separate `_to_load` queues), so the 5-second spin in `saveMinimap` reads false while a backlog is still waiting, and the `waitForChildrenLoaded()` above it is skipped entirely for a tile that was already resident. Fixed with a scoped switch rather than by reverting: `WMOGroupRender::ScopedBlockingUpload` makes `upload()` call `wait_until_loaded()` instead of bailing, and `saveMinimap` holds one across its single `drawMinimap` call. That was chosen over having `saveMinimap` pre-load the tile's objects, which cannot reach a WMO owned by a neighbouring tile that intrudes into the view, and over pumping until `upload()` reports complete, which needs a completion signal the class does not expose. **The interactive path is untouched** and keeps bailing and retrying, which is the part worth having. **The streaming upload burst is capped at one tile.** `WorldRender::_frame_max_chunk_updates` was 256; the budget is tested *before* a tile draws, so a tile that started within budget always finished its 256-chunk burst and the counter only then exceeded the limit — a second full tile got through, up to 512 chunk alphamap uploads in a single `paintGL`, each one a 12,288-float conversion plus a 48 KB `texSubImage3D`. At 32 the first tile still completes and everything after it defers. Deferral is lossless rather than lossy: `TileRender::draw` skips the whole update block without reaching `endChunkUpdates()`, so neither the chunk nor the tile update flags are cleared and the tile re-enters next frame. **The OpenGL error drain is now separable from the context guard.** `verify_context_and_check_for_gl_errors` fuses two unrelated things: constructor checks that a context is current — the guard the codebase's own hazard notes depend on, since `OpenGL::Scoped` destructors throw without one — and a destructor `glGetError` loop that is pure diagnostics. `NOGGIT_OPENGL_ERROR_CHECK=OFF` cannot switch off only the second, because it also selects the `#else` branches of `bufferData`/`bufferSubData` in the same file, and those do not restore the previous buffer binding; flipping it changes GL binding state program-wide. A separate `NOGGIT_DO_NOT_QUERY_OPENGL_ERRORS` (compile time) plus `NOGGIT_GL_ERROR_QUERY=0` (environment) now governs the drain and nothing else. **The runtime switch is a function-local static, not a namespace-scope `bool const`** — correcting an earlier draft of this change, which placed the flag at namespace scope on the reasoning that `context.inl` is included by one `.cpp` and so is ordered before the global `gl` object in the same translation unit. That premise is false: nine `.cpp` files include `opengl/context.inl` directly (`src/opengl/{context,shader,texture}.cpp`, `src/noggit/{Alphamap,Particle,map_horizon,wmo_liquid}.cpp`, `src/noggit/application/NoggitApplication.cpp`, `src/noggit/rendering/LiquidTextureManager.cpp`) and many more reach it transitively through `opengl/scoped.hpp`, which thirteen headers pull in. Each gets its own copy in the anonymous namespace, initialisation order across translation units is unspecified, and a flag read before its dynamic initialiser runs still holds its zero-initialised `false` — silently disabling the drain in whichever units lost the race. A function-local static is initialised on first use and thread-safe from C++11 on, so there is no order left to get wrong; the compile-time branch is a `constexpr` function returning `false`. **Default behaviour is unchanged** — with neither set the drain runs exactly as before — so `CMakeLists.txt` still needs an `OPTION(NOGGIT_GL_ERROR_QUERY … ON)` adding `-DNOGGIT_DO_NOT_QUERY_OPENGL_ERRORS` when off, which was left to the lead. `_extra_info` also stopped being a `std::function`: one guard is constructed per wrapped GL call, and the type erasure existed to serve the single `programString` call site, whose lambda captured only `this` and now takes the `OpenGL::context*` as an argument. **Measurement, and what it is not.** The only thing measured is the cost of `glGetError` in isolation, on a scratchpad Qt microbenchmark outside this application: this machine's driver (NVIDIA 596.21, RTX 5070 Ti, offscreen 4.1 core context, 20,000 calls per timed loop, median of 21 runs, three separate program runs) puts a bare `glBindTexture` at 4.35 / 4.29 / 4.26 ns and the same call followed by one `glGetError` at 525.25 / 567.98 / 533.78 ns — the drain costs **~520–565 ns per GL call, roughly 120× the call it is checking**. The constructor's context verification measured at or below noise against the drain alone, which is what makes splitting them worth doing rather than academic. **That figure is what the new opt-out saves when it is enabled, and nothing more. No speedup is claimed for the editor as built:** the drain is still on by default, so a default build issues exactly the same GL traffic it always did, and the microbenchmark says nothing about what fraction of a real frame it represents. **No in-application frame time was measured at any point** — not before, not after, and for none of the changes in this row. The stall-shaped items (the texture wait, the upload burst) are argued from what the code does, not from a timing; whether they are perceptible has not been established and needs a human at the GUI with a map open. Three smaller items rode along: `use_program::~use_program` hoisted its `getIntegerv(GL_VERTEX_ARRAY_BINDING)` out of the loop over enabled attribute arrays (`glDisableVertexAttribArray` cannot change the binding, so it was loop-invariant; the odd comparison against an attribute location is left exactly as it was, because changing which arrays get disabled changes GL state); `WorldRender::draw` dropped its `model_with_particles` declaration — to be precise, and correcting an earlier draft of this row that called it a per-frame gather, the map was only ever *declared*: nothing inserted into it, because its only consumers and every line that would have filled it sit inside the same commented-out particle and ribbon block, so what was removed is one empty container's construction per frame, not a gather; the WMO frustum test at `WorldRender.cpp:583` gained the parentheses the M2 path above it already has, so an instance the frame-coherence shortcut has already accepted no longer pays a redundant `frustum.intersects` — same decisions, one fewer test; and the `_loaded_tiles_buffer` null terminator is now bounded, since a full 64×64 map leaves `tile_counter == size()` and wrote one past the end. **Evidence.** `-DUSE_SQL=ON` and `build-nosql` both link (`noggit.vcxproj -> …\bin\RelWithDebInfo\noggit.exe`); the Catch2 suite is unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions; the built editor was launched and closed cleanly (exit code 0) both with and without `NOGGIT_GL_ERROR_QUERY=0`, and its fresh `log.txt` carries no error line. Re-verified after the three corrections above (the offline-render guard, the function-local static, and the `model_with_particles` wording): both configurations rebuilt after force-touching every translation unit that includes `context.inl`, each reporting `noggit.vcxproj -> …\bin\RelWithDebInfo\noggit.exe` with no error and no new warning — the diagnostics on the touched units are the pre-existing `C4200`/`C4005`/`C4018`/`C4267` set, none of them on a line this row changed. The suite still reads 422 / 417 passed / 5 skipped / 276,964 assertions. Three launches were made and all three showed `Noggit Crimson Project Selection`, exited on `CloseMainWindow` with code 0 and wrote a 13-line `log.txt` with no error line: the `USE_SQL` build by default, the `USE_SQL` build under `NOGGIT_GL_ERROR_QUERY=0`, and the `build-nosql` build. **What that does not cover:** the minimap regression this row fixes is in the offline render path, which needs a loaded map and a human at the GUI to exercise, so the fix is argued from the code and the build, and has not been observed producing a correct minimap. **Deliberately not done**, each because the risk outweighed an unmeasurable return: `N_ARRAY_TEX` stays 1 (layer slots are never recycled, so sharing arrays would eventually reuse a freed layer); the commented-out per-group WMO visibility test and the commented-out M2 texture-redundancy checks stay commented out (both were disabled by hand, neither says why, and both fail visually rather than loudly); the per-frame gather containers stay function-local (retaining them keys a cache on raw `Model*` across tile unload); and no in-application frame timings were taken at all — the dev bridge is gone, nothing can open a map without a human at the GUI, and no instrumentation was left in the tree. |
| 2026-08-25 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `src/noggit/ui/Help.cpp`, `src/noggit/ui/FontAwesome.cpp`, `src/noggit/ui/FramelessWindow.hpp`, `src/noggit/ui/GroundEffectsTool.cpp`, `src/noggit/ui/AlphaIntegrityReport.cpp`, `src/noggit/ui/DatabaseSpawnPanel.cpp`, `src/noggit/ui/tools/UiCommon/expanderwidget.cpp`, `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.{hpp,cpp}`, `src/noggit/ui/tools/{AreaTriggerEditor/AreaTriggerEditor,LightEditor/LightEditor,MapCreationWizard/Ui/MapCreationWizard}.cpp`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.cpp`, `src/noggit/ui/windows/noggitWindow/widgets/MapListItem.{hpp,cpp}`, `src/noggit/ui/windows/noggitWindow/widgets/MapBookmarkListItem.cpp`, `src/noggit/ui/windows/settingsPanel/SettingsPanel.cpp`, added `src/noggit/ui/WaitCursor.hpp` — interface modernisation pass. Appearance and affordance only: no control changed its range, step, signal or semantics, and no control was removed. **The left tool strip is a floating card.** `Noggit--Ui--toolbar` matches nothing — the class carries no `Q_OBJECT`, so Qt sees `QToolBar` — and the strip therefore fell through to the plain `QToolBar` rule and rendered as an opaque square-cornered `#22262E` slab forty pixels from the rounded, bordered, lighter card carrying the view toggles. It is now reached by position, `QWidget#leftToolbarHolder > QToolBar`, whose specificity is 65538 against 257 for `QToolBar:vertical` and 1 for `QToolBar`, so it wins by cascade rather than document order. The one-line `Q_OBJECT` alternative was deliberately not taken: it depends on whether AUTOMOC has `Toolbar.h` in its scan set, a `CMakeLists.txt` question this change was not allowed to answer. Hover had to be restated with it — the base `QToolButton:hover` fills with the exact colour the strip's surface has just become, i.e. 1.000:1, the same defect the view-toggle block documents — and the checked wash went 0.26 to 0.36 because at 0.26 over the old slab it composited to `#553232` and measured 1.37:1, which this sheet's own standard calls invisible at icon-button size. **Measured, not derived**, by a temporary probe that laid a `QToolBar` out off-screen inside a `QWidget` named `leftToolbarHolder` under this sheet, grabbed it and sampled pixels: surface `#2A2F38`, checked face `#6E3C3A` (1.52:1), checked leading edge `#E8543F` (3.69:1), button box 42x37 identical checked and unchecked so nothing reflows, bar box 52x86 against 50x84 — exactly the 1px card border on four edges. The first version of this change was **wrong and the probe caught it**: it added only a border cancel and left the fill to the still-dead type selector, so the strip measured `#22262E` and no card appeared. One correction to a figure elsewhere in the sheet: the toolbar icon size measured 24x24, not 22. **Icons on a scaled display: attempted, measured, and reverted.** `FontAwesomeIconEngine::pixmap` was changed to render at `qApp->devicePixelRatio()` (**2** on this machine) and tag the result, on the theory that `QIcon` asks the engine for a logical size and then stretches the 1x bitmap. **That theory is wrong and the change was a visual regression: every icon came out at exactly twice its intended size.** Settled by a standalone Qt 5.15.2 probe, built against this machine's Qt and run with this application's exact attributes (`AA_UseDesktopOpenGL`, `AA_EnableHighDpiScaling`), driving a `QIconEngine` through `QIcon::pixmap(QSize)`: requesting 22x22 calls the engine with **22x22** — Qt does not pre-multiply by the ratio — and the ratio the returned pixmap carries is **discarded**, a 22x22 bitmap tagged ratio 2 coming back reported as 22x22 at ratio 1. The oversized bitmap is therefore taken at face value: 44x44 at ratio 1, logical 44x44. The same probe ruled out the smaller variant of the idea (tag the ratio without resizing), which Qt strips identically. `QIcon::pixmap(QSize)` has no path that can carry a high-DPI pixmap out of an engine — the ratio-aware overload is `QIcon::pixmap(QWindow*, QSize)`, which only a call site has the window for — so sharpening these icons belongs at the call sites, and the engine is back to its upstream body with the measurement recorded in the file so the idea is not retried. The revert also un-clips the two main-menu rows, whose icon `QLabel`s are `setFixedSize(ICON_EXTENT, ICON_EXTENT)` with no `setScaledContents` and were cropping the doubled pixmap to half. `FontNoggitIconEngine` derives from it and reverts with it. **Both probes were removed**; the two files that carried them are byte-identical to `HEAD` (`git checkout --`), and the final `log.txt` contains no probe line. **The two main-menu list rows had no layout at all.** `MapListItem` and `MapListBookmarkItem` each stack-allocated a `QGridLayout`, handed it to `setLayout` and let it destruct on return — `QLayout`'s destructor clears the widget's layout pointer — so every label was placed by absolute `setGeometry` and parented to the *list view* rather than to the row. Rebuilt on the shape already reviewed in `ProjectListItem`: real nested layouts, elastic labels, `WA_TransparentForMouseEvents` so the row stays the context-menu target now that the labels are genuinely its children, and font ranks through `QFont` instead of the eight inline style sheets that pinned every row to 12px/10px whatever theme was loaded. `_map_pinned_label` was an uninitialised member read only when pinned; it is `nullptr`-initialised. **Inline style sheets that fought the theme.** The highest-traffic one, `ExpanderWidget`'s section header (seven tool panels), set `border: none` and `font-weight: bold`, which erased the theme's inter-section hairline and its 2px accent reservation and beat 600 with 700; it now sets `text-align: left` alone, which is kept because `QPushButton` has no `setAlignment` and a theme silent about this button would centre a section caption. `Help.cpp`'s seventeen headings carried one string containing `margin-left: 150px`, a hand-measured attempt to clear a glyph-width icon column that could only be right at one font and one DPI; they take a `QFont` bold plus `QLabel#help-section-header`. Five hardcoded colours — `red`, `orange`, the Qt colour name `RED`, `#d04040` and the Qt colour name `darkGray` — were replaced by the `state="error"`/`"warn"` properties and one `accessibleName` rule the sheet has shipped since it was written and **nothing in `src/` had ever set** (grepped: zero `setProperty("state", ...)` call sites tree-wide). The wizard's WMO-path field in particular filled solid red, swallowing the path the user needed to read. Making it *only* `state="error"` was itself a regression, because that property is answered by exactly one rule in one stylesheet: under the **System** theme no application stylesheet is loaded at all, the attribute selector matches nothing, and an invalid path drew no feedback whatever. It now signals three ways — the property, so a theme can style it richly; a **widget-level** stylesheet carrying the same `#E0574B` outline, which outranks the application stylesheet and so decides the border whether or not the active theme has a rule and whether or not there is an application stylesheet; and a tooltip, so the reason does not rest on distinguishing a colour. **Discoverability.** The twenty view-bar toggles had no explicit tooltip, so Qt fell back to `QAction::text()` — a bare name. Each now carries a rich-text name, a sentence of description, and its real shortcut where one exists, taken off the `ADD_TOGGLE`/`ADD_TOGGLE_POST` lines in `MapView::setupMainMenu`. Nothing calls `setShortcut`, so those `QAction`s keep sole ownership of the key and a stale entry can only display a wrong hint. `"Flight bounds\nCurrently doesn't work !"` was the *action text*, so the caveat and its newline were the button's accessible name; the caveat moved to the description. **One widget class per toolbar.** The History button was a `QPushButton` handed to `QToolBar::addWidget`, the only control on that bar no `QToolButton` rule could reach; it is an action now, and its popup sets `Qt::Popup` once at construction instead of destroying and recreating its native window on every click. **One key, three defaults.** `systemWindowFrame` defaulted to `false` here and `true` in the two other readers, so a fresh profile ran the main window with the OS frame and the settings window with the custom title bar, under a radio button that said "system frame". Aligned to `true`; both call sites now check the returned pointer, one of which used to dereference it unconditionally. **Wait cursor on map selection**, which synchronously constructs a `World` from a WDT on the UI thread once per arrow-key press through the list; `WaitCursor` was promoted out of `AlphaIntegrityReport.cpp` rather than copied. A debounce in front of it was considered and **rejected**: `getWorld()` is read immediately afterwards and again by the enter-map path, where `NoggitWindow.cpp:161`'s `assert` compiles to nothing under `NDEBUG` and line 163 dereferences — deferring construction while leaving those readers in place trades a stall for a null dereference. **Evidence.** `check_qss.py theme.qss .` reports `0 error(s), 0 warning(s)` at 298 rules / 855 declarations (289 / 821 before), ancestor pseudo-states still none. `-DUSE_SQL=ON` and `build-nosql` both link (`noggit.vcxproj -> ...\bin\RelWithDebInfo\noggit.exe`). The Catch2 suite is unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. The built editor launched, showed `Noggit Crimson Project Selection`, exited on `CloseMainWindow` with code 0, and its fresh `log.txt` carries thirteen lines and no error. **Deliberately not done.** The gizmo bar's hard-locked 32x32 boxes and 26px icons stay: the plan for them rests on the sheet's stated 22px toolbar icon, which measured 24, and no change there could be checked by eye without an open project. `Vector3Widget`'s saturated red/green/blue axis chips stay: the palette's rule is that accent is the only saturated colour, there is no blue token, and inventing three is the lead's call. The three missing `_filled` tool icons stay missing: the existing thirteen are hand-authored *coloured* art, not `generate_icon_set.py` output — verified by rendering `tool_raise_lower`/`tool_light`/`tool_chunk` from the table and comparing bytes, where `tool_chunk` is identical and the other two are 5-14x larger files — so flat white masks would make three of sixteen tools look wrong beside thirteen. **That comparison also found a live hazard: running `generate_icon_set.py .` as its own docstring instructs would overwrite the hand-authored coloured icons with flat masks.** The status bar's per-frame rebuild, its inverted selection test and the non-modal alpha-conversion progress dialog are all in `src/noggit/MapView.cpp`, outside this change's ownership, and are reported to the lead rather than touched. |
| 2026-08-25 | `src/noggit/ui/windows/noggitWindow/widgets/MapListItem.{hpp,cpp}`, `MapBookmarkListItem.{hpp,cpp}`, `src/noggit/ui/windows/projectSelection/widgets/ProjectListItem.{hpp,cpp}`, `NoggitWindow.cpp`, `NoggitProjectSelectionWindow.cpp` — **map and project names were losing their descenders**, and the cause was not the font. `setItemWidget()` installs these rows as *persistent editors*, so a row's geometry is not the item rect: it comes from `QStyledItemDelegate::updateEditorGeometry()` via `QStyle::subElementRect(SE_ItemViewItemText)`, and `QStyleSheetStyle` subtracts the view's `::item` margin, border and padding from that rect. Both components hand `QListWidgetItem::setSizeHint` exactly the row's own minimum, so every row was then handed *less* than its minimum and the `QVBoxLayout` squeezed the text column. Measured with a standalone Qt 5.15.2 probe against the shipped theme rather than guessed: `_continents_table` and `bookmarks_table` are plain `QListWidget`s with no `accessibleName`, so they match `QAbstractItemView::item { padding: 5px 6px; }` and lose 10px — the title label needed 19px and was given **11**, an 8px shortfall against a font whose descent is 4px, which is exactly "Eastern Kingdoms" without its g. `listView` carries `accessibleName "project_list"` and matches that sheet's lighter `::item { margin: 2px 2px; }`, losing 4px — so the project row was clipping too, by precisely its descent, which is why it read as fixed. Three candidate fixes were measured and two rejected. Querying the view's style for `SE_ItemViewItemText` directly reports a loss of **zero** whatever the theme says, so there is nothing honest to subtract; and adding a fixed allowance alone still clipped (16px short) under a theme with exaggerated item chrome. What holds is `setMinimumHeight(contentMinimum())` on the row: `QWidget::setGeometry` clamps to the widget minimum, so the delegate cannot squeeze the row below its content whatever a theme asks for — zero clipping under both the shipped sheet and a deliberately hostile 24px-chrome one. `ensurePolished()` precedes it so the floor is computed from the style sheet's font sizes and not from the `QFont` defaults the constructor sets, and `changeEvent` re-arms it on `StyleChange`/`FontChange` so a live theme switch cannot leave it stale. A separate `ITEM_CHROME_HEADROOM` is added to the size hint — the only consumer of which is the item — so that under a normal theme the row *fits* its slot instead of clamping and overhanging it; 12px for the two map lists against the measured 10, 6px for the project list against the measured 4, kept smaller there because that window is a fixed 792x416 and every pixel of row pitch costs a visible project. Measured before → after: map row title 11px → 19px (pitch 50 → 63), bookmark row title 11px → 19px (pitch 50 → 63), project row title 15px → 19px (pitch 71 → 77). Appearance only — no control changed range, step, signal or semantics, no control was removed, and the same click still opens the same project and the same map. Also `LINE_SPACING` on the two map rows raised 2px → 3px to match the project row, the map tab's column given even 8px gutters and one 8px gap in place of uic's 9/6 plus a hand-placed 5px spacer, and both project-selection columns set to one 8px gap so the two section headings align. Verified by building `USE_SQL=ON` and the default configuration, running both executables to a shown window and a clean exit code 0 with no new lines in `log.txt`, and the Catch2 suite unchanged at 422 cases / 417 passed / 5 skipped / 276964 assertions. |
| 2026-08-25 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `src/noggit/ui/FramelessWindow.hpp`, `src/noggit/ui/windows/settingsPanel/SettingsPanel.{cpp,ui}` — window-frame assessment plus a theme and settings-window pass, appearance and affordance only: no control changed range, step, signal or semantics and none was removed. **The frameless window frame was assessed and deliberately NOT made the default.** The library under it is sound — `winnativeeventfilter.cpp` handles `WM_NCCALCSIZE`, returns `HTCAPTION` from `WM_NCHITTEST` so Aero Snap, caption drag, double-click-maximise and Win+Arrow are all Windows' own, sizes from the per-monitor work area in `WM_GETMINMAXINFO` including auto-hide taskbars, defers `WM_DPICHANGED` to Qt on purpose, and builds the native system menu from `WM_CONTEXTMENU`. The integration is not. Measured with a standalone Qt 5.15.2 probe rather than reasoned about: `isTitleBar` is `localMouse.y <= (tbh + bh)`, and on this machine (Windows 11, 100%, dpr 1.0) `SM_CYCAPTION` 34 + `SM_CYSIZEFRAME` 5 + `SM_CXPADDEDBORDER` 6 gives a **45px caption band** while the `TitleBar` holder lays out at **29px**, or **34px** once `NoggitWindow.cpp:107` inserts the menu bar into it. Everything in that band that is not a registered ignore object returns `HTCAPTION`, so Windows keeps the press and the Qt widget underneath never sees it. Three consequences, all concrete: the maximise button was never passed to `addIgnoreObject` (only minimise and close were) so it was **dead to a single click** — hovering, painting, doing nothing, with double-click still maximising through `DefWindowProc` and disguising it; the **entire application menu bar** sits at y 1..33 inside the band and is not registered either, so every top-level menu would be unclickable and would start a window drag instead; and nothing called `setTitleBarHeight`, so **11px of real content** below the bar dragged the window. Two of the three are fixed here — the maximise button is now an ignore object, and the band is set from the holder's `sizeHint().height()` after `addWindow` (deliberately after: `updateQtFrame_internal`, which turns that value into the DWM frame margin, runs only inside `addFramelessWindow`, so setting it later changes the hit test and nothing else; `sizeHint` is a floor because a band shorter than the bar costs drag area while a band taller than it eats clicks). The third is in `NoggitWindow.cpp`, which this pass does not own: the fix is one call to the new `Noggit::Ui::makeTitleBarWidgetClickable(this, widget, _menuBar)` immediately after the `insertWidget(2, _menuBar)` at line 107. Two further reasons not to flip the default: `NoggitProjectSelectionWindow` — the first window the user sees — never calls `setupFramelessWindow` at all, so flipping it would dress two windows of three and leave the launcher wearing an OS frame, which is more inconsistent than today; and it would be a no-op for anyone who has ever pressed Save in Settings, since that writes the key unconditionally (`HKCU\Software\Noggit\Noggit\systemWindowFrame` reads `true` on this machine, checked with `reg query`). Also on that path and independent of the default: `iconButton->setMinimumWidth(32)` was stretching the application icon 14% horizontally, because the form pins that `QLabel` to 28x28 with `scaledContents` and `QWidget::setMinimumWidth` raises the maximum to match when the two cross — removed, since the theme already supplies the margin it was reaching for; the maximise control now swaps to a restore icon by being renamed to `titlebar_restore` and repolished on `QWindow::windowStateChanged`, so Aero Snap and a caption double-click reach it too and the icon stays the theme's rather than being set from C++ where the next polish would undo it; and the title label follows `windowTitleChanged` instead of being copied once, before anything can retitle the window. **Theme.** Slider handles were 6px wide — `subControlRect(CC_Slider, SC_SliderHandle)` returned 6x16 under the probe — narrower than the accent fill they terminate, so the most repeated control in the editor read as a progress bar with a scratch on it. Now a 16px round grip (12px fill plus a 2px ink ring each side) with a 6px groove and a -5px handle margin, so the geometry closes exactly at 6 + 5 + 5 and no range, step or emitted value moves. **This row originally claimed "2.7x the hit target for identical travel" and that claim was wrong.** 2.7x is the ratio of the PAINTED areas, 6x16 to 16x16; the hit target did not move at all, because Qt derives `SC_SliderHandle` from the WIDGET's height and never from the QSS handle box, as the probe recorded in the row two below this one measured directly. A style sheet cannot change how many pixels start a drag. The claim is corrected here rather than deleted, and the sheet's own comment, which asserted the same thing plus a specific pinned widget height it does not own, was rewritten to say only what this file decides. Section headings (`QGroupBox::title` and the `ExpanderWidget` captions) went from 11/600 text.dim on a 1.17:1 stroke.soft rule to 11/700 text (10.34:1) on a 1.46:1 stroke rule; the size did not change, so nothing in any tool panel reflows. The selected project row dropped from a 0.26 accent wash (#502C2B, a saturated slab at that area) to 0.18 (#402829, 1.235:1 against the panel, just above this sheet's own surface-elevation steps rather than inside that band as this row first said) with the 2px accent edge untouched and now the primary mark; the title, text.hi, reads 11.83:1 on it. The secondary line on that row was left at text.dim and measured only on the best of its five states; the row below this one carries the measurement of all five and the fix. A `QPushButton[state="primary"]` rule was added beside the existing `[state="danger"]` — accent fill with ink text at 4.88:1, accent.hi #EE6852 on hover at 5.70:1, accent.dim with text.hi on press at 5.01:1 because ink there would be 3.10:1, and an explicit `:disabled` since a bare `QPushButton:disabled` ties it on Qt specificity. #EE6852 is the one new colour and is declared in the sheet's own token table, so the static checker accounts for it. **The palette was left alone on purpose:** the surface ramp is tight (bg.sunken to bg.base 1.064, base to raised 1.101) but every surface token is also read by `darkPalette()` in `ApplicationEntry.cpp`, which this pass does not own, and changing one side only would desync styled widgets from unstyled ones. **Settings window:** its `saveButton` takes the new primary property so the confirming action stops being indistinguishable from Discard, and the footer hint dropped to the 11/400 text.dim caption rank (5.38:1), which the row below this one reverts to 12/400 text (10.34:1) because the primary button had already settled the hierarchy that demotion was for. That hint was a second widget also named `label_48` — `uic` silently renamed it `label_481`, a generated name that moves whenever the form does — so it is now `_restart_hint`; its text, format and position are unchanged and nothing referenced either label. The frameless title bar itself is now styled at all (`QWidget#TitleBar` on bg.sunken with `WA_StyledBackground` set, title at the 13/600 text.hi window-title rank). **Evidence.** `check_qss.py theme.qss .` reports 303 rules, 871 declarations, 24 palette tokens, **0 errors 0 warnings**. Both configurations link (`noggit.vcxproj -> H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe` and the `build-nosql` equivalent); `NoggitWindow.cpp` and `SettingsPanel.cpp` were force-recompiled and produced no error and no new warning (the two that appear, `C4005` on `revision.h` and `C4129` at `NoggitWindow.cpp:903`, are pre-existing and on lines this row does not touch). Catch2 unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. Both executables were launched with project auto-load temporarily off so the owner's live project was never opened (the setting was restored afterwards), both showed `Noggit Crimson Project Selection`, both exited on `CloseMainWindow` with code 0, and the fresh 13-line `log.txt` carries no error line. The slider, heading, primary-button and hint-label changes were also rendered off-screen through the probe under this exact sheet and looked at, before and after. **What is not covered:** nothing here was seen inside the running editor — there is no way to drive the GUI from this session — so the tool dock, the map list and the frameless path all need a human to confirm, and the frameless path in particular is still off by default with its menu-bar defect still open. |
| 2026-08-25 | `src/noggit/ui/tools/UiCommon/ExtendedSliderUi.ui`, `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.cpp`, `src/noggit/ui/TerrainTool.cpp`, `src/noggit/ui/minimap_widget.{hpp,cpp}`, `src/noggit/ui/FramelessWindow.hpp` — tool dock, view toolbar and minimap affordance pass, appearance only: no control changed range, step, signal or semantics and none was removed. **The brush slider band stays 16px. An earlier revision of this row claimed it had to go to 20px “because that is what sets the HIT TARGET”, and that claim was wrong; the widening is reverted and the file's comment now records the correct measurement.** The retracted claim was that Qt derives the `SC_SliderHandle` rect from the WIDGET's height and never from the QSS handle box, leaving the grip's top and bottom rows page-stepping at a 16px band (quoted as 16x12 = 192 px² at band 16 and 16x16 = 256 px² at band 20). Re-measured with a standalone Qt 5.15.2 probe that loads the shipped sheet, puts a 212px slider at 50 percent and sweeps the pointer over every pixel of the control, counting the positions `QStyle::hitTestComplexControl(CC_Slider, ...)` reports as `SC_SliderHandle` — the same call `QSlider::mousePressEvent` uses to choose drag over page-step — the handle rect is **16x16 = 256 px² at bands 16, 18, 20 and 22 alike**, because `QStyleSheetStyle` sizes the handle from the QSS box (6px groove plus the -5px margins) and centres it in the band. The widening bought zero grabbable pixels and instead put four rows of page-step area inside the grip's own row, on the control that sets brush radius and speed, for 4px × 18 instances. Band 14 is the first that clips (16x14 = 224), so 16 is the smallest correct value; an UNSTYLED slider does behave the way the retracted claim assumed (16x20 = 320 at band 20 under `QWindowsVistaStyle`), which is where the mistake came from, but this application always has a sheet loaded. What did move the hit target is the theme's 6px-tick-to-16px-grip rewrite, 6x16 = 96 to 16x16 = 256, 2.7× — measured in the same sweep against the pre-change sheet, and consistent with the separate theme row's claim. **`SliderAction`'s value read-out no longer resizes the slider under the pointer.** It is right-aligned and pinned to the widest reading its formatter produces across eleven samples of the range (measured, not assumed to be at an endpoint: the climb formatter converts radians to degrees and rounds), because the slider was the only expanding item in that row and every digit the number gained or lost was paid for by the slider mid-drag. The read-out string is now produced by one formatter instead of two copies kept identical by hand, and that slider gains a 160px minimum width — it spans 1571 steps and fell back to the style's ~84px hint, i.e. 19 units of travel per pixel. No range, step, page step, decimals, signal or slot changed at any of the eighteen `ExtendedSlider` construction sites or at either `SliderAction`. **The minimap's “Select a map” placeholder is quiet, palette-driven and out of the paint path.** It was `QFont("Arial", 30)` — a hardcoded family at 30 POINTS in `QPalette::WindowText`, the loudest text role there is — and the `QFont` was constructed inside `paintEvent`, which on this widget runs on every mouse move. It was also too large to be read: `QFontMetrics` under a probe puts that string at 348x67 px against a widget whose minimum is 128px, and `AlignCenter` with no wrap flag clips rather than shrinks, so the pane rendered “elect a ma” at 128px and at 256px alike. It is now the widget's own font at 13px (75x17 px, fits from the 128px minimum up) in `QPalette::PlaceholderText`, which `darkPalette()` already sets to the text.dim token #8A93A0: 5.72:1 on the #16181D of `main_menu_minimap_holder` and 5.38:1 on the #1B1E24 base the other hosts sit on, both over the 4.5:1 floor and deliberately under body rank. The font is cached in a member and rebuilt from `changeEvent(QEvent::FontChange)`, so it follows the application font without being reconstructed per repaint. **Two comments in `FramelessWindow.hpp` overstated what the code does and are corrected; no code in that file changed.** The header said `setTitleBarHeight(sizeHint)` “closes” the caption band, but the hit test is `localMouse.y <= (tbh + bh)` (`winnativeeventfilter.cpp`, the `getHTResult` lambda) and that call sets `tbh` only, so the band still overhangs the bar by `bh` device pixels whatever is passed — a reduction, not a closure, and the comment now says so. The header also labelled its measured 45px band “100% scaling, dpr 1.0”; a per-monitor-aware probe reads this desktop at 144 dpi (150%, `SM_CYCAPTION` 34 + `SM_CYSIZEFRAME` 5 + `SM_CXPADDEDBORDER` 6 = 45 device px), and a Qt probe with `AA_EnableHighDpiScaling` reports `devicePixelRatio` 2.0 on the same monitor because Qt 5.14+ rounds 1.5 up, while framelesshelper converts what we pass with its own `GetDpiForWindow/96` = 1.5 — so the 45-vs-29 comparison was mixing device and Qt pixels, and the corrected comment records both ratios and that they agree only at 100%. `makeTitleBarWidgetClickable` is kept but its comment no longer implies the menu-bar defect is fixed: it states that nothing calls it, and gives the exact one-line edit — `Noggit::Ui::makeTitleBarWidgetClickable(this, widget, _menuBar);` immediately after `titleBarWidget->horizontalLayout->insertWidget(2, _menuBar);` in `NoggitWindow.cpp`, currently line 115 — which this pass does not own. **Evidence.** Both configurations build and link: `noggit.vcxproj -> H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe` and the `build-nosql` equivalent, with no error and no new warning (the two that appear, `C4005` on `revision.h` and `C4129` at `NoggitWindow.cpp:903`, are pre-existing and on lines this row does not touch). Catch2 unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. Both executables were launched, both showed `Noggit Crimson Project Selection`, both exited on `CloseMainWindow` with code 0, and the log carries no error line. **What is not covered:** the slider and the minimap placeholder were measured and rendered through probes, not seen inside the running editor — there is no way to drive the GUI from this session — so the tool dock and the map list still need a human to confirm, and the frameless path remains off by default with its menu-bar defect open. |
| 2026-08-25 | `dist/noggit-themes/CrimsonSlate/theme.qss` — contrast repair on this batch's own theme pass, plus two comment corrections. Colour and comment only: no selector added or removed, no geometry, size or weight touched except the one font-size named below, and the static checker reports the same 307 rules and 885 declarations before and after. **Every unchecked icon in the application was illegible, and that is what "accent everywhere" was.** The two `QWidget[accessibleName="Font*ButtonStyle"]` rules are not ordinary widget styling: they are the only input to the pen colour of every icon in the editor, because both icon engines polish one parentless probe widget carrying one of those names and read its palette back (`iconPenColor`, `FontAwesome.hpp`). QSS `color` writes WindowText, Text and ButtonText together, so a sheet gets exactly two slots there, and the engine spends them as `QIcon::On` = checked on the enabled rule and `QIcon::Off` = **unchecked** on the `:disabled` rule, with genuinely-disabled separated only by the engine's 0.45 alpha. The `:disabled` slot held text.off #59606B, read off the selector name as if it meant disabled. That measured **2.12:1** on bg.overlay and 2.39:1 on bg.raised, so 15 of the 16 left-strip tools and 12 of the 20 view toggles were under any legibility floor simultaneously; the only glyphs that survived were the checked ones on their accent plate or over their accent underline, which is the whole reason the chrome read as accent-coloured. The accent was not spreading, the neutrals had gone. The slot now holds a new token, **text.mid #A0A8B4**, declared in the sheet's header table so the checker accounts for it: unchecked measures **5.60:1** on bg.overlay, 6.32:1 on bg.raised, 6.96:1 on bg.base and 7.40:1 on bg.sunken, against checked #C6CCD6 at 8.33 / 9.39 / 10.34 / 11.00 on the same four. Unchecked hover, which the engine derives as `lighter(125)` rather than reading, goes #6F7886 to #C8D2E1, i.e. **1.94:1 to 5.66:1** on the stroke.hi hover plate both bars use — the old hover was fractionally *worse* than the old resting state. Genuinely disabled stays deliberately dim at 2.29:1 (composite #5F6570), against 1.39:1 before, and the point is that it is no longer identical to unchecked. Checked-to-unchecked is a 1.49:1 step and is a supporting cue only; the accent plate, the 2px leading edge and the 2px underline are untouched and remain the primary state marks. **Measured, not derived.** A standalone Qt 5.15.2 probe loads this sheet and nothing else, polishes a widget under each of the two accessible names, runs `iconPenColor`/`iconPenColorForMode` verbatim over the palette it reads back, then lays a real `QToolBar` out inside a real `leftToolbarHolder`, grabs it and samples pixels. It reports Disabled WindowText #59606B to #A0A8B4 and the derived hover #6F7886 to #C8D2E1, and the grabbed strip gives the checked glyph on the checked face at 5.49:1 and the unchecked glyph on the bar at 5.60:1. **The probe also corrected an arithmetic assumption made without it:** a hand-written HSV model of `QColor::lighter(125)` predicted #ECF3FF for the checked hover pen where Qt actually produces #F8FBFF. The probe lives in the session scratchpad and no file in the repository refers to it. **The selected project row's second line was below the floor in three of its five states, not one.** The 0.18 wash is kept — its area argument is sound and the 2px accent edge is the mark — but the text.dim caption on it had been measured only on the best-reading state. All five: at rest 5.38:1, hovered 4.75:1, selected 4.35:1, **selected and hovered 3.80:1**, selected with the window inactive 4.33:1. Lifting the wash cannot fix that column (at 0.12, low enough to stop reading as an elevation step, selected-and-hovered still only reaches 4.13:1), so `QLabel#project-information` takes text.mid instead: 6.96 / 6.15 / 5.64 / **4.92** / 5.60, minimum 4.92:1. Size and weight are unchanged, so the caption still reads as secondary to the 14px/600 title — which is what carried that rank anyway. The comment's claim of "the 3:1 floor this sheet uses for a caption on a tinted plate" is removed: no such floor exists here, 3:1 is the WCAG minimum for a graphical object or for large text, and this is 11px prose. **The Settings restart hint goes back to body rank.** "Changes may not take effect until next launch" is the only thing in that window explaining why a saved setting appears to do nothing, and it had been demoted from 12/400 text (10.34:1) to 11/400 text.dim (5.38:1) to separate it from the buttons beside it. The Save button's new accent fill had already done that, and it is a difference in kind rather than of degree; the demotion was a second answer to a settled question, paid for with **half the hint's contrast and a sixth of its size**. It is 12px #C6CCD6 again at 10.34:1 on bg.base, keeping the 12px right margin that pass added, which was right. **The slider comment now describes only what this file decides.** It asserted "a 2.7x larger hit target" — that is the painted-area ratio; the hit target did not move, because Qt derives `SC_SliderHandle` from the widget height — and it asserted a specific pinned widget height in `ExtendedSliderUi.ui`, a file this sheet does not own and whose value changed in this same batch. Both are gone: the comment states the 16px painted box (12px fill plus a 2px ring), the 6px groove and the -5px margin that closes it, records that a taller band leaves empty rail rather than clipping, and names no number belonging to another file. The corresponding overstatement in this table's earlier row is corrected in place rather than deleted. **Evidence.** `python check_qss.py theme.qss H:/NoggitUpdate` reports 307 rules, 885 declarations, **25** palette tokens (24 before; text.mid is the one addition), **0 error(s), 0 warning(s)**. Both configurations link, to `build/bin/RelWithDebInfo/noggit.exe` and `build-nosql/bin/RelWithDebInfo/noggit.exe`. The Catch2 suite is unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. Both executables were launched, each showed a window titled `Noggit Crimson Project Selection`, each exited on `CloseMainWindow` with code 0, and each wrote a 13-line `log.txt` with no error, failure or warning line. No setting was changed to do that: `favorite_project` reads -1, so the `load_favorite && favorite_proj_idx != -1` test at `NoggitProjectSelectionWindow.cpp:47` is already false and no project auto-loads. **What is not covered.** Nothing here was driven inside a loaded map — there is no way to operate the GUI from this session — so the tool strip and the view-toggle bar were measured through the probe and the palette rather than looked at in the running editor with a project open. text.dim is left exactly as it was for every caption sitting on bg.base or bg.sunken, where its 5.38:1 and 5.72:1 are fine; only the two roles on lighter surfaces move. |
| 2026-08-25 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `dist/noggit-themes/CrimsonSlate/generate_icons.py`, `dist/noggit-themes/CrimsonSlate/images/*.png` (all 34, regenerated), `dist/noggit-themes/CrimsonSlate/nodes_theme.json`, `dist/noggit-themes/CrimsonSlate/check_qss.py`, `src/noggit/application/ApplicationEntry.cpp` — **the palette itself was replaced.** Appearance only: no control changed range, step, signal or semantics, none was removed, and the static checker's selector-coverage mode reports **0 dropped** selectors against the previous sheet (399 -> 400; the one addition is the base rule `QWidget#leftToolbarHolder > QToolBar QToolButton`, which the strip needed in order to declare its own box). **Why four previous passes were invisible, measured rather than asserted.** Each of them fixed a real defect inside a colour scheme nobody had redesigned. The four "surface levels" spanned **1.321:1 end to end** — adjacent steps 1.064, 1.101 and 1.128, all under the just-noticeable difference for a large flat field — so the application had one surface wearing four names, which is why it read as a wireframe of itself. And the accent #E8543F and the error #E0574B measured **1.024:1 apart in luminance and 4.0 degrees in hue**, i.e. the same colour, so red simultaneously meant selected, focused, checked, active tool, pressed, danger, error, delete and hover-on-close. **The new palette is warm slate with a gold accent** (every neutral at hue 30-40 degrees, 6-19 percent saturation; gold because it is the only accent that stays fully saturated at the lightness this palette needs — the blue candidate #6FAEDC is only 61 percent saturated at L=0.388). Surfaces bg.void #100E0B, bg.alt #1D1916, bg.panel #292621, bg.raised #3C3732, bg.overlay #4A4640, bg.hover #5B5651; adjacent steps **1.104 / 1.158 / 1.281 / 1.256 / 1.292** (six surfaces, so five steps -- an earlier draft of this row listed four and opened with 1.279, which is not a step between any adjacent pair here). The ladder is deliberately NOT uniform, and the two bottom steps are the honest weakness of it: bg.void -> bg.alt at 1.104 and bg.alt -> bg.panel at 1.158 are still near the just-noticeable difference for a large flat field, because bg.void has to stay dark enough to sit beside a lit 3D viewport and bg.panel has to keep body text at 7:1. Those two are separated in practice by a stroke, not by fill alone. End to end the surface ladder is **2.057:1** (bg.void to bg.overlay; bg.hover is a state rather than a surface, and void to hover is 2.657:1) against the previous palette's **1.321:1** total -- so the span roughly doubles even though the bottom of the ladder does not. The window recedes, a button comes forward to bg.raised, an input and an item view go BACK to bg.void as wells, and a popup floats at bg.overlay. **bg.overlay is not lighter because the 7:1 body floor binds:** text #E4DFD7 on #4A4640 is 7.065:1 and on #4B4741 it is 6.955:1 and fails, so bg.hover carries icons and short labels only. Text ranks measured on void / alt / panel / raised / overlay / hover — text.hi #F3F0E9 16.93 / 15.34 / 13.24 / 10.34 / 8.23 / 6.37, text #E4DFD7 14.53 / 13.16 / 11.36 / 8.87 / **7.07** / 5.47, text.dim #BFB7AA 9.70 / 8.79 / 7.58 / 5.92 / 4.72 / 3.65, text.off #7F786A 4.40 / 3.99 / 3.44 / 2.69 / 2.14 / 1.66. Body clears 7:1 on every surface it is permitted on and nothing enabled is below 4.5:1; text.off is the documented exception WCAG 2.1 SC 1.4.3 exempts, and it is never used for enabled text. **The accent moved crimson -> gold in ONE edit, everywhere at once**, because half-swapped is worse than either end state: accent #DFA52E, accent.hi #F0BA4A, accent.press #B8801F, ink #100E0B on all three at **8.77 / 10.85 / 5.64** (the old crimson managed 4.88:1 with the same ink, which is why the primary button was always marginal, and why its press had to invert to light text — gold does not). Accent versus error is now **34.5 degrees and 1.389:1** where it was 4.0 degrees and 1.024:1. Status is ok #4FB87E, warn #E2803C, bad #E86F62, info #6FAEDC — 6.09 / 5.30 / 4.94 / 6.29 on bg.panel, used as text only on bg.void and bg.panel and as borders and fills above that. **Honest limit:** accent versus warn is 15.8 degrees and warn versus bad is 18.8, so hue alone cannot separate them; a warn or error input therefore takes a coloured border AND a 14-percent tinted fill (bad -> #2E1C17, warn -> #2D1E12) while a focused input takes a gold border and is NEVER tinted. **The two bars over the 3D view now move in opposite directions: hover LIFTS, active RECESSES.** On the bg.overlay card an inactive tool is transparent with a text.dim glyph at 4.72:1, a hover lifts to bg.hover (1.292:1) with a stroke.hi edge and a derived #E6E3DD glyph at 5.66:1, and the active tool drops to bg.void — **2.057:1 below the card** — with a 3px gold leading rule at 4.27:1 and a gold glyph at 8.77:1, three simultaneous cues that cannot be confused with a neighbour that went up. The view-toggle bar is the same model rotated onto a 2px bottom rule, so eight recessed dark wells with gold glyphs read as a row of latched switches. **The icon pen is two declarations and they are the only input to every glyph in the editor.** Both engines resolve it from one parentless probe widget's palette, and QSS `color` writes WindowText, Text and ButtonText together, so a sheet gets exactly two slots: the plain rule is `QIcon::On` (checked) and the `:disabled` rule is `QIcon::Off` (unchecked, and also genuinely disabled at the engine's 0.45 alpha). They are now accent #DFA52E and text.dim #BFB7AA. text.dim rather than text is forced by arithmetic, not taste: hover is derived as `QColor::lighter(125)`, and on text #E4DFD7 that **clips to pure #FFFFFF** (HSL lightness 0.869 x 1.25 = 1.000), as does text.hi; #BFB7AA sits at 0.708, lifts to 0.885 and lands on #E6E3DD, a real 1.551:1 response. Disabled composites to #5F5A53 / #6C675F / #777168 / #7F7970 on void / panel / raised / overlay — still deliberately sub-threshold, but no longer identical to unchecked. **All 34 PNGs were regenerated**, which is the single most likely way a pass like this gets dismissed as unfinished: the fifteen colour constants in `generate_icons.py` were repointed, `INPUT` moved from bg.raised to bg.void because an indicator is a well now, the ring went 1.6 -> 4.0 canvas units (the sheet displays that canvas at half size, so 0.8px -> **2px**, which is what the component spec asks for) and the corner 5.0 -> 8.0 (2.5px -> **4px**), and the disabled indicators take a bg.panel fill with a stroke.soft ring instead of a flat hairline slab. `nodes_theme.json` moved with them or the node editor would have been the one window left blue-grey. **The application QPalette moved in the same edit**, since anything QSS does not select falls through to it: Window=bg.panel, Base=bg.void, AlternateBase=bg.alt, Button=bg.raised, Highlight=accent, HighlightedText=bg.void (8.77:1), Light=stroke.hi, Midlight=bg.overlay, Mid=stroke, Dark=bg.panel, Shadow=bg.void, and Disabled Highlight drops to bg.raised so a selection in a dead view is a recess rather than a gold plate. QPalette::Link takes info rather than the accent, because a hyperlink is information and the accent means one thing. **MEASURED WITH A STANDALONE Qt 5.15.2 PROBE, and it corrected the brief.** Control height 28px was specified with a combo formula of `h = 23 + vpad`; the probe swept every vertical padding pair from 3/2 to 7/6 and the real relation is **`h = 19 + vpad_top + vpad_bottom`**, so the specified `3px ... 2px` would have shipped a **24px** combo box beside a 28px button. It is `5px 24px 4px 10px`, and the probe reports QPushButton, QLineEdit, QSpinBox and QComboBox all at exactly 28x1. The brief also said to write the 34x34 tool buttons as `min-`/`max-width`/`height: 34px`; QSS min- and max- size the **content rect**, not the box, and the probe measured that spelling laying out at **44px**. The rules declare the content size that arrives at 34 after padding and borders (strip: 3+3+22+5+1 wide, 1+4+24+4+1 tall; view bar: 1+6+20+6+1 and 1+5+23+3+2), and the probe confirms **34x34** on both bars in both states. `QFontMetrics` puts the 13px section heading at a **17px** line box, not the 18 assumed, so `QGroupBox` margin-top is 22px with the title padding at 5px rather than 6 and the probe reports `contentsRect().top() = 35` = 22 + 1 border + 12 padding, with no clipping. **Rendered evidence, sampled from real grabs under this sheet.** Tool strip: card #4A4640, resting face #4A4640, checked face **#100E0B at 2.057:1**, checked leading rule **#DFA52E at 4.265:1**. View bar: OFF #4A4640, ON **#100E0B**, ON bottom rule #DFA52E. Tool panel: body #292621, button face #3C3732, input well #100E0B, **button versus input 1.638:1** where the previous sheet had them on the same surface at 1.000:1, primary fill #DFA52E, danger edge #E86F62, list well #100E0B, selected row **#413213 at 1.551:1** against the well with text.hi on it at 10.87:1 and text.dim at 6.23:1 — which is why `text.mid` is **deleted** rather than maintained and `QLabel#project-information` goes back to text.dim: the worst reading in the five-state project-row matrix is now **4.88:1** against 3.80:1 before. Selection on bg.overlay is the one place it goes DOWN instead of tinting, because no accent alpha keeps body text at 7:1 there (0.16 gives 5.49:1, 0.24 gives 4.81:1), so a selected menu item recesses to bg.raised (body 8.87:1) plus a 2px gold rule at 5.36:1. **Evidence.** `python check_qss.py theme.qss .` reports 308 rules, 939 declarations, 26 palette tokens, 45 url() references resolving to all 34 images, **0 error(s), 0 warning(s)**; with `--moc build/noggit_autogen` the only four warnings are the pre-existing dead selectors this sheet already documents (`Noggit--Ui--toolbar` and `PaletteList`, neither of which carries `Q_OBJECT`, plus the two single-dash spellings kept for comparison). Both configurations compile and link — `noggit.vcxproj -> H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe` and `H:\NoggitUpdate\build-nosql\bin\RelWithDebInfo\noggit.exe` — with `ApplicationEntry.cpp` recompiling clean and no new warning. Catch2 unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. The executable was launched, showed a window titled `Noggit Crimson Project Selection`, exited on `CloseMainWindow` with code 0, and wrote a 13-line `log.txt` with no error line and no theme-fallback line, so the sheet loaded. **What is not covered.** Nothing was driven inside a loaded map — there is no way to operate the GUI from this session — so the strip, the toggle bar, the menus and the panels were measured through grabs and arithmetic rather than looked at with a project open, and the claim that the chrome stays legible beside snowy Dun Morogh rests on the card being 2.057:1 darker than the app's own darkest chrome, not on a screenshot of snow. Three hardcoded inline stylesheets remain immune to any palette (`Vector3Widget.cpp`'s literal red/green/blue, `MapCreationWizard.cpp`'s `ERROR_OUTLINE_QSS`, and the inline `styleSheet` property in `TitleBar.ui`); they belong to other files and are not touched here. The `titlebar_close` glyph is a single PNG applied through `qproperty-icon`, which is resolved at polish time and cannot change with the button state, so one pen has to serve both the resting bar and the destructive hover fill and no colour clears 3:1 on both — text.hi wins at 16.93:1 at rest, so the FILL is the half that had to move: it is close.hover #A75047 (text.hi 4.75:1) and close.press #974840 (5.56:1) rather than bad #E86F62, on which the one available pen measured 2.68:1. An earlier draft of this row claimed "ink on bad, 6.31:1" for that glyph, which described a glyph the theme has never shipped -- `generate_icons.py` writes the pixel as text.hi. |
| 2026-08-25 | `src/noggit/ui/DesignTokens.hpp` (new), `src/noggit/ui/Toolbar.cpp`, `src/noggit/ui/FontAwesome.hpp`, `src/noggit/ui/minimap_widget.cpp`, `src/noggit/ui/SpawnTilePicker.cpp`, `src/noggit/ui/texturing_tool.cpp`, `src/noggit/ui/AlphaIntegrityReport.cpp`, `src/noggit/ui/AutoTextureDialog.cpp`, `src/noggit/ui/widgets/Vector3Widget.cpp`, `src/noggit/ui/tools/ToolPanel/ToolPanel.cpp`, `src/noggit/ui/tools/ToolPanel/ToolPanelScroll.ui`, `src/noggit/ui/tools/MapCreationWizard/Ui/MapCreationWizard.cpp`, `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.cpp` — the Ironforge design system applied at the WIDGET level, where a style sheet cannot reach. Appearance and affordance only: no control changed range, step, signal or semantics, none was removed, and every edit is fill, border, radius, pen, font or layout margin. **The problem this addresses is that a theme pass cannot fix a colour set from C++.** Three kinds of site are immune to `theme.qss` — a `QPainter`, a `QGraphicsColorizeEffect`, and a widget-level `setStyleSheet`, which OUTRANKS the application sheet for every property it names — and all three were carrying hand-picked literals. `DesignTokens.hpp` (new, header-only, carries the project GPL header) states the palette, the type scale, the 4px spacing scale and the radii once, in `Noggit::Ui::Design`, so the painted half of the application and the styled half cannot drift apart again. It deliberately does not read the active theme; it is the floor, and any site QSS *can* reach is left to QSS. It is distinct from and does not collide with `src/noggit/ui/windows/UiStyle.hpp` (different guard, different namespace, different scope). **Contrast failures fixed, all measured (WCAG 2.1 relative luminance, sRGB), not estimated.** `Vector3Widget`'s three axis chips were `background-color: red|green|blue` with `color: white` — Qt colour NAMES, i.e. pure #FF0000, #008000 and #0000FF. White on pure red is **3.99:1 and fails the 4.5:1 floor**, pure green 5.14:1, pure blue 8.59:1, so the three were also wildly unequal in weight. They are now the status triad with INK on them: X BAD **6.31:1**, Y OK **7.79:1**, Z INFO **8.04:1** — all passing, within 1.27:1 of each other, and 106.5 and 165.0 degrees off the accent so an axis can never read as "selected". **The minimap's markers were the worst of it, and the obvious fix would have made them worse.** The camera arrow was `Qt::red` and the sky spheres `Qt::blue`; the map underneath them spans the game's whole brightness range, so measured against snow #E8ECEF and shadowed rock #2A2118 they ran 3.37/3.95 and 7.23/**1.84**. Swapping in palette tokens alone would have given ACCENT 1.85 on snow and INFO 2.02 — *worse than what they replaced*. Every marker is therefore stroked in INK at 3px and then in its token at 1px, so it is read against its own outline: ACCENT on INK 8.77:1, INFO on INK 8.04:1, TEXT_DIM on INK 9.70:1, none of which move with the terrain, while the outline is 16.22:1 on snow and 9.52:1 on sand. Where the outline goes weak (1.22:1 on dark rock) the marker is already 6.59-7.19:1 against the terrain directly, so the pair always has exactly one strong edge. The same file's unsaved-tile marker went pure #FFFF00 to WARN (`db.dirty` is warn everywhere) and its tile selection pure #FF0000 to ACCENT. **The crimson overload, removed from the painted surfaces.** The texture opacity slider's handle was pure red on a pure-black-to-pure-white ramp, on the one control in the editor with no error state and nothing destructive about it. The ramp now runs RAMP_LO to RAMP_HI (the palette forbids pure black and pure white; the ramp still spans 17.36:1 and the trim is 1.4% and 5.1% off the ends) and the handle is ACCENT, 34x6 at radius 2. **The 1px INK border on it is load-bearing, not decoration:** ACCENT against RAMP_LO is 9.00:1 but against RAMP_HI only **1.93:1**, so a borderless gold grip would vanish at full opacity — worse than the red's 4.00:1 there. INK is 16.93:1 against RAMP_HI and 8.77:1 against ACCENT, so contrast never drops below 8.77:1 anywhere on the range. `SpawnTilePicker`'s four colours became INFO/OK/WARN/ACCENT; its selection outline was (255,40,40), so the tiles you had deliberately chosen looked like a warning about them (ACCENT is 8.77:1 on BG_VOID against that red's 5.13:1). `MapCreationWizard`'s invalid-path outline was hardcoded #E0574B — **1.024:1 and 4.0 degrees from the old accent, i.e. the same colour the theme used for focus**, and an invalid field is almost always also the focused one. It is now BAD plus the FILL_ERROR tint, because gold and WARN are only 15.8 degrees apart and the design system makes that separation structural: an invalid input takes a border AND a tint, a focused input takes a border and NEVER a tint. TEXT_HI on that tint is 15.04:1 and the edge against its own fill 5.61:1. **A real defect in the icon layer, found by reading the mechanism rather than the comment.** `FontAwesomeButtonStyle` set `accessibleName("FontNoggitButtonStyle")` — the OTHER engine's probe — so the two icon sets shared one selector and *every* theme's `QWidget[accessibleName="FontAwesomeButtonStyle"]` rule was dead, matching no widget in the process. Verified appearance-neutral before changing it: CrimsonSlate, Dark and Dark/editing_theme each give the two selectors byte-identical `color` and `:disabled` declarations, so no icon in any shipped theme moves; the rule simply becomes reachable, which it must be if a sheet is ever to distinguish the general icon font from Noggit's own tool glyphs. That file's measurement table, which still quoted the retired near-neutral palette, is replaced with the four Ironforge pen states measured across all six surfaces. **The tool strip and view-toggle bar glyphs were being resampled, and the size was measured with a standalone Qt 5.15.2 probe rather than assumed.** Nothing set `iconSize` on these bars — not the widgets, not `MapView`, and not the theme, which gives `qproperty-iconSize` to the window controls and dock buttons but to no tool bar — so it fell through to `QStyle::PM_ToolBarIconSize`. The probe reports style `windowsvista`, **PM_ToolBarIconSize = 36**, and `QToolBar::iconSize()` 36x36 both with and without the sheet applied. A reasonable guess would have been 24; it is 36. The design system asks for a 20px icon in a 34x34 button and the sheet leaves a content box near 22px, so all 36 glyphs on the two bars that sit over the 3D view for the entire session were rasterised at 36 and resampled down to about 22 — a 61% reduction, and for a FONT glyph that is the whole difference between a crisp stem and a soft one. Both bars now pin 20, which the engines rasterise at 1:1. The BUTTON box is untouched and stays with the sheet, which overrides `setFixedSize` from C++. **Spacing and type.** `ToolPanelScroll.ui`'s four zero margins became the design system's (12, 8, 12, 8). **That change is only safe because the width floor moved with it:** `TOOL_CONTENT_WIDTH` is a contract, the texturing tool's Paint tab asks 229px against it, and taking 24px out of a fixed 250 would have left 226 and put a horizontal scroll bar under the most-used tool in the editor — exactly what that constant exists to prevent. So the scroll area's `minimumSize` went 250 to 274 and the dock minimum grew by the same 24px, leaving the content floor at exactly 250. The probe also recorded what this does *not* fix: `PM_LayoutLeftMargin` is **13px**, and texturing/terrain pin their own 9px, so the dock's contents were never flush (9 or 13 in) and now sit at 21 or 25. That 9-vs-13 disagreement is pre-existing and is not made worse, but it is why the total inset is not the flat 12px the scale describes; closing it means zeroing contents margins across roughly sixteen tool widgets and is out of scope here. `AutoTextureDialog`'s report took the monospace rank (Consolas 12px regular, `StyleHint(Monospace)` as the fallback) instead of `systemFont(FixedFont)`, which resolves to Courier New at the system POINT size and made the editor's three monospaced readouts three different typefaces. `AlphaIntegrityReport`'s two emphasis states took the status ranks — "no scan yet" to TEXT_DIM (7.58:1 on BG_PANEL) and "NOTHING WAS CHECKED" to WARN (5.30:1), weight AND colour together since the design system never lets a warning ride on hue alone. Those two keep their widget-level sheets deliberately: the emphasis is per-STATE, no selector can see which branch the label is in, and `QFont::setBold` would lose to the theme's own `QLabel { font-weight: 400 }`. **Audited and deliberately left alone,** so the next pass does not redo the search: `TerrainTool.cpp:131` (geometry only), `texturing_tool.cpp`'s old slider sheet (inside a block comment, dead), `GroundEffectsTool.cpp:371` (commented out), `expanderwidget.cpp:42` (`text-align: left` has no widget-level equivalent on QPushButton and no theme sets it), `NoggitProjectCreationDialog.cpp:21` (the `QLabel#icon` selector does match — the line above it renames the widget), and `TitleBar.ui`'s two `styleSheet` properties, which are empty strings and therefore inert. **Evidence.** Both configurations build and link: `noggit.vcxproj -> H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe` and the `build-nosql` equivalent. All eight touched translation units were force-recompiled and produced **no error and no warning of any kind**. Catch2 unchanged at 422 cases / 417 passed / 5 skipped / 276,964 assertions. Both executables were launched, each showed a window, each exited on `CloseMainWindow` with code 0, and each wrote a log with zero error, failure or warning lines. Every ratio quoted above was produced by a script implementing the WCAG per-channel linearisation and `(Lmax+0.05)/(Lmin+0.05)`, validated first against the design system's own published figures, which it reproduces to three decimal places. **What is not covered:** nothing here was seen inside the running editor with a map loaded — there is no way to drive the GUI from this session — so the axis chips, the opacity slider, the minimap markers, the tool-strip glyph size and the tool-dock margins were measured and reasoned about but not looked at, and all of them need a human to confirm. |
| 2026-08-25 | `src/noggit/ui/windows/UiStyle.hpp` (new), `src/noggit/ui/windows/projectSelection/NoggitProjectSelectionWindow.{ui,hpp,cpp}`, `src/noggit/ui/windows/projectSelection/widgets/ProjectListItem.cpp`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.{hpp,cpp}`, `src/noggit/ui/windows/noggitWindow/widgets/MapListItem.cpp` -- applied the Ironforge design system's spacing scale and type ranks to the three surfaces a user meets before the 3D view: project selection, map selection, and the editor's menu bar and status bar. **Rhythm.** Every margin in these windows was previously a local decision -- the project selection window ran a 5px spacer next to a 10px margin next to uic's default 6px gap, and the map column had 8px gutters beside a 9px one -- so `UiStyle.hpp` states the 4px scale once and both windows draw from it and nothing else. **Type ranks, and why they are style sheets rather than `setFont`.** Measured against the shipped sheet with a standalone Qt 5.15.2 probe: a label asking for 15px through `setFont` polishes to 12px, because the sheet opens with a bare `QWidget { font-size: 12px; }` that every widget in the application matches and a style sheet font outranks `setFont`. Weight survives, size never does. `Style::applyRank` therefore writes the smallest possible widget sheet -- `font-size` and `font-weight`, no colour -- which the same probe shows still lets the application sheet colour the label, so combining it with the published `[state="value"]` rank yields 15px/600 at text.hi with colour still owned by the theme. Where the sheet already publishes the wanted rank the object name IS the rank and no widget sheet is written at all. **The status bar.** `MapView` adds seven readouts and names none of them, so the sheet could only reach them as `QStatusBar QLabel` and painted all seven identically. `MapView` is not this pass's to edit, but the `QStatusBar` is: `NoggitWindow` now filters `QEvent::ChildAdded` on it and dresses each arriving `QLabel` with an object name and, for position, frame rate and the loaded/rendered counts, the value rank -- which is the first time `QLabel[state="value"]` has ever fired, the rule having shipped with nothing in `src/` setting the property. A probe confirmed the burst arrives as exactly seven `ChildAdded` events in `addWidget` order and that the mapping wraps correctly on a second map entry; the positional identity and its limits are written at the call site. **A layout defect the capture found.** The project selection form gave one column `QLayout::SetFixedSize`; Qt applies that by calling `setFixedSize` on the layout's `parentWidget()`, which for a nested layout is the widget owning the top-level layout, so one column's size hint was pinning the whole window. **The right-hand pane of the map screen** was a scroll area containing a square minimap and nothing else; it gains a head naming the selected map and its type, and a foot saying what to do with the minimap, both built only from the row already selected in the list so no client lookup can fail. **Measured contrast**, sampled off real captures rather than asserted: primary button label ink `#100E0B` on accent `#DFA52E` 8.77:1; the search placeholder renders text.dim `#BFB7AA` where the combo boxes beside it render content at text.hi `#F3F0E9`; the pane head `#F3F0E9` on `bg.panel` 13.24:1 over its 11px text.dim `#BFB7AA` subtitle at 7.58:1. The plus glyph was removed from the primary button for the same reason: both icon engines resolve their pen from two theme-owned slots, neither of which is ink, and text.dim on the accent fill measures 1.106:1. The favourite-star colour in both list rows is a `QGraphicsColorizeEffect` and so is unreachable from any sheet -- it is moved to the new accent by hand and named. No control's range, step, signal or set changed. |
| 2026-08-26 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `src/noggit/application/ApplicationEntry.cpp`, `src/noggit/ui/DesignTokens.hpp`, `src/noggit/ui/minimap_widget.cpp`, `src/noggit/ui/FontAwesome.hpp`, `src/noggit/ui/texturing_tool.cpp` -- **repair pass over the palette change in the three rows above, which shipped one real regression and five statements that were not true of the code they sat beside.** Appearance and comments only; no control changed range, step, signal or semantics. **The regression: the minimap markers lost their halos.** Moving them onto palette tokens was correct, but it was done by replacing the pen outright, and that file's own header warns against exactly this -- the map under the markers spans the game's entire brightness range, so ACCENT alone is 2.20:1 on snow, i.e. *worse than the pure red it replaced*. An invisible dirty-tile marker is not a cosmetic defect: it is how somebody closes a map without saving. Every marker is stroked INK first and its token second again, so it is read against its own outline and not against the terrain -- INK is 19.27:1 on snow, and where the outline goes weak on dark rock the core carries it instead, so the pair always has one strong edge. The tile markers take a halo clamped to the cell width (`tileMarkerHaloWidth`), floored at `MARKER_PEN_WIDTH + 1`, because a flat 3px pen on a 2px cell at the widget's 128px minimum would paint a 3x3 blob across both neighbours and turn a sparse selection into a smear. **Two contrast failures.** The danger button's pressed fill was #C4574A, putting ink at **4.41:1 -- under the 4.5 floor, on a control that is ENABLED**, so nothing exempted it; it is bad.press #C85F54 at **4.79:1**, still 1.32:1 darker than the hover fill it steps down from. The title-bar close glyph is a PNG applied through `qproperty-icon`, which Qt resolves once at polish time and never restates per pseudo-state, so ONE pen serves the resting bar and every fill; the pen must be light (ink is 1.00:1 on the bg.void bar) and the FILL is therefore the half that can move -- close.hover #A75047 and close.press #974840 put text.hi at **4.75:1 and 5.56:1**, where bad #E86F62 gave 2.68:1. **Five false statements, corrected against measurement rather than reworded.** `DesignTokens.hpp`'s FILL_ERROR/FILL_WARN disagreed with the composited values `theme.qss` actually paints (now both #2E1C17 and #2D1E12); the application QPalette had an **inverted shade ramp**, Mid #565049 being lighter than Button #3C3732, which makes QStyle draw bevels inside out (Mid is bg.panel now, below Button); `FontAwesome.hpp` claimed its two pens were "105 degrees in hue" apart when they measure **3.20**; and `texturing_tool.cpp` claimed an 18.5:1 opacity ramp that measures **17.36:1** between RAMP_LO #0C0A08 and RAMP_HI #F2F0EC; and the surface ladder was written as four adjacent steps opening at 1.279 when six surfaces give five steps and the bottom two measure **1.104 and 1.158** -- the palette's real weak point, and the one the previous rows were loudest about having fixed. Two claims in the rows above were corrected in place for the same reason -- they describe code that no longer ships as described. **Evidence.** All eight contrast pairs quoted here were recomputed from the shipped hex values with the WCAG 2.1 per-channel linearisation and `(Lmax+0.05)/(Lmin+0.05)`, and every one matches the figure written beside it in the source: danger press 4.79, danger label 8.87, danger border 3.86, close hover 4.75, close press 5.56, body on panel 11.36, dim on panel 7.58, ink on accent 8.77 -- **8/8 pass** their floor. The build links (`noggit.vcxproj -> H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe`) and the executable was launched, ran 25s to the project-selection window with no startup crash, and wrote no error line to its log. **What is not covered:** still nothing driven inside a loaded map from this session, so the minimap halos in particular are verified by arithmetic and by the pen the code now sets, not by a screenshot over snow. |
| 2026-08-26 | `media/noggit_icon.png`, `media/noggit.ico`, `src/noggit/ui/WindowChrome.{hpp,cpp}` (new), `src/noggit/application/ApplicationEntry.cpp`, `CMakeLists.txt`, `dist/noggit-themes/CrimsonSlate/theme.qss`, `src/noggit/ui/windows/projectSelection/**`, `src/noggit/ui/windows/noggitWindow/NoggitWindow.cpp`, `src/noggit/MapView.cpp`, `src/noggit/World.cpp`, `src/noggit/ui/**`, `patches/0002-*`, `patches/README.md` -- **new product mark, the version string removed from the chrome, a title bar the window manager no longer owns, and a modernisation pass over the controls.** **The mark.** The supplied 1254x1254 crimson-and-gold potion becomes `media/noggit_icon.png` at 256x256 with alpha intact and `media/noggit.ico` as a ten-frame icon -- 16/20/24/32/40/48 as BMP entries, 64/96/128/256 PNG-compressed -- generated with System.Drawing because this machine has neither PIL nor ImageMagick and nothing was fetched. The .ico was read back frame by frame to confirm it, rather than assumed: a single-size or malformed icon is a shipping defect that only shows up on somebody else's taskbar. **The version string.** It was on the launcher banner and in both window titles. A build id belongs where somebody looks for it when filing a bug, so it stays in the About dialog and the startup log and is gone from the chrome; titles now read simply `Noggit Crimson`. `cmake/revision.h.in` also defined `STRPRODUCTVER` TWICE, the second silently winning -- a duplicate object-like macro with differing replacement lists is a constraint violation a stricter compiler may reject. That file lives in the `cmake` SUBMODULE, where a parent-repo diff carries zero content lines and a fresh clone reverts it, so the fix is exported as `patches/0002` under the convention `patches/README.md` already established for `blizzard-archive-library`. **The title bar.** Qt does not own the non-client area, so no theme pass could ever reach it: the launcher shipped a #F3F3F3 system title bar directly above #292621 content, a 13.58:1 step at the seam and the brightest surface in a deliberately dark application. `WindowChrome` sets the DWM caption, caption-text and border colours on Windows 11 and falls back to the dark-mode flag on Windows 10 1809+, via an application-wide show filter so that dialogs opened deep inside tools are covered without any list of them existing to go stale. The caption is bg.panel because that is what a screen grab of the launcher samples immediately below the bar -- the one value that removes the seam rather than merely darkening it. Non-Windows keeps its own title bar; that is a cosmetic shortfall and is not reported as an error. **A correction, recorded because the reasoning was wrong and not just the number.** An earlier revision of this work reported that the launcher cropped its action column, and changed the window minimum and the list size policy to fix it. THE COLUMN WAS NEVER CROPPED. The screen captures were taken by a DPI-UNAWARE process: Qt reports devicePixelRatio 2 on this display, `GetWindowRect` handed the capturing process 1588px for a window whose real surface is 2360px, and the capture was the window's top-left corner read as if it were the window. `QWidget::grab` from inside the application returns 2360x1440 and shows the Create button spanning x 623..843 inside a 1180 logical central widget, 337px clear of the frame. The minimum size is kept on its own merits -- Qt was enforcing none at all, so the window could be dragged until its contents overlapped -- but it fixes nothing that was broken, and the comments claiming otherwise are rewritten. **Controls.** Sliders were rebuilt in the sheet and in the two hand-painted cases, and the pass then went through radii, focus rings, scrollbars, tabs, menus and control density. **Regressions this introduced and this same change set repairs, all found by an adversarial review rather than by use:** the new `QSlider:vertical` band set `min-width: 20px`, and because `QStyleSheetStyle::setGeometry` ASSIGNS minimums rather than raising them it destroyed `OpacitySlider`'s `setFixedWidth(35)`, laying the widget out at 20px while its `paintEvent` still drew a 35px ramp -- the texture opacity slider rendered as a clipped stub, and the fix is an ID-selector rule at specificity 257 against the band's 17; `QToolButton:focus` overwrote the accent leading bar that `:checked` draws, at equal specificity and later in document order, so every checked tool button outside two scoped toolbars lost its \"this mode is on\" cue on focus; the new minimap pixmap cache was devicePixelRatio-unaware and halved the minimap's resolution on exactly the 2x display this was developed on; and `QProgressBar` drew #E4DFD7 text on its own #DFA52E chunk at 1.657:1, unreadable past the halfway mark, now ink at 8.772:1. **Numbers.** A verification pass recomputed every figure the change set states and found SEVENTEEN that were false, including two written the same hour in `WindowChrome` itself; each was recomputed from the shipped values rather than reworded, and the surface ladder claim in the row above this one was corrected in place for the same reason. Contrast is WCAG 2.1 sRGB per-channel linearisation, `(Lmax+0.05)/(Lmin+0.05)`. **Evidence.** `check_qss.py theme.qss .` reports 0 errors and 0 warnings; the build links to `H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe`; the executable was launched, ran to the project-selection window and logged no error line. Two files that are COMPILED but were untracked -- `ToolWidgetStyle.hpp`, included by twelve translation units, and `WindowChrome.{hpp,cpp}` -- are now tracked; either would have produced a commit that does not build for anyone who cloned it. Three files had been rewritten LF->CRLF by a scripted edit, inflating a 75-line change into a 1510-line diff; they are back to LF and every diff in this change set is now content-only. **KNOWN NOT FIXED, recorded rather than hidden:** six hover-ring and read-only-border pairs measure between 1.360:1 and 2.952:1 against the sheet's own new rule that an enabled control's edge is `edge #8A8378` (`theme.qss:1010, :1194, :1833, :2815, :2947, :3040, :3383`); `hole_tool.cpp:43` claims its slider swap changes nothing while `ExtendedSlider`'s fixed 0..100 internal range against a 0..1000 outer range costs 10x drag granularity; and `QLabel[openExternalLinks]` info blue is 3.910:1 on bg.overlay, under the 4.5 text floor on that surface only. |
| 2026-08-27 | `src/noggit/MissingPlacementLog.{hpp,cpp}` (new), `src/noggit/rendering/PlaceholderCube.{hpp,cpp}` (new), `src/noggit/ui/tools/MissingObjects/MissingObjectsPanel.{hpp,cpp}` (new), `src/noggit/ui/windows/projectSelection/widgets/LauncherArt.{hpp,cpp}` (new), `src/noggit/AsyncObject.cpp`, `src/noggit/ModelInstance.cpp`, `src/noggit/WMOInstance.cpp`, `src/noggit/rendering/WorldRender.cpp`, `src/noggit/MapView.{cpp,h}`, `src/noggit/World.{cpp,h}`, `src/noggit/liquid_layer.{hpp,cpp}`, `src/noggit/ChunkWater.{hpp,cpp}`, `src/noggit/ui/Water.{h,cpp}`, `src/noggit/tools/WaterTool.cpp`, `src/noggit/ui/windows/projectSelection/**`, `dist/noggit-themes/CrimsonSlate/theme.qss` -- **three features: missing placements become visible and fixable, liquid gets terrain-style height brushes, and the launcher takes the crimson brand.** **MISSING PLACEMENTS. The defect was worse than reported.** A model or WMO whose file cannot be found was not merely invisible -- `ModelInstance::intersect` and `WMOInstance::intersect` returned early, `recalcExtents` collapsed the bounding box to zero volume, and `World::select_objects_in_area` skipped anything with `loading_failed()`. So a broken placement could not be clicked OR marquee-selected, which means it could not be deleted or repointed either; the only signal in the entire program was a save-time modal telling the user to go and read `log.txt`. A missing TEXTURE already fell back to `shanecube.blp`, which is why models felt like the gap. Now: `AsyncObject::error_on_loading` -- the one place any asset failure is decided -- records into a process-global registry, and the viewport draws a checkered placeholder cube sized and coloured by kind (M2 magenta at 2.0yd, WMO warn at 8.0yd, WMO-internal doodad info at 1.0yd, selected flat accent), distinguished by CELL PATTERN as well as hue because this design system does not let a state ride on hue alone. Against the ink cell those measure 6.145, 6.779 and 8.043 to 1 -- not a WCAG claim, since this is a 3D viewport rather than body text, but the check that a checker still reads as a checker when small. Crucially the placeholder carries a REAL extent, so picking, frustum culling and screen-space projection all work again and the placement can finally be selected and fixed. `FakeGeometry` was studied and deliberately not reused: it is built inside the SUCCESSFUL-load path from `bounding_box_min/max`, which are never written for a failed model, it draws through a renderer that returns early on `loading_failed()`, and it already means \"loaded but no render passes\" -- overloading it would make two problems with opposite fixes indistinguishable. A dockable Missing Objects panel lists file, kind, owner UID, world XYZ and ADT tile, groups by file because one missing model with two hundred placements is ONE fix and not two hundred, and double-click moves the camera to the placement. **Threading, chosen deliberately.** Failures are detected on `AsyncLoader` worker threads. Workers touch only an STL registry under its own mutex; the panel polls a generation counter. A queued signal was rejected on lifetime grounds: `AsyncLoader` is a process-global whose threads outlive `MapView`, which `deleteLater`s its docks, so any `QObject` target a worker held could dangle -- precisely the intermittent, unattributable crash this kind of code is notorious for. Staleness is bounded by one poll; crash risk is zero by construction because the type a worker calls cannot reach a `QObject`. **LIQUID HEIGHT BRUSHES, with no data-model change.** Per-vertex heights already existed (`std::array<liquid_vertex, 9*9>`, height in `position.y`), both the MH2O and MCLQ paths already round-tripped them, and `liquid_vert.glsl` already fetched per vertex -- so raise, lower, flatten and smooth are brush logic only and nothing reaches `liquid_layer::save`. Raise/lower calls `MapChunk::changeTerrainProcessVertex` THROUGH the `MapChunk*` the caller already holds rather than copying it, so the curve is bit-identical to the terrain tool's instead of a second implementation that drifts. Undo is per chunk per stroke via the existing `registerChunkLiquidChange`. **The seam claim in the audit was disproved by measurement rather than accepted.** The audit said shared border vertices land on identical floats so no stitching is needed. Evaluating every chunk base of a 64x64 map in float32, **341 of 1024** column borders have the two owners' stored x differing by up to **0.00390625 yd** -- one ULP at x around 32900 -- because `xbase + CHUNKSIZE*k + 8*UNITSIZE` and `xbase + CHUNKSIZE*(k+1)` round differently. That is enough to put a rim vertex inside the radius for one owner and outside for the other, and with a flat falloff the tear is the full stroke strength. Border vertices are therefore keyed on a canonical integer, and every layer of a chunk is fanned over rather than a layer index, since nothing in the format ties chunk N layer 0 to chunk N+1 layer 0. **THE LAUNCHER** takes the supplied mockup: banner with the potion mark and a two-line letterspaced wordmark, sectioned columns with red ticks, project cards carrying generated per-project artwork, and a crimson primary action with an outer glow. Crimson is a BRAND surface here and is scoped by object name so it cannot leak: the working UI, all 34 generated icons, `DesignTokens.hpp` and the QPalette stay gold. Letterspacing comes from `QFont::setLetterSpacing` because QSS supports neither `letter-spacing` nor `text-transform`; the glow comes from `QGraphicsDropShadowEffect`, and it was verified on the real widget that the effect does NOT suppress the style-sheet background in this Qt, so no wrapper container was needed. **A SECOND FALSE CLIPPING REPORT, recorded because it is the same trap twice.** The verification stream reported the launcher opening with its action column cropped and derived a devicePixelRatio of 4/3 from `GetWindowRect`. Qt itself reports **dpr 2**: the window is 1180 logical, the Create button spans x 606..906, and `QWidget::grab` returns 2360x1520 showing the column complete. `GetWindowRect` called from a DPI-UNAWARE process returns virtualised coordinates, so a capture built from it reads the window's top-left corner as the whole window. Nothing was cropped and nothing was changed for it. **Evidence.** CMake reconfigure was required because `collect_files` globs at configure time; all five new translation units then appear exactly once in the project. The build links to `H:\NoggitUpdate\build\bin\RelWithDebInfo\noggit.exe` with 0 errors and 0 warnings, `check_qss.py` reports 0 errors and 0 warnings, the executable runs to the project-selection window with no error line logged, every new file is tracked, and every diff in this change set is content-only with no line-ending flips. |
| 2026-08-27 | `src/noggit/ui/tools/ChunkManipulator/**` (ChunkData.hpp, ChunkGridTransform.{hpp,cpp}, ChunkPack.{hpp,cpp} new; ChunkClipboard and ChunkManipulatorPanel rewritten), `src/noggit/tools/ChunkTool.{hpp,cpp}`, `src/noggit/MapView.cpp`, `src/noggit/ui/GroundEffectsTool.*`, `src/noggit/ui/GroundEffectSetEditor.*`, `src/noggit/DBCFile.cpp`, `include/utf8.h` (deleted), `src/noggit/data/_Data.h` and `src/noggit/simulation/_Simulation.h` (deleted), `docs/upstream/**` (moved), `.gitignore` -- **the chunk manipulator becomes a working tool, ground effects gets an overhaul, DBC writes stop being able to destroy the file they are replacing, and the repository root is tidied.** **THE CHUNK MANIPULATOR WAS A SCAFFOLD.** `ChunkClipboard` compiled but was never instantiated anywhere in the tree, `copySelected` built a cache on the stack and dropped it, `pasteSelection` had an empty body, and the panel constructor was empty -- nothing could be copied and nothing could be pasted. It now copies thirteen data classes, pastes them as ONE undo step, sews height seams, transforms, and carries a portable pack. **Cross-window paste**, the feature the competing tool's author called a godsend, works by publishing every copy to a versioned pack in the temporary directory, written to a sibling and renamed so a reader never sees a partial file, and picked up by a second editor through a file watcher PLUS an mtime poll -- the watcher alone loses its subscription when the path is replaced by rename rather than modified. The pack is magic-stamped, version-refused, length-checked exactly (a short file and an appended one are both rejected), FNV-1a hashed, read through a bounds-checked cursor with per-field sanity caps, and decoded into a local so a refused pack leaves the clipboard byte-identical. Textures travel as PATH STRINGS because a destination map does not share the source's texture index table. **Four hazards found by the streams that the audit had not identified.** A `memcpy` of `sizeof(layer_info)*4` into an `ENTRY_MCLY[4]` -- 8 bytes against 16 -- wrote source `flags` where `textureID` is read and left two layers uninitialised, and that field is the only carrier of ground-effect ids. `selectRange` returned `true` from a `for_all_chunks_in_range` lambda, which `World.inl:121-125` answers with `setChanged`, so DRAGGING A SELECTION marked every ADT it crossed as having unsaved changes. `has_mccv` cannot be copied as a flag on its own, because `MapChunk::save` gates the MCCV block on the runtime `hasMCCV` that a copied header word cannot reach, so copying Chunk flags without Vertex colours produced a chunk whose halves disagreed, invisibly until save. And raising `nTextures` without matching alphamaps dereferences `alphamaps[k-1]` unconditionally in `apply_alpha_changes` -- a null dereference a hand-edited pack could reach, now guarded by measuring the layer count back off the alphamaps that actually exist. **Seam sewing is symmetric.** `fixGapLeft`/`fixGapAbove` overwrite one chunk's edge with its neighbour's in a single direction, so over a pasted block iteration order decides which side wins; instead every copy of a shared border vertex is gathered by its position on the map-wide vertex grid and the MEAN is written back to all of them, which is the only way a corner vertex with four copies ends up consistent. **DBC WRITES WERE UNSAFE FOR EVERY CALLER.** `DBCFile::save` opened the real file with `ios_base::trunc`, destroying it before a byte of the replacement was written, so a full disk or an antivirus lock left a truncated DBC and no way back -- and the ground effect editor writes TWO DBCs in succession, so a failure on the second used to leave the first already replaced. It now writes a sibling and renames, keeps a `.bak`, and checks the stream AFTER `close()` because a full disk is usually reported at the final flush. It stays void and non-throwing on purpose: of its ten callers, in `Sky.cpp`, `MapCreationWizard`, `AreaTriggerTool` and `MapView`, almost none catch anything, so turning a silent partial write into an uncaught exception would trade one failure for a worse one. What changed is that a failure now leaves the ORIGINAL INTACT. A heap over-read in `DBCFile::removeRecord` was also fixed; it was reported as unreachable and is not -- deleting an area trigger or deleting a map in the Map Creation Wizard both run it. **The paste action is now RAII-closed.** Roughly five hundred lines separate `beginAction` from `endAction`, and the realistic throw is `std::bad_alloc` from the per-chunk `Alphamap` and 64 KiB alpha buffer a large paste allocates. An escaping exception did not merely lose the paste: `NOGGIT_CUR_ACTION` stayed set for the rest of the session, and since paste tests that global itself, paste then silently refused every later attempt until the editor was restarted. **REPOSITORY TIDYING, all verified inert before removal.** `include/utf8.h` had NO consumer anywhere -- the only `utf8` matches in `src/` are unrelated local variables -- so it is deleted and its UTF8-CPP row retired from the third-party licence table above, with the removal recorded there rather than silently dropped, because removing an unused bundled component reduces the licence surface and that should be auditable. `src/noggit/data/_Data.h` and `src/noggit/simulation/_Simulation.h` were 0-line placeholder comments in otherwise-empty directories; `collect_files` globs `*.cpp` only and nothing referenced either path. `todo_1.4.md` and `uid_fix_concept.md`, upstream working notes referenced by nothing, moved to `docs/upstream/`, and their now-stale root un-ignores removed from `.gitignore` -- which matters in a repository that ignores everything and allowlists explicitly, since a file moved into a non-allowlisted path becomes invisible to `git add`. **A LARGER `src/` REORGANISATION WAS CONSIDERED AND DELIBERATELY NOT DONE.** Grouping the flat files under `src/noggit/` into subdirectories is mechanical and the build would catch mistakes, but this is a FORK that tracks upstream, and moving files wholesale converts every future upstream merge into a hand-resolved conflict, permanently, in exchange for tidiness -- while this very file commits the project to logging its modifications against upstream. The layout stays aligned with `Marlamin/noggit-red`. **Evidence.** A CMake reconfigure was required because `collect_files` globs at configure time; the build then links clean on the first attempt with 0 errors, `check_qss.py` reports 0 errors and 0 warnings, the executable runs to the launcher with no error line logged, no source file is untracked, both submodule pointers remain at their public commits, and every diff is content-only -- the one file whose numstat differs under `--ignore-cr-at-eol` is fully CRLF at HEAD and fully CRLF now, the difference being a trailing newline the rewrite added. **KNOWN AND NOT FIXED:** paste applies with no confirmation regardless of clipboard size, unlike the set editor which confirms above a threshold -- left alone because a paste is a single undo step and a modal on the primary hotkey path costs more than it saves; and undoing a bulk object replace restores WMOs with their default doodad set, because `ObjectInstanceCache` carries only file key, type, position, rotation and scale, which is a pre-existing `Action` limitation this feature makes routine rather than introduces. |
| 2026-08-27 | `src/noggit/application/ApplicationEntry.cpp`, `media/banner_backdrop.jpg` (new), `resources/resources.qrc`, `src/noggit/ui/windows/noggitWindow/**` (MapSelectionArt.{hpp,cpp} new; NoggitWindow and MapListItem relaid out), `src/noggit/ui/windows/projectSelection/widgets/{BrandBanner,LauncherArt}.{hpp,cpp}`, `src/noggit/ui/FontAwesome.*`, `dist/noggit-themes/CrimsonSlate/{theme.qss,generate_icons.py,images/**}`, `src/noggit/ui/tools/LightEditor/**` (LightBrowser.{hpp,cpp} new), `src/noggit/Sky.{h,cpp}`, `src/noggit/MapView.{cpp,h}` -- **the application stops rendering every icon at a quarter of the available resolution, the map-selection window is rebuilt to the supplied mockup, and lights become selectable objects.** **THE BLUR HAD ONE CAUSE AND IT WAS A MISSING LINE.** `Qt::AA_EnableHighDpiScaling` was set but `Qt::AA_UseHighDpiPixmaps` WAS NOT SET ANYWHERE IN THE TREE. With scaling on and a measured devicePixelRatio of 2, Qt lays out in logical pixels and rasterises TEXT straight to the device surface -- which is why text stayed sharp -- but `QIcon::pixmap()` returned a ratio-1 bitmap that the compositor then stretched over twice the area in each direction. Every toolbar glyph, window icon and QIcon in the application was drawn with a QUARTER of the pixels of the screen it was drawn on. The attribute must be set before the `QApplication` is constructed, since Qt reads these during construction and ignores later changes. **Setting it then broke three sites, which is recorded because it is instructive rather than embarrassing.** `BrandBanner.cpp`, `MapSelectionArt.cpp` and `MapListItem.cpp` had each been written for the pre-flag world: they requested `size * ratio` and then called `setDevicePixelRatio(ratio)`, which was correct when the flag was off and applies the ratio TWICE now that it is on. The launcher potion mark rendered at 128 logical against an intended 96 and ran the full height of the banner band into the crimson rule. All three now derive the ratio from the pixmap that actually came back -- logical width is `width() / (width() / target)`, exactly `target` by construction -- which is correct with the flag on or off and whatever size the icon engine chooses to return. `BrandBanner`'s cache guard was also comparing a device width against `mark_extent * ratio` while the engine returns its own 256px source, so it was true on every paint and the mark was rebuilt from the QIcon on EVERY paintEvent. A comment at the pin-star site asserting that `AA_UseHighDpiPixmaps` \"only reaches the QWindow overload\" was the reason that site double-scaled; the `QSize` overload forwards to `pixmap(0, size)` and applies the ratio like every other, so the claim is corrected in place rather than deleted. **THE BANNER CARRIES SUPPLIED ARTWORK.** The source was 5504x3072 at 6.7 MB; `media/banner_backdrop.jpg` is a 2880x480 band at 131 KB, sized so it lands at scale factor exactly 1.000000 on a 1440-logical banner at ratio 2 -- no resampling at all -- and it is scaled at DEVICE resolution with `KeepAspectRatioByExpanding` before the ratio is stamped, because scaling at logical size and letting `drawPixmap` magnify would discard three quarters of the pixels. The artwork alone is unusable for text: its brightest pixel is #FF9F7A, on which text.hi measures 1.759:1. A veil plus a held scrim carry the wordmark to **17.437:1 at worst-dark and 16.260:1 at worst-bright**, with the brand crimson at 4.932 and 4.600 -- and the honest limit is stated in the code rather than glossed: past the scrim the veil alone gives 7.159:1 at 1180 wide and 6.307:1 at 800, under the 7:1 prose floor, accepted only because no text is drawn there. If Qt's `qjpeg` plugin is missing from a stripped deployment the decode returns null and the banner falls back to a band gradient, so the picture is lost and the wordmark stays legible. **THE MAP-SELECTION WINDOW** is rebuilt as two panes to the mockup: a header strip with the mark and a compact wordmark, tabbed Maps and Bookmarks, a labelled filter form, map CARDS with a circular emblem, id subtitle, type badge and chevron, a red-thumbed scrollbar, a full-width ADD NEW MAP action, and a right pane with Enter/Edit tabs, an empty state and the existing minimap preview. It is a RE-SKIN: the map list, bookmarks, pinning, the minimap and its double-click-to-enter all keep their existing behaviour, and **nothing dead was shipped** -- the Type and Expansion combos in the mockup turned out to be already wired and persisted to QSettings, so they are live rather than omitted. Two pre-existing quirks in `applyFilterSearch` were deliberately left: the Type combo's \"Unknown\" entry maps to a value that matches nothing, and the WMO checkbox is an exclusive toggle rather than an additive one. Seven marks Qt could not supply are painted rather than faked: the shipped icon set has `chevrondown`/`chevronup` and a filled `caretright` but no magnifier, map pin or chevron-right, and the only other source is a Font Awesome font this fork does not redistribute. **LIGHTS** gain viewport picking, gizmo interaction and a cross-map browser, following the parallel path database spawns already use rather than being added to `selection_type` -- a third variant alternative would touch every `std::get` and `.index()` site in the codebase and buy nothing. `Sky::_selected` had been declared, initialised and read but NEVER WRITTEN anywhere, so the plumbing had been started and abandoned. A `LightSnapshot` forward declaration was added to `LightEditor.hpp`, which used the type without including `Sky.h`; both uses are parameters in a declaration, so a forward declaration is sufficient and matches the house rule to forward-declare in headers. **Evidence.** One build failure, fixed: the new `resources.qrc` comment used this tree's `--` em-dash convention inside an XML comment, where `--` is illegal per XML 1.0 section 2.5. The build then links clean, `check_qss.py` reports 0 errors and 0 warnings, the executable runs with no error logged, nothing is untracked, and every diff is content-only with no line-ending flips. The launcher was captured through `QWidget::grab` at 2360x1520 -- Qt's own 2 x 1180 x 760 -- because a DPI-UNAWARE capture process reports 1588x1051 for the same window and yields the corner-crop artefact this file has already recorded twice. |
| 2026-08-27 | `dist/noggit-themes/CrimsonSlate/theme.qss`, `src/noggit/MapView.{cpp,h}`, `src/noggit/ui/tools/ToolPanel/ToolPanel.cpp`, `src/noggit/ui/tools/UiCommon/ExtendedSlider.cpp`, `src/noggit/ui/tools/ViewToolbar/Ui/ViewToolbar.cpp`, `src/noggit/ui/windows/projectSelection/widgets/ProjectListItem.cpp` -- **the editor chrome joins the design language the launcher and map window established, and a hover state that had shipped but could never run is made to work.** **THE HOVER BUG IS THE INSTRUCTIVE ONE.** The launcher's project card carried a `:hover` rule in the sheet and it had never once fired, because a plain `QWidget` does not track the pointer: without `Qt::WA_Hover` there is no enter/leave repaint for `QStyleSheetStyle` to re-evaluate. Every other control in that window hovers correctly because `QAbstractButton` sets the attribute for itself, so the one hand-built widget was the one place it was missing. Even once firing it would have been nearly invisible: the hover fill `#34312C` against the resting `#292621` measures **1.164:1**, under the ~1.28 this palette treats as the smallest step a large flat field can carry -- the same threshold that made the pre-gold palette read as one surface wearing four names. It is now bg.raised `#3C3732` at **1.281:1**, with the border carrying **5.922:1** against the fill. Selection needed nothing: it already fires on `currentItemChanged`, which is the crimson state the user could see. **THE EDITOR WAS NOT UNSTYLED -- IT WAS STYLED TO THE PREVIOUS REVISION OF THE DESIGN LANGUAGE.** The two brand windows had moved to a third idiom (a bg.void ground carrying bg.panel cards, with 11/700 letterspaced-capital headings preceded by a crimson tick) while the editor stayed on the second (a bg.panel ground with bare full-width hairlines under 13/700 text.hi headings). The consequence was measurable rather than aesthetic: the editor's section furniture was two type ranks larger, one rank brighter, and its dividing rule carried **1.66x** the contrast of the reference's, so the working window shouted its structure louder than the brand windows while conveying less. The tool dock now drops to a bg.void ground with sections as bg.panel cards, matching the move both brand windows already made. **GOLD REMAINS THE INTERACTIVE ACCENT AND THAT IS A CORRECTNESS CONSTRAINT, NOT A PREFERENCE.** Crimson appears in exactly two new places, both `border-left` on base selectors with NO pseudo-state -- static brand ticks. Repainting state crimson would recreate the defect this project already fixed once, where accent `#E8543F` and error `#E0574B` measured 1.024:1 apart and 4.0 degrees in hue and red therefore meant selected, focused, checked, active, pressed, danger, error and delete at the same time. Gold against `bad` is **34.5 degrees and 1.389:1**; crimson against `bad` is **16.0 degrees**, half the separation. The dock title rule stays gold for the same reason: `MapView.cpp:328` retitles that dock from the active tool on every mode change, so it is literally the answer to \"which tool am I in\" -- a state, not a brand surface. **DEAD WEIGHT FOUND AND REMOVED.** `qtadvanceddocking` is linked and built but **never instantiated** -- there is not one `DockManager` or `ads::` reference outside `src/external`, and every dock in the editor is a plain `QDockWidget`. The sheet's ~96-line `ads--*` block was therefore unreachable, including a tab rule carrying a heading rank nothing would ever read. An `expanderwidget` `:checked` rule was equally unreachable, its `setCheckable(true)` having been commented out at the call site. Both are deleted rather than maintained. `#editor-menu-bar` was minted in C++ for a sheet rule that was never written; the eight `status-*` object names were in the same state -- the C++ half of the status-bar ranking pass had landed and its QSS half never had, so seven readouts still carried an identical leading rule including the leftmost, which lands against the window edge. Those hooks are now used. **Evidence.** The build links clean on the first attempt with zero errors. `check_qss.py` reports 0 errors; it reports 4 warnings ONLY when run with `--moc`, and running the same command against `git show HEAD:...theme.qss` produces the identical four selectors with only line numbers shifted, so they are pre-existing and not introduced -- the earlier \"0 warnings\" in this file was a checker run without that flag. Every one of the 25 numeric claims in the added lines was recomputed and all 25 are exact; one comment that read correctly as a border's own resting-to-hover step but scanned as border-against-fill was rewritten to state both. All 14 new id selectors were resolved against real `setObjectName` sites, which the checker does not validate. The executable runs and **stderr is completely empty**, which is the meaningful negative for a 397-line style-sheet change, since Qt prints parse failures and unknown properties to stderr and otherwise discards them in silence. **WHAT IS NOT COVERED, stated plainly:** only the launcher path was exercised. The config was created fresh, so no project was open and `MapView`, the tool dock and the view toolbar were never rendered. Every editor-chrome change in this row is verified STATICALLY -- by selector resolution, specificity arithmetic and recomputed contrast -- and not by looking at it. The user's own project was deliberately not opened to obtain a screenshot. |
| 2026-08-28 | `dist/noggit-themes/CrimsonSlate/**` (theme.qss, generate_icons.py, nodes_theme.json, 12 regenerated icon PNGs), `src/noggit/ui/DesignTokens.hpp`, `src/noggit/application/ApplicationEntry.cpp`, `src/noggit/ui/tools/UiCommon/ExtendedSlider*`, `src/noggit/ui/texturing_tool.cpp`, `src/noggit/ui/windows/projectSelection/widgets/ProjectListItem.*`, `src/noggit/ui/windows/noggitWindow/widgets/MapListItem.cpp`, `src/noggit/rendering/PlaceholderCube.cpp` -- **the interactive accent moves from gold to crimson across the whole application, the status ramp is re-solved so it does not collide, the sliders are rebuilt, and the project card becomes a real control.** **THE ACCENT DECISION WAS REVERSED BY THE USER AND THAT IS RECORDED HONESTLY.** Two previous rows argued for keeping gold, on the grounds that crimson-as-accent recreates a defect this project already fixed: the pre-gold palette had accent #E8543F and error #E0574B measuring 1.024:1 apart and 4.0 degrees in hue, so one red meant selected, focused, checked, active, pressed, danger, error and delete at once. The user asked three times for the editor to match the crimson identity, which is their call to make. The objection was therefore not ignored but SOLVED: `bad` moves #E86F62 -> **#EF752E** and `warn` #E2803C -> **#D8AB18**, putting accent-versus-bad at **32.25 degrees** and 1.390:1. Left alone it would have been **16.00 degrees** and the collision would have returned. Every warm pair widened; the tightest is now warn-versus-bad at 23.9 degrees against the gold sheet's tightest of 15.8, and that limit is stated in the sheet rather than hidden. Form still carries its share: a danger button is an edge until hovered, an invalid input takes a border AND a tint, a focused input takes a border and never a tint. **ONE CRIMSON CANNOT DO GOLD'S JOB, and the sheet now says so mechanically.** Gold sat at luminance 0.4280 and cleared the 3:1 mark floor on the entire surface ladder; crimson #E5405C sits at 0.2110 and does not -- it is 2.925:1 on bg.raised and 2.329:1 on bg.overlay. The rule is therefore explicit: a mark takes plain accent only where both neighbours are bg.void, bg.alt or bg.panel, otherwise accent.hi, and EVERY focus ring is accent.hi without exception, because focus can coincide with hover and every hover face is one surface lighter. `accent.hi` is the launcher's #F05A73 lifted 1.106:1 to **#F3687F** for a measured reason: #F05A73 is 2.862:1 on bg.overlay and misses the floor by 4.6 percent. **INK ON THE ACCENT HAD TO BE RE-DECIDED SITE BY SITE**, because gold gave ink 8.772:1 and crimson gives 4.790:1. Rest and hover keep ink at 4.790 and 6.513; the PRESSED fill cannot -- ink clears 4.5:1 only above luminance 0.1952 and text.hi only below 0.1550, and accent.press #C32842 sits at 0.1351 -- so six pressed states switch to text.hi at 4.983:1. `QPalette::LinkVisited` moved to accent.hi, which is a repair: the old #B8801F was already 4.41:1, under the body floor. **THREE LIVE GOLD SURVIVORS THE MIGRATION MISSED, one of them invisible to the audit that looked for it.** `PlaceholderCube.cpp` wrote the retired accent and warn as DECIMAL literals -- 223.0f/255.0f and 226.0f/255.0f -- under a comment claiming they came from `Noggit::Ui::Design` while the file did not include it. Every grep in the migration searched for the hex strings and a hex grep cannot see a decimal, so this would have shipped the 3D viewport drawing a SELECTED object in retired gold while the rest of the application had gone crimson -- and the selected mark is the most accent-semantic thing the editor draws. It now reads the tokens through `Design::color` at first use, so the coupling is real rather than asserted. The two `ACCENT_GOLD` constants in `ProjectListItem` and `MapListItem` are `QGraphicsColorizeEffect` colours, unreachable from any style sheet, and both are now **accent.hi #F3687F** rather than plain accent -- measured, because a row lightens under the pointer and again on press, and plain accent is **2.947:1** on the selected-hover fill, under the 3:1 mark floor. accent.hi holds at 6.513 / 5.900 / 5.093 / 4.440 / 4.613 / 4.008 and 3.714 at its worst. `MapListItem`'s comment had additionally INVERTED: it justified moving the star by saying it must not be left behind \"while everything around it goes gold\". **A FALSE FIGURE CAME BACK FOR A SECOND TIME AND IS NOW RECORDED RATHER THAN JUST DELETED.** The sheet stated that under the gold pen the unchecked-to-checked step was \"1.106:1 and 105 degrees\". The contrast is right; the hue is **3.2 degrees** -- gold #DFA52E and text.dim #BFB7AA are almost the same hue, which is precisely why the gold pen could not signal checked by hue at all. That same 105 was retired from `FontAwesome.hpp` earlier in this same session and reappeared here, so the correction now names it as a repeat so it cannot return a third time. **THE PROJECT CARD IS A REAL CONTROL, and this took three attempts because the first two were built on a false premise.** QSS `:hover` DOES NOT FIRE on that widget even with `Qt::WA_Hover` set -- established by launching the application, driving the real cursor onto the card with `SetCursorPos` from a DPI-AWARE process and sampling the border from a `PrintWindow` capture, which stayed at the non-hover value while the `[state=\"selected\"]` rule on the SAME widget worked. The widget is installed through `setItemWidget`, i.e. as a persistent editor. Interaction is therefore driven from C++, and the result is measured on the shipped binary rather than asserted: at rest the ring is #E5405C two device pixels wide on a #3C2927 fill, and hovered it is **#F05A73 four pixels wide on #453331** -- colour, width and fill all responding. Enter and Leave DO reach a persistent editor; only the QSS pseudo-state does not. **Evidence.** A live-pixel census of both rendered windows counts **zero** pixels of #DFA52E, #F0BA4A or #B8801F and zero of the retired #E86F62 and #E2803C, against 21,575 accent pixels. The build links clean on the first attempt across a 1,586-insertion change set with no fixes required. `check_qss.py` reports 0 errors and 0 warnings plain, and 0 errors with 4 warnings under `--moc`; the same four type-selector warnings reproduce against `git show HEAD:` so they are pre-existing and unchanged. stderr is 0 bytes after opening a project and reaching the map window, which is the meaningful check for a 1,031-line sheet change since Qt reports style-sheet parse failures there and otherwise discards them silently. No range, step, decimal, signal or connect changed in either slider -- verified by filtered diff, not by trust. **KNOWN DOCUMENTATION DEBT, listed so it is not rediscovered as a defect:** five comments still quote the retired gold figures although the code beneath each consumes `Design::` tokens and is correct -- `CurrentTexture.h:42`, `FontAwesome.hpp:1175` and `:1247`, `minimap_widget.cpp:431`, and `theme.qss:3633`. |
| 2026-08-28 | `README.md`, `docs/setup.md`, `patches/README.md`, `src/noggit/application/NoggitApplication.cpp`, `src/noggit/application/Configuration/NoggitApplicationConfigurationReader.cpp`, `src/noggit/ui/UidFixWindow.cpp`, `src/noggit/ui/uid_fix_mode.hpp`, `src/noggit/MapView.cpp`, `src/noggit/Selection.cpp`, `tests/CMakeLists.txt`, and 18 files moved into `src/noggit/{terrain,assets,reports}/` -- **a release-readiness pass: a fresh clone was proved to build, two documented commands that would have broken a user were corrected, a config parse bug was found, and the fork's own loose sources were grouped.** **A CLEAN CLONE BUILDS AND RUNS, verified end to end for the first time.** Cloned to scratch, `git submodule update --init --recursive` exit 0 with all six submodules resolving, both patches applying, configure and build exit 0, and the executable launching. Crucially there are **zero compiled-but-untracked files** -- the allowlist `.gitignore` has produced that failure three times before and did not this time. **TWO DOCUMENTED COMMANDS WOULD HAVE BROKEN THE PERSON WHO FOLLOWED THEM.** `README.md` carried upstream's `git submodule update --recursive --remote` with no fork note. In this fork that command is DESTRUCTIVE: `--remote` ignores the recorded commit and takes each submodule's remote tip, silently discarding both applied patches -- including `0001`, without which Client > Patch Client writes MPQ archives the game ignores with no error anywhere. It exits 0 and prints reassuring \"checked out\" lines while doing it. Measured in a throwaway clone: patch presence went 1 -> 0 for both submodules. Separately, `patches/README.md`'s own release checklist -- the one written to PREVENT a broken pointer -- told a maintainer that BOTH submodules should print `6cac330`. They are different repositories: `cmake` is `T1ti/build-dependencies` and correctly records `7a5741a`, and `6cac330` does not exist in it at all. A maintainer following it literally would have concluded the pointer was re-recorded by accident and \"fixed\" it to a commit on no remote, breaking `git submodule update --init --recursive` for every cloner. Both are corrected, the second with a per-path table so one SHA can never again be read as covering two repositories. **A DANGLING `else` HAD SILENTLY DISABLED A CONFIGURATION FIELD.** In `NoggitApplicationConfigurationReader.cpp` an `else` with no body sat between two `if`s, so the line parsed as `else if (contains(\"ApplicationListFilePath\"))` and the listfile path was read ONLY when `ApplicationNoggitDefinitionsPath` was absent -- which it never is in a file this application writes. Every normal run left the field an empty string. It survived unnoticed because its only consumer was a log line that announced `Listfile found! : ` UNCONDITIONALLY, with its failure branch commented out, and printed the empty value after the colon. So the log positively asserted the thing that was wrong, on the very line `docs/setup.md` tells a user with no window to read first because it \"names the missing piece\". Parse and log are the same defect from two ends; both are fixed, and the log now reads `Listfile found! : listfile.csv`. A second line saying \"Configuration File Loaded! Creating New File\" -- which says the opposite of what it does -- was corrected in the same place. **PROFANITY AND DEBUG OUTPUT REMOVED FROM SHIPPED UI.** `UidFixWindow` carried a reachable button labelled with an obscenity and an explanatory paragraph containing another; both are rewritten to say what the option actually does, and the underlying `uid_fix_mode` enumerator was renamed with it -- renaming only the local variable would not have compiled, which is how the enumerator was found. `Selection.cpp` was writing `DEBUG Chunk Unit texture weights:` into the HTML panel a user reads to identify what they clicked. **THE FORK'S OWN LOOSE SOURCES ARE NOW GROUPED, and the deferral in the row above is superseded on evidence rather than reversed on preference.** That row declined a `src/` reorganisation because this is a fork tracking upstream and moving files makes every future merge a hand-resolved conflict. That argument is entirely about UPSTREAM files, and measuring against the fork base `6f0776d4` shows the split is clean: of the **115** files loose in `src/noggit/`, **97 are upstream** (34 of them already modified here, so already conflict-prone) and **18 were written by this fork**. Upstream has never seen `AssetScan.hpp` and never will, so moving it costs **zero** future conflicts. Those 18 went to `terrain/` (AlphaIntegrity, AmbientOcclusion, ErosionKernel, TerrainRules), `assets/` (AssetDependencies, AssetScan, PatchAssetPacker) and `reports/` (MissingPlacementLog, UidCollisionLog), taking the loose count to **97** and leaving the tree stating a rule about itself: loose in `src/noggit/` means upstream, a feature-named subdirectory means this fork wrote it. No upstream file was moved, and the 18 fork-authored loose files under `src/noggit/ui/` were deliberately left -- moving them would take that directory from 85 loose to 67 for the same churn, which does not pay. **THE ONE THING THE BUILD WOULD NOT HAVE CAUGHT WAS HANDLED FIRST.** `tests/` is a standalone CMake project outside `ALL_BUILD` whose source list is hand-maintained with literal paths, and it feeds them through `noggit_add_if_present`, which on a missing path prints `skipping absent source` and **configures successfully**. Moving the files while updating only the application would therefore have produced a green app, a stale test binary still reporting its old pass count, and a test project that no longer compiled the moved sources -- precisely the class of silent breakage this repository has shipped before. Seven literal paths were updated in the same change, the test project reconfigured with **no** `skipping absent source` line, and the suite rebuilt and run: **35/35 passed, 0 failed**, with the one DB-gated integration test skipped as documented. **Evidence.** Application build links clean; `check_qss.py` 0 errors and 0 warnings; the executable runs with no error line logged; nothing untracked; every diff content-only; all 18 moves recorded by git as renames; the three new directories verified visible to the allowlist `.gitignore` before the move; and both submodule pointers still at their public commits. **KNOWN AND NOT FIXED -- these are release decisions for the maintainer, not defects to hide:** `src/external/glm` is 420 tracked files with **no licence grant of any kind** in the tree, which is a genuine legal blocker for publication; glm is dual-licensed (Happy Bunny / MIT) but the correct text was deliberately NOT written from memory, because an invented licence file is worse than a missing one -- `copying.txt` from the matching glm 0.9.9 release must be committed. A local branch `backup-before-rewrite` still carries the 16 trailers an earlier session removed from history; a normal `git push` is unaffected, but `--all` or `--mirror` would publish them. Nine `media/project-icon-*.png` are the World of Warcraft logo mark and are compiled into the binary. And the ADT save path has no atomicity, no backup and can fail silently -- the DBC path was given temp+rename+`.bak` protection in an earlier row and the ADT path has no equivalent, which is the largest remaining risk to a user's work. |

Keep this table current. When application code lands under `src/`, every new file needs the
project's GPL header, matching the 337 existing files that carry it:

```cpp
// This file is part of Noggit3, licensed under GNU General Public License (version 3).
```

## Upstream authorship

Noggit predates this repository — the source headers refer to **Noggit3**, and the lineage runs
back through earlier Noggit and Noggit SDL releases by authors not represented in this repo's
git history. Credit the project, not just the commit log.

`noggit-red` itself spans 1441 commits from 2020-10-09 to 2025-01-17. Principal contributors by
commit count:

> T1ti · Skarn · sshumakov3 · Alister · Martin Benjamins · Balkron · p620 · EIntemporel ·
> Kaev · ihm-tswow · Intemporel · BalkronPainter · Felfired · Havric · DennisWG · Varen ·
> BinarySpace

Regenerate with `git shortlog -sn 6f0776d4`. Before publishing, check upstream for an
`AUTHORS`/`CONTRIBUTORS` file added after the fork point and prefer it over this list.

## Bundled third-party code

Everything under `src/external/` is third-party and carries its own terms, and so is
`include/win/`. `src/External` is explicitly exempt from this project's coding rules per the
README.

**Removed since the last audit.** `include/utf8.h` (UTF8-CPP, Boost Software License 1.0) was
deleted on 2026-08-27. It had **no consumer anywhere in the tree** -- the only `utf8` matches in
`src/` are unrelated local variables in `ChangesetArchive.cpp`, `GameTeleBuilder.cpp` and
`LauncherArt.cpp` -- so it was carried, and had to be complied with, for nothing. Removing an
unused bundled component reduces the licence surface rather than enlarging it; it is recorded
here so the audit trail shows the row was retired deliberately and not lost.

**Scope and method of this audit.** Every entry below was determined *from the files present in
this working tree* — a `LICENSE`/`COPYING` file, or a licence grant in the component's own
sources — and cites the path that proves it. Nothing here was inferred from a library's
reputation or looked up in an upstream repository. Where the tree does not state the terms, the
row says **UNRESOLVED** rather than guessing. Audited 2026-07-31 against the tree as it stands;
re-run it whenever a submodule pointer or a vendored copy moves.

"GPL-3.0 compatible" means the component may be combined and redistributed as part of a GPL-3.0
work. Permissive terms (MIT, BSD, zlib, libpng, Boost) are. LGPL-2.1 is, via LGPL-2.1 §3, which
permits relicensing a copy under GPL v2 *or any later version*. LGPL-3.0 is, being GPLv3 with
added permissions. GPL-3.0-or-later is. **GPL-2.0-only would not be** — nothing found in this
tree carries GPL-2.0-only terms.

### Vendored under `src/external/`

| Component | Location in tree | Licence | Evidence in tree | GPL-3.0 compatible |
|---|---|---|---|---|
| framelesshelper | `src/external/framelesshelper` | MIT | `framelesshelper/LICENSE` | Yes |
| imguizmo | `src/external/imguizmo` | MIT | `imguizmo/LICENSE` | Yes |
| rapidfuzz-cpp | `src/external/rapidfuzz-cpp` | MIT | `rapidfuzz-cpp/LICENSE` | Yes |
| tsl (robin-map/robin-set) | `src/external/tsl` | MIT | Full MIT grant heading all four headers, e.g. `tsl/robin_map.h:1-23` | Yes |
| Dear ImGui | `src/external/qtimgui/imgui` | MIT | `qtimgui/imgui/LICENSE.txt` | Yes |
| NodeEditor | `src/external/NodeEditor` | BSD-3-Clause | `NodeEditor/LICENSE` | Yes — requires Qt5 ≥ 5.10 |
| tracy | `src/external/tracy` | BSD-3-Clause | `tracy/LICENSE` (states 3-clause BSD on its face) | Yes |
| libnoise | `src/external/libnoise` | LGPL-2.1 | `libnoise/LICENSE.md` | Yes (LGPL-2.1 §3) |
| QtAdvancedDockingSystem | `src/external/QtAdvancedDockingSystem` | LGPL-2.1 | `QtAdvancedDockingSystem/LICENSE`, `gnu-lgpl-v2.1.md` | Yes (LGPL-2.1 §3) |
| qt-color-widgets | `src/external/qt-color-widgets` | LGPL-3.0-or-later | `qt-color-widgets/COPYING`, plus a per-file grant in every source, e.g. `src/abstract_widget_list.cpp:1-22` | Yes |
| qtgradienteditor | `src/external/qtgradienteditor` | LGPL-2.1 with the Nokia Qt LGPL Exception 1.1 | Per-file Qt licence block, e.g. `qtgradienteditor/qtgradienteditor.cpp:1-41` | Yes (LGPL-2.1 §3) — see caveat below |
| **glm** | `src/external/glm` (v0.9.9.8, per `glm/detail/setup.hpp:11`) | **UNRESOLVED** | None. No `LICENSE`/`COPYING`/`copying.txt`/`manual.md`; no licence grant in any header. The only copyright notice in the whole directory is a Sun Microsystems permission notice in `glm/ext/scalar_ulp.inl:1-6`, which covers that one file. | **Unresolved** |
| **imguipiemenu** | `src/external/imguipiemenu` | **UNRESOLVED** | None. Two files (`PieMenu.hpp`, `PieMenu.cpp`), no licence file, no header, no attributed author. | **Unresolved** |
| **qtimgui** (the Qt wrapper itself, not the bundled Dear ImGui) | `src/external/qtimgui/{QtImGui,ImGuiRenderer}.{h,cpp}` | **UNRESOLVED** | None. `LICENSE.txt` exists only under `qtimgui/imgui/`; the wrapper sources carry no grant. | **Unresolved** |
| **PNG2BLP** (the top-level converter, not its bundled libraries) | `src/external/PNG2BLP/*.{h,cpp}` | **UNRESOLVED** | None. No licence file, no header on any of the ten top-level sources. | **Unresolved** |

### Third-party libraries nested inside PNG2BLP

`src/external/PNG2BLP` vendors five further libraries. These were not previously recorded at
all, and one of them is copyleft.

| Component | Location in tree | Licence | Evidence in tree | GPL-3.0 compatible |
|---|---|---|---|---|
| zlib | `PNG2BLP/zlib` (v1.2.11) | zlib | `zlib/zlib.h:1-21` | Yes |
| libpng | `PNG2BLP/libpng` | libpng licence | `libpng/LICENSE` | Yes |
| png++ | `PNG2BLP/pngpp` | BSD-3-Clause | `pngpp/COPYING`, `pngpp/AUTHORS` | Yes |
| libtxc_dxtn | `PNG2BLP/libtxc_dxtn` | MIT | `libtxc_dxtn/txc_dxtn.h:1-23` | Yes |
| **libimagequant** | `PNG2BLP/libimagequant` (v2.12.2) | **GPL-3.0-or-later** | `libimagequant/blur.c:1-18` states the GPLv3-or-later grant in full. `pam.h:1-14` adds a separate permissive notice from Jef Poskanzer / Greg Roelofs for that file. | Yes — but see below |

`libimagequant` being GPLv3-or-later is fine for this project (the whole work is GPL-3.0 already)
and it is *not* a blocker, but it is worth stating plainly: the built binary is unambiguously
copyleft, and the previous summary — which implied the bundle was permissive and LGPL — was
wrong about that.

### Third-party outside `src/external/`

| Component | Location in tree | Licence | Evidence in tree | GPL-3.0 compatible |
|---|---|---|---|---|
| **StackWalker** (Jochen Kalmbach) | `include/win/StackWalker.{h,cpp}` — compiled via `error_handling.cpp` | **UNRESOLVED** | None. No licence text in either file; the header records only a 2005 CodeProject release history. | **Unresolved** |

### Git submodules

These are referenced by commit, not copied into this repository, but a user who clones with
`--recursive` receives them and the built binary links them.

| Component | Location in tree | Licence | Evidence in tree | GPL-3.0 compatible |
|---|---|---|---|---|
| **blizzard-archive-library** | `src/external/blizzard-archive-library` (submodule) | **UNRESOLVED** | None. No `LICENSE`/`COPYING`; no grant in any source. `README.md` is a stale copy of the sibling library's, so even the identity of the project is unstated. | **Unresolved** |
| **blizzard-database-library** | `src/external/blizzard-database-library` (submodule) | **UNRESOLVED** for the library as a whole | None at top level. One nested component *is* stated: ByteStream (Pablo Albiol) is LGPL-3.0-or-later per `include/external/ByteStream.h:1-18` and `src/external/ByteStream.cpp:1-18`. That covers those two files only. | **Unresolved** |
| build-dependencies (`cmake/`) | `cmake` (submodule) | **UNRESOLVED** | Build scripts only; no licence file. Not linked into the binary. | **Unresolved** (low risk — build tooling) |
| `dist/definitions`, `dist/listfile`, `dist/themes` | submodules | **UNRESOLVED** | Data, not code. No licence file. | **Unresolved** (data, not linked) |

### Fetched at build time — not distributed by this repository

`FetchContent` pulls these during configure. Publishing this repository's *source* does not
distribute them; shipping a **built binary** does, and their obligations attach at that point.
None of them is present in this tree, so none can be audited from it.

| Component | Fetched from | Licence |
|---|---|---|
| StormLib (Ladislav Zezula) | `gitlab.com/prophecy-rp/dependencies.git` (`cmake/FindStormLib.cmake`) | Not determinable from this tree |
| CascLib (Ladislav Zezula) | `gitlab.com/prophecy-rp/dependencies.git` (`cmake/FindCascLib.cmake`) | Not determinable from this tree |
| Lua 5.x | `gitlab.com/prophecy-rp/dependencies.git` (`cmake/FindLua.cmake`) | Not determinable from this tree |
| sol2 | `github.com/tswow/sol2` (`cmake/FindSol2.cmake`) | Not determinable from this tree |
| FastNoise2 | `github.com/tswow/FastNoise2` (`cmake/FindFastNoise2.cmake`) | Not determinable from this tree |
| lodepng | `github.com/lvandeve/lodepng` (`cmake/Findlodepng.cmake`) | Not determinable from this tree |
| nlohmann/json | `github.com/ArthurSonzogni/nlohmann_json_cmake_fetchcontent` (`cmake/FindJson.cmake`) | Not determinable from this tree; declares `cmake_minimum_required` below 3.5 |
| Qt5 | supplied by the user | Not distributed here |

> [!warning] Unresolved before publishing
> Seven components carry **no licence statement anywhere in this tree**, so their terms are
> genuinely unknown here — not "probably MIT":
>
> `glm`, `imguipiemenu`, `qtimgui` (wrapper), `PNG2BLP` (top level), `StackWalker`,
> `blizzard-archive-library`, `blizzard-database-library`.
>
> All seven are compiled into `noggit.exe`. Every one is inherited from upstream rather than
> introduced by this fork, which does not make it someone else's problem once you publish.
> Each must be resolved against its own upstream and its licence text committed alongside it.
> Until then the safe reading is that this repository redistributes code whose terms it cannot
> evidence.
>
> Two further gaps are obligations rather than unknowns — the terms are stated, but the licence
> texts they point the reader at were not vendored:
> `libimagequant`'s sources say "See COPYRIGHT file for license" and no `COPYRIGHT` file exists;
> `qtgradienteditor`'s Qt block points at `LICENSE.LGPL` and `LGPL_EXCEPTION.txt`, neither of
> which is present. Copy both in.
>
> One wording caveat on `qtgradienteditor`: its 2009 Nokia header opens with "No Commercial
> Usage / This file contains pre-release code and may not be distributed." That sentence belongs
> to the Technology Preview option of Qt's three-way licence block, and the same block offers
> LGPL-2.1 as an explicit alternative ("Alternatively, this file may be used under the terms of
> the GNU Lesser General Public License version 2.1"). This project relies on the LGPL-2.1
> option. Anyone reading only the first lines will think otherwise, so do not delete this note.

## AtlasForge — independent reimplementation, no shared code

Some of the workflow design here is **inspired by** AtlasForge (RetroGooby), a closed-source
Windows editor for WoW 1.12.1: tile-centric editing, ghost placement with a staged pending-change
list, an explicit build/commit step, and an environment "Doctor".

**No AtlasForge code is included, and none could be.** It is distributed as a binary-only
release with no published source. Only observable UX concepts were reimplemented, independently,
against a different game version (3.3.5a) and a fundamentally different data layer — AtlasForge
builds client-side MPQ patches, whereas this writes server-side database changesets. There is no
analog to `patch-Z.MPQ` here.

If you describe the lineage publicly, describe it as conceptual inspiration. Do not imply
affiliation, endorsement, or shared implementation.

## TrinityCore — schema facts, not source code

`docs/schema-335.md` documents the TrinityCore 3.3.5 world database layout. **No TrinityCore
source code is copied into this project.** TrinityCore is GPL-2.0, which is not compatible with
GPL-3.0, so this distinction matters and is deliberate.

What the documentation contains is factual: column names, ordinal positions, types, and defaults
read from `information_schema` on a running database, plus semantic notes about which columns the
server derives rather than reads. Database column names are an interface. Where the core's own
`INSERT` column list is reproduced, it is reproduced as a factual list of identifiers, with its
location cited so a reader can verify it.

Keep it that way. Do not paste TrinityCore implementation code into this repository.

## Blizzard content — none, ever

No game client data is included or may be: no MPQ archives, no DBC/DB2 files, no models,
textures, maps, or extracted assets. Users supply their own legally obtained 3.3.5a client.

The synthetic fixtures in `tools/dev-db/02_seed_synthetic.sql` use real creature and gameobject
**display IDs**. These are integer keys into the client's DBC files, not content — they have to
be real for `displayId` → model resolution to be testable at all. Every accompanying value
(names, bounding radii, coordinates, entries, GUIDs) is invented, and all IDs sit far above the
ranges TDB uses.

## Not published — verify before every release

These are gitignored because they describe private infrastructure or hold secrets. Confirm none
are tracked before pushing:

| Path | Why |
|---|---|
| `docs/environment.md` | One machine's toolchain, database inventory, and security posture |
| `tools/dev-db/db-policy.json` | Names the operator's live databases |
| `tools/dev-db/dev-db.config.json` | Machine-specific paths and connection details |
| `tools/dev-db/.generated/**` | Generated credentials, filled bootstrap, structure dumps |
| `tools/dev-db/*.local.sql` | Bootstrap with real passwords substituted |

Each has a committed `.example` twin, so a fresh clone is usable without them.

Pre-push check:

```bash
git ls-files | grep -E "environment\.md|db-policy\.json|dev-db\.config\.json|\.local\.sql|\.generated/"
```

That must print nothing. Also confirm `tools/dev-db/01_bootstrap_root.sql` still contains its
two `__CHANGE_ME__` placeholders rather than real passwords.
